# Implementation Plan — ADR-156: RBAC Session Claim Refresh

**Target ADR:** `adrs/tasks/ADR-156-rbac-session-claim-refresh.md`  
**Status:** Complete  
**Date:** 2026-08-29

## Summary

Fix empty seller nav by refreshing JWT merchant claims when ownership/membership appears, plus layout hydrate permissions parity and idempotent system role seed.

## Done

1. `resolveMerchantSessionClaims` shared module
2. Wired into `src/auth.ts` + JWT callback refresh in `create-app-auth-config.ts`
3. Extended `hydrateMerchantSessionClaims` + merchant layout
4. `ensureSystemRoles` for Drizzle/PG via role use cases
5. Tests + `docs/audit/rbac-audit-implementation-report.md`
