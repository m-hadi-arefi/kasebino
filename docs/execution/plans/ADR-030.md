# Plan — ADR-030 API Architecture and Standards

## Summary

Encode MerchantOS API architecture as an importable contract in `src/api-standards/`: public JSON under `/api/v1`, Route Handler conventions calling application use cases, Zod-at-boundary validation stance, stable error envelope with `correlationId` and Persian human messages (plus code→fa map), Idempotency-Key for sale/order mutations, versioning stance, JSON camelCase vs DB snake_case note, and high-level auth/authz header expectations. Align with `docs/rules/api-rules.md` and `docs/architecture/15-api-architecture.md`. No full HTTP routes or Zod package install this cycle (first routes / ADR-027 may add Zod dependency).

## Iranian First impact

- Persian text: Yes — API `error.message` is Persian (`fa-IR`); stable English `code` + `API_ERROR_MESSAGES_FA` map for client-side resolution
- RTL: N/A for JSON keys; Persian strings are RTL-safe plain text for clients
- Jalali / تومان: N/A (formatting remains presentation when resources include dates/money)
- Iranian workflows: auth and rate-limit errors user-safe in Persian; never return OTP in production
- Mobile: payload/latency budgets noted; keep envelopes small
- Checklist: UX shell N/A; Persian API human messages in scope

## Schema / API / UI / events / cache

- No OLTP schema changes
- No live Route Handlers / UI this cycle (uiuxpromax N/A) — contract + envelope builders + asserts
- Analytics: API metrics deferred to observability ADRs; envelope always carries `correlationId` for later tracing
- Telemetry gate: correlationId only; no emit

## Domain changes

- `src/api-standards/` — versioning, surfaces, error envelope, Persian message strategy, correlationId, Idempotency-Key policy, naming (camelCase wire / snake_case DB), auth headers + AuthZ in application, OTP production ban, assert helpers + envelope factories
- Light sync: `docs/rules/api-rules.md`, `docs/architecture/15-api-architecture.md` (Persian messages, camelCase, ADR-030 pointer)

## Gates

- Telemetry: N/A emit; correlationId contract yes
- Persistence: N/A; note mapping camelCase↔snake_case at boundary/repos
- uiuxpromax: N/A
- No delivery: no delivery endpoints encoded as required

## Test matrix

- Unit: public API prefix `/api/v1`; health may stay unversioned
- Unit: error envelope shape includes `code`, Persian `message`, `correlationId`
- Unit: code→fa map resolves known codes; unknown codes get safe Persian fallback
- Unit: Idempotency-Key required for sale complete / order create paths
- Unit: JSON camelCase vs DB snake_case stance
- Unit: Auth Authorization/Bearer|cookie high-level; AuthZ in application
- Unit: never return OTP in production
- Unit: Zod boundary validation stance encoded
- Unit: correlationId generated/propagated by helpers
- `npm run validate`

## Related ARD checkboxes

- Cross-cutting API convention for all ARDs documenting `/api/v1` tables — fulfilled at standards layer; per-endpoint tables remain per-ARD

## Iranian feature checklist (in scope)

- Error messages Persian: yes (envelope + map)
- Primary UI / RTL layout / mobile UX / تومان / Jalali: N/A — no UI this ADR

## Risks

- Installing Zod solely for this ADR before first route → avoid; stance `boundaryValidation: "zod"` until ADR-027 / first handlers pull dependency
- Overbuilding OpenAPI now → Future Evolution; ARD endpoint tables remain mandatory

## Completion notes

- Implemented as planned; `npm run validate` green (161 tests)
- Iranian feature checklist: Persian API error messages + code→fa map; UI/RTL/تومان/Jalali N/A
- `src/api-standards` contract + helpers; synced `docs/rules/api-rules.md` and `docs/architecture/15-api-architecture.md`
- Zod dependency deferred until first Route Handlers / ADR-027; stance encoded
- Next: ADR-078 Testing Strategy
