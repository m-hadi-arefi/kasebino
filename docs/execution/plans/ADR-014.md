# Plan — ADR-014 Analytics Domain Boundaries OLTP vs Mongo

## Summary

Encode MerchantOS dual analytics planes as an importable contract in `src/analytics-boundaries/`: merchant operational dashboards (AN-01..04) on **PostgreSQL projections**; product/audit/warehouse/clickstream/session/management telemetry on **MongoDB**; money truth never leaves PG; Mongo never OLTP SoT; analytics never on checkout critical path. Persian + Jalali/`Asia/Tehran` + RTL notes for merchant-facing reports. Full Mongo plane → ADR-056; warehouse bridge → ADR-057; merchant OLTP widgets → ADR-063 / ARD-016.

## Iranian First gate

| Check | Impact |
| --- | --- |
| Persian text? | Merchant report titles/labels/errors Persian (`fa-IR`); event codes may stay English |
| RTL? | Charts/filter-bar layout contract `dir=rtl` for merchant analytics UX |
| Jalali? | Time buckets Jalali + `Asia/Tehran` for merchant-facing ranges |
| تومان? | Money truth from PG projections; display unit تومان (LOCALE_DEFAULTS) |
| Iranian behavior? | Merchant skimable reports; platform analytics admin-only (not merchant UX) |
| Iranian mobile? | Merchant reports tablet-skimable contract; avoid desktop-only BI for merchants |

Iranian checklist (in scope): analytics & reporting — Persian labels, Jalali ranges, RTL charts/tooltips. Full UI screens N/A (uiuxpromax N/A → ADR-063 / ARD-016).

## Gates

- Telemetry: this ADR *is* the telemetry plane boundary; emit/ingest → ADR-056+
- Persistence: dual-read rules only; no new PG tables / no Mongo client this cycle
- uiuxpromax: N/A (contract + Persian/Jalali report notes; no visual dashboards)
- Delivery: no courier/delivery
- Prerequisites: ADR-001, ADR-003 completed

## Task breakdown

1. Mark STATUS `in_progress`; write this plan
2. `src/analytics-boundaries/` — decision, plane map (PG AN-* vs Mongo capabilities), dual-read / money-truth, never-Mongo-SoT asserts, off-checkout-critical-path, admin-only platform analytics, Persian+Jalali+RTL merchant reports UX contract, deferrals to ADR-056/057/063
3. Align asserts with `PRODUCT_ARCHITECTURE.dataPlanes`, `bounded-contexts` analytics split, `ANALYTICS_CRITICAL_PATH`, compose mongo never-OLTP
4. Tests + `npm run validate` → STATUS completed → progress-log Next ADR-056
5. Foundation notes on ARD-016 / ARD-021 + docs/ards STATUS

## Schema / API / UI / events / cache

- Schema: none (projections → ADR-063 / ARD-016; Mongo collections → ADR-056+)
- API: document reserved AN-* paths (`/api/v1/analytics/*`) vs Mongo ingest (`POST /api/v1/analytics/ingest`) ownership
- UI: Persian RTL Jalali report UX contract only
- Events: warehouse mirror remains outbox `mongodb_warehouse` off critical path
- Cache: Redis TTL 60s for merchant OLTP widgets (existing cache-keys); no change this cycle

## Test matrix

- Decision: PG for AN-* merchant dashboards; Mongo for warehouse/product/audit/clickstream
- Money truth / dual-read: accounting figures from PG; engagement from Mongo
- Mongo never OLTP SoT assert
- Analytics/warehouse not on checkout critical path (align event-driven)
- Platform analytics admin-only
- Persian RTL Jalali merchant report UX contract
- Aligns with PRODUCT_ARCHITECTURE + bounded-contexts planes
- `npm run validate`

## Related ARD checkboxes

- ARD-016 — boundaries foundation; projections/APIs/UI remain
- ARD-021 — plane split foundation; Mongo ingest bridge remains (ADR-056+)

## Iranian feature checklist (in scope)

- Dashboard labels Persian (contract): yes
- Jalali / Asia/Tehran time buckets for merchant reports: yes
- Charts/tooltips RTL contract: yes
- UX screens / live charts / تومان formatting UI: N/A (deferred ADR-063)

## Risks

- Over-implementing Mongo ingest (ADR-056) or AN-* widgets (ADR-063) — keep contract-only
- Confusing ARD-016 money widgets with Mongo management dashboards — dual-read rules explicit

## Completion notes

- Implemented `src/analytics-boundaries` — dual-plane decision (PG AN-01..04 vs Mongo warehouse/product/audit/clickstream), money-truth + dual-read, never-Mongo-SoT, off-checkout-critical-path, admin-only platform analytics, Persian RTL Jalali merchant report UX contract; asserts aligned with product-architecture / bounded-contexts / event-driven / compose
- Validate green (563 tests); STATUS → completed; Next: ADR-056
- Iranian checklist: Persian labels + Jalali/`Asia/Tehran` + RTL report contract; live chart UI N/A (uiuxpromax N/A → ADR-063 / ARD-016)
