# Plan — ADR-094 HTTP /api/v1 Route Handlers

## Summary

Ship Zod-validated App Router `/api/v1` handlers for MVP domains using use cases + production Drizzle repos; envelope errors; auth via ADR-095 sessions; no business logic in routes.

## Iranian First

- Persian error messages in API envelopes for clients
- Iranian phone on auth paths (already)
- UX N/A beyond error messageFa

## Tasks

1. [x] Shared route helpers: parseBody, ok, fail, requireAuth, correlationId
2. [x] Domain routes: catalog, inventory, sales, CRM, loyalty, orders, payments, notifications, admin, merchants/stores, storefront public reads
3. [x] Wire injectable composition (`createApiContext` / `getApiContext`) for handlers (ADR-123 preview)
4. [x] Contract/unit tests for critical routes (auth validation, POS sale, storefront catalog, admin suspend)
5. [x] Idempotency-Key on sale/payment mutations
6. [x] OTP rate-limit via in-memory limiter (documented local fallback pending ADR-108 Redis)

## Implementation notes

- Handlers live in `src/infrastructure/http/**` (testable); thin `app/api/v1/**/route.ts` adapters.
- Tenant scope from session `merchantId` — body `merchantId` mismatch → 403.
- Public storefront DTOs via ADR-077 ACL helpers.
- No delivery/courier endpoints.
- ADR remains in `adrs/tasks/` until acceptance + critical-path Postgres smoke are fully signed off.

## Remaining / deferred

- Full Compose Postgres live smoke of every route (contract tests use InMemory; production path uses `createProductionRepositories`).
- Redis rate limiter wiring → ADR-108.
- Full composition root hardening → ADR-123.
- OpenAPI codegen deferred (typed contract tests shipped).

## Post-complete → ADR-113
