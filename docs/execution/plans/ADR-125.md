# Plan — ADR-125 Production UI Shell + Page Migration

## Summary

Expand shadcn primitives, build shared shells/composites (Persian RTL, ADR-020 teal tokens), migrate every frontend page, Playwright-audit all routes, deliver production-ui-redesign report. Continuous pass — no intermediate review stops.

## Iranian First impact

| Check | In scope? |
| --- | --- |
| Persian text | Yes — all chrome, empty/error/loading, nav labels |
| RTL | Yes — shells, sidebar/bottom-nav mirrored |
| Jalali | Yes — keep JalaliDateText; no Gregorian-only date picker UX |
| تومان | Yes — TomanDisplay on money surfaces |
| Iranian behavior | OTP login, store switcher, POS keypad |
| Iranian mobile | Bottom nav + ≥44px taps; POS thumb-zone CTAs |

## uiuxpromax gate evidence

**Binary:** `uiuxpromax` CLI not present. Gate via `uiuxpromax-integration` + `docs/uiux/*` + skill `ui-ux-pro-max` (palette CLI suggestion **rejected** — keep teal daylight).

### Brief (Persian + RTL)

| Item | Detail |
| --- | --- |
| Surface | کل فرانت‌اند: فروشنده، ادمین، ویترین، مارکتینگ |
| Personas | مغازه‌دار / صندوقدار / مشتری فروشگاه / اپراتور پلتفرم |
| Shell | دسکتاپ: سایدبار؛ موبایل: ناوبری پایین؛ صندوق: تمام‌صفحه فشرده |
| Brand | هویت کاسبینو — فیروزه‌ای روشن ADR-020؛ نه بنفش/کرم جنیریک |
| Screens | همهٔ مسیرهای merchant/admin/storefront/marketing + `/ui-kit` |
| States | بارگذاری (Skeleton)، خالی، خطا — کپی فارسی |
| Nav (merchant) | داشبورد، صندوق، سفارش‌ها، مشتریان، محصولات، موجودی، وفاداری، اعلان‌ها، فروشگاه‌ها |
| Component map | shadcn primitives + PageHeader / EmptyState / ErrorState / LoadingState / ConfirmDialog / FilterBar / StatCard / DataTable / AppSidebar / AppBottomNav / AppTopbar / StorefrontChrome |
| Density | تحلیلی: فاصله استاندارد؛ POS: فشرده + نوار تسویه چسبان |
| A11y | فوکوس Radix، landmarks، بدون ایموجی به‌جای آیکن (Lucide) |
| Motion | محاورهٔ dialog/sheet؛ hover ۱۵۰–۳۰۰ms؛ `prefers-reduced-motion` |
| Mobile mock | ≈۳۹۰px Android؛ تبلت ۷۶۸؛ دسکتاپ ۱۴۴۰ |

## Decision being implemented

ADR-125 FR-1..FR-6.

## Task breakdown

1. Expand shadcn primitives; update `mvp-primitives.ts` + tests.
2. Token polish in `globals.css` (shadow-md, focus, reduced-motion).
3. Build layout composites + wire merchant/admin/storefront shells.
4. Migrate all pages (auth → merchant → POS → admin → storefront → marketing → ui-kit).
5. Install Playwright; route audit loop; screenshots under `docs/uiux/audits/`.
6. `npm run validate`; write `docs/uiux/audits/production-ui-redesign-report.md`.
7. Update `adrs/STATUS.md` + progress log.

## Playwright auth strategy

- Public: marketing, login, ui-kit, storefront catalog (demo slug env `E2E_STORE_SLUG` or soft-skip).
- Gated: optional `E2E_STORAGE_STATE` path; if missing, document as debt and audit reachable public shells + login forms.

## Gates

- typecheck + lint + vitest
- Iranian checklist for shells
- Playwright smoke for route manifest (best-effort on gated)

## Out of scope

Dark mode, rebrand, new features, Gregorian calendar as SoT.
