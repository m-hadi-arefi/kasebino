# Plan — ADR-046 Migration Strategy Drizzle Kit

## Summary

Encode MerchantOS OLTP migration strategy as an importable contract in `src/migration-strategy/`: Drizzle Kit is the only path for versioned SQL under `src/infrastructure/database/migrations/`; forward-only in production; expand/contract for breaking changes; migrate job before app traffic; CI applies on staging; lock-aware review; never hand-write baseline SQL outside drizzle-kit; never lose Persian UTF-8 data. Solidify `drizzle.config.ts` out path + `db:generate` / `db:migrate` scripts. Ensure migrations folder exists (empty until domain schema generates). Assert helpers + Vitest.

## Iranian First impact

- Persian text: migrations must preserve UTF-8 Persian product/customer text; no ASCII-only collations or encoding changes that corrupt `fa` text
- RTL: N/A at SQL migration layer (presentation remains RTL)
- Jalali: timestamps stay UTC `timestamptz` in DDL; display → Asia/Tehran + Jalali in presentation
- تومان: money columns remain integer IRR minor units through migrations; تومان formatting stays presentation
- Iranian workflows: tenant DDL (merchant_id) and catalog text survive expand/contract safely
- Mobile: lock-aware / online-index guidance protects POS latency during large future DDL
- Checklist: UX N/A (infra/contract-only); Persian data-preservation rules encoded on contract

## Database design

- No new concrete domain tables in this ADR (schema remains empty until domain ADRs)
- Migrations folder: `src/infrastructure/database/migrations/` (drizzle-kit `out`)
- Process: design/ARD → schema change → `npm run db:generate` → review SQL (locks, indexes, NOT NULL backfills, Persian safety) → CI/staging migrate job → prod migrate job before traffic
- Forward-only production (no destructive down migrations on prod)
- Expand/contract for breaking column/type renames
- Forbidden: hand-authored baseline SQL outside drizzle-kit; auto-migrate on app boot in production
- Online index builds noted as future evolution for large tables

## Domain changes

- `src/migration-strategy/` — tool=drizzle-kit, folder paths, scripts, forward-only, expand/contract, deploy order, CI staging, lock awareness, Persian data safety, forbidden practices, assert helpers
- Touch-ups: schema index comment points at ADR-046 strategy; ADR-042 scripts comment no longer “stub deferred”
- No repository / domain table implementations

## API / UI / events / cache

- No HTTP routes or UI (uiuxpromax N/A)
- No event schemas
- No cache changes

## Gates

- Telemetry: N/A (migration process contract only)
- Persistence: drizzle-kit migrate strategy; soft-delete depth ADR-047; CI wiring depth may grow with deploy ADRs
- uiuxpromax: N/A (no UI)
- No delivery: N/A

## Test matrix

- Unit: migrations folder path matches drizzle.config `out` and ADR-042 placement
- Unit: generate/migrate npm scripts are `drizzle-kit generate` / `drizzle-kit migrate`
- Unit: forward-only in production; no auto-migrate on boot prod
- Unit: expand/contract required for breaking changes
- Unit: migrate job before traffic; CI apply staging
- Unit: lock awareness / review checklist flags present
- Unit: no baseline SQL outside drizzle-kit; no hand-written SQL as SoT
- Unit: Persian UTF-8 data must not be lost; ASCII-only collation changes forbidden
- Unit: assert helpers throw on violations
- Unit: migrations directory exists on disk
- `npm run validate`

## Related ARD checkboxes

- Future ARDs: use this contract for “Drizzle migrations generated and reviewed”
- ARD-001 / ARD-019: migrate-job pattern documented; concrete domain baseline still awaits domain tables

## Risks

- Generating empty/noisy SQL with empty schema → keep folder + `.gitkeep`; generate only when schema has tables
- Over-encoding deploy platform details → keep CI/staging as contract; K8s job YAML later
- Drift from ADR-042 placement → import `DRIZZLE_PLACEMENT` / scripts constants

## Completion notes

- Implemented as planned; `npm run validate` green (120 tests)
- Iranian feature checklist: UX N/A (infra/contract-only); Persian UTF-8 data-preservation + no ASCII-only collations encoded; timestamps/money policies unchanged
- `src/migration-strategy` contract: drizzle-kit exclusive; migrations folder aligned with `drizzle.config.ts` out; forward-only prod; expand/contract; migrate-before-traffic + CI staging; lock awareness; no baseline SQL outside kit
- Migrations directory present (README + `.gitkeep`); no hand-authored baseline SQL; domain DDL awaits domain schema ADRs
- Next: ADR-047 Data Integrity Soft Delete and Audit Fields
