# Plan — ADR-031 Merchant Authentication Architecture

## Summary

Encode passwordless **phone OTP** merchant authentication foundations: importable decisions contract in `src/merchant-auth/` (OTP-first, no password MVP, rate-limit notes, AUTH-* / JWT deferral) plus Identity bounded-context ports and domain/application logic under `src/modules/identity/` — Iranian MSISDN normalization, OTP challenge aggregate, `SmsPort` with console/mock adapters (ADR-083 Proposed), Persian auth error messages. No NextAuth session (ADR-033), no Drizzle OLTP schema/migration this cycle, no UI (uiuxpromax N/A).

## Iranian First impact

- Persian text: Yes — auth error messages and SMS OTP template copy in Persian (`fa-IR`)
- RTL: UI N/A this ADR; contract notes RTL-first login/OTP layouts for future screens
- Jalali / تومان: N/A (no user-facing dates/money surfaces)
- Iranian workflows: SMS OTP, no password MVP; shopkeeper phone norms
- Mobile: Iranian `09xxxxxxxxx` / `+98` normalization; OTP numeric entry friendly noted for UI ADRs
- Checklist: Auth & communications (Persian OTP/errors/phone validation) in scope; UI shell/RTL layout N/A

## Schema / API / UI / events / cache

- Persistence: repository **ports** only (`AuthUserRepository`, `OtpChallengeRepository`); Drizzle tables/migrations deferred to ARD-002 persistence pass / later ADRs
- API routes: not wired this cycle (handlers + NextAuth → ADR-033); path contracts noted in merchant-auth
- UI: none — uiuxpromax N/A
- Events: domain event shape `MerchantLoggedIn` (verify success); analytics funnel emit deferred
- Cache/rate-limit: policy constants documented (OTP 3/min, auth 5/min); Redis middleware → ADR-055
- SMS: `SmsPort` + console/mock adapters only — no real Iranian provider until ADR-083 Accepted

## Domain / application changes

- `src/merchant-auth/` — OTP-first / no-password MVP, AUTH-01..06 binding notes, rate-limit notes, env OTP disclosure rules, JWT→ADR-033, assert helpers
- `src/modules/identity/domain/` — Iranian phone VO helpers, `OtpChallenge`, repository ports, `MerchantLoggedIn` event
- `src/modules/identity/application/` — `SmsPort`, Persian `MerchantAuthError`, request/verify OTP use cases (in-memory-friendly)
- `src/modules/identity/infrastructure/sms/` — `ConsoleSmsAdapter`, `MockSmsAdapter`
- Upgrade `src/shared/domain/phone-number.ts` to use Iranian normalization (or re-export identity helpers)

## Gates

- Telemetry: Auth funnel noted; no warehouse emit this cycle
- Persistence: ports only; Drizzle when ARD-002 schema lands
- uiuxpromax: N/A (no pages/components)
- No delivery: customer vs merchant identity remain separate; no delivery auth

## Test matrix

- Unit: normalize `0912…`, `912…`, `+98912…`, `0098912…` → E.164 / national forms; reject invalid with Persian error
- Unit: merchant-auth contract — passwordless MVP, OTP rate 3/min, never OTP in production
- Unit: request OTP creates challenge + dispatches via SmsPort mock (no provider HTTP)
- Unit: verify OTP success/failure; Persian `messageFa` strings
- Unit: console/mock adapters exist; no production SMS SDK import
- `npm run validate`

## Related ARD checkboxes

- ARD-002 Authentication — partial foundations (domain/ports/SMS mock); remain `todo` until JWT + routes + UI + Drizzle + Redis rate limits

## Iranian feature checklist (in scope)

- Error messages Persian: yes
- Phone validation Persian: yes
- SMS OTP template Persian: yes (adapter payload)
- Primary UI / RTL layout / mobile shell / تومان / Jalali: N/A — no UI this ADR
- RTL note recorded for future login screens

## Completion notes

- Implemented as planned; `npm run validate` green (182 tests)
- Iranian feature checklist: Persian auth errors + SMS template + Iranian phone; UI/RTL shell / تومان / Jalali N/A (uiuxpromax N/A)
- `src/merchant-auth` contract + `src/modules/identity` OTP foundations; SmsPort console/mock only (ADR-083 Proposed)
- Drizzle schema / NextAuth JWT / Route Handlers deferred (ARD-002 remainder + ADR-033)
- Next: ADR-033 NextAuth JWT Strategy
