# Plan — ADR-082 Pickup-Only Fulfillment MVP Decision

## Summary

Encode the binding MVP fulfillment decision as a testable **`src/pickup-only/`** contract: only in-store pickup; forbid delivery/courier/rider/shipping routes and capabilities without a superseding ADR. Align assert helpers with `product-architecture` (ADR-001) + `ordering-domain` (ADR-011); restate ADR-091 timers (unpaid 30m / ready hold 24h, no silent refund). Complements ADR-015 scope guardrails with a fulfillment-specific reinforcement layer.

## Iranian First gate

| Check | Result |
| --- | --- |
| Persian text? | Yes — surface Persian pickup labels/instructions from ordering contract (آماده تحویل، فقط دریافت حضوری، …) |
| RTL? | N/A UI this cycle — contract forbids delivery-oriented LTR shipping UIs; timelines/CTAs → ARD-034 + uiuxpromax |
| Jalali? | N/A — no date display surface |
| تومان? | N/A — money unchanged; timers only |
| Iranian behavior? | Pickup-only neighborhood retail; never courier-first |
| Iranian mobile? | N/A domain contract; customer navigates to store / merchant prep deferred to ARD-034 |

## Task breakdown

1. `src/pickup-only/` — decision snapshot: `fulfillmentType=pickup`, forbidden delivery-class capabilities, forbidden URL segments, timers, Persian pickup copy refs, supersede policy
2. Assert helpers: `assertPickupOnlyFulfillment` (align product + ordering), `assertPickupOnlyCapability`, `assertPickupOnlyRoute` / path segments, `assertNoDeliveryAnywhere`
3. Unit tests: forbidden capabilities rejected; forbidden routes/segments rejected; fulfillment modes aligned with product-architecture + ordering; timers 30m/24h; Persian pickup labels present; allowed pickup passes
4. Validate → STATUS completed → progress-log **Next: ADR-012**
5. ARD-034 — note foundations only; remain `todo` until checkout/board UI

## Schema / API / UI / events / cache

- Persistence: N/A (no shipping tables already locked by ADR-011)
- API: reject delivery route segments/capabilities; full routes → ARD-034/011
- UI: none — uiuxpromax N/A (UX hides delivery deferred)
- Events: pickup funnel only; OrderDelivered remains outOfMvp
- Cache: N/A

## Gates

- Telemetry: N/A (analytics funnel policy noted; warehouse later)
- Persistence: N/A
- uiuxpromax: N/A
- No delivery: **this ADR**

## Test matrix

- Default / only allowed fulfillment = `pickup`
- `assertPickupOnlyFulfillment("delivery"|"courier"|"shipping")` throws ADR-082
- `assertPickupOnlyCapability` rejects delivery/courier/rider_fleet/shipping
- `assertPickupOnlyRoute` rejects delivery/courier/shipping/rider URL segments and path fragments
- Alignment: product `ALLOWED_FULFILLMENT_MODES` === ordering modes; forbidden modes ⊆ product forbidden
- Timers: unpaid 30m; ready hold 24h; `noShowSilentRefund === false`
- Persian labels/copy include آماده تحویل + فقط دریافت حضوری
- `npm run validate` green

## Related ARD checkboxes

- ARD-034 — pickup-only decision foundations; remain `todo` until API + UI + notifications

## Iranian feature checklist (in scope)

- Persian pickup status/instruction strings on contract: yes
- Pickup-only Iranian retail workflow: yes
- Primary UI / RTL / mobile shell / Jalali / تومان screens: N/A

## Risks

- Duplicating forbidden lists → re-export/align with product-architecture + app-router segments; ADR-082 owns fulfillment supersede narrative

## Completion notes (2026-08-03)

- Implemented as planned; `npm run validate` green (493 tests)
- Iranian feature checklist: Persian pickup copy on contract (آماده تحویل، فقط دریافت حضوری); UI/RTL/Jalali/تومان N/A; pickup-only Iranian retail workflow encoded
- `src/pickup-only` — assertPickupOnlyFulfillment / Capability / Route (+ aliases) / assertNoDeliveryAnywhere; timers from ADR-091; aligned with ADR-001 + ADR-011
- STATUS → `completed`
- Next: ADR-012 Payment Domain
