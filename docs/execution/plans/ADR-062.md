# Plan — ADR-062 Management Dashboard Analytics

## Summary

Encode MerchantOS **management dashboard analytics** in `src/mgmt-dashboard-analytics/`: platform_admin metrics from Mongo `mos_mgmt` rollups (activation / engagement DAM·MAM / commerce GMV proxy / reliability / trust & safety), with explicit **GMV instrument notes** that money reconciles to PostgreSQL (never Mongo as accounting SoT), Persian widget/metric labels, Jalali/`Asia/Tehran` bucket stubs, freshness SLAs, and audited platform_admin access. In-memory rollup store for tests; HTTP APIs / uiuxpromax admin charts → ARD-025 / ADR-089.

## Iranian First gate

| Check | Impact |
| --- | --- |
| Persian text? | Dashboard titles, legends, metric labels Persian (`MGMT_TITLES_FA` / `MGMT_METRIC_LABELS_FA`); event codes English |
| RTL? | Chart UI deferred (uiuxpromax → ADR-089 / ARD-025); stubs declare `dir=rtl` / `fa-IR` |
| Jalali? | Rollup period keys ISO-safe; Jalali/`Asia/Tehran` presentation stubs for admin reports |
| تومان? | GMV proxy labeled تومان for display; money truth note points to PG reconciliation |
| Iranian behavior? | Portfolio ops KPIs for SaaS leadership — not merchant AN-* OLTP widgets |
| Iranian mobile? | Tablet-skimable notes; avoid desktop-only BI for admins |

Iranian checklist: UX screens N/A (infra + labels + stubs). Persian titles + RTL stubs + Jalali presentation stub + تومان/source labels required. Full admin UI → ARD-025 / ADR-089 + uiuxpromax.

## Gates

- Telemetry: this ADR *is* management rollup analytics (`mos_mgmt`); inputs from warehouse/product streams (documented); optional DashboardWidgetViewed → ADR-059 later
- Persistence: Mongo `mos_mgmt` rollups; PG selective money reconciliation stance only; in-memory this cycle
- uiuxpromax: N/A (no admin dashboard pages this cycle; Persian labels/contracts only)
- Delivery: no courier/delivery GMV breakdown
- Prerequisites: ADR-057, ADR-059 completed; ADR-056 `mos_mgmt` collection locked; ADR-014 platform_admin audience

## Task breakdown

1. STATUS `in_progress`; write this plan
2. `src/mgmt-dashboard-analytics/` — decision, metric groups, DAM/MAM/GMV instrument notes, freshness SLAs, Persian labels, platform_admin + audit gates, rollup document schema, InMemory `mos_mgmt` store + overview/activation/engagement aggregates, asserts
3. Flip mongodb-analytics / analytics-boundaries management pointers; ARD-025 / ARD-021 foundation notes
4. Tests + `npm run validate` → STATUS completed → progress-log Next: ADR-064

## Schema / API / UI / events / cache

- Schema: logical `mos_mgmt` docs `{ period, metric, merchantId?, value, source, moneySource?, occurredAt }` — indexes on `period+metric`
- API: reserve `/api/v1/admin/mgmt/overview|activation|engagement` (ARD-025); HTTP wire → ARD-025
- UI: none this cycle (Persian titles ready); uiuxpromax → ADR-089 / ARD-025
- Events: consume warehouse/product for rollups; emit `MgmtRollupUpdated` (catalog note); ingest is batch/job not checkout path
- Cache: Redis TTL 60–900s on mgmt widgets (documented only)

## Test matrix

- Collection is `mos_mgmt` (ADR-056); never OLTP / money SoT
- platform_admin audience enforced; non-admin throws
- DAM/MAM instrument notes present; GMV labeled proxy vs PG reconciliation
- Overview/activation/engagement aggregates from in-memory rollups
- Persian titles include Persian script; RTL stubs; Jalali presentation stub
- Freshness SLAs documented (live ≤1m / standard ≤15m / daily T+1)
- Money figures carry source label (`postgresql` vs `mongo_proxy`)
- Off checkout critical path
- Pointer flips + `npm run validate`

## Related ARD checkboxes

- ARD-025 — rollup foundations + Persian labels + platform_admin gates + API path reserve; HTTP / uiuxpromax / live driver remain
- ARD-021 — management plane consumer note
- ARD-018 — embed later; not this cycle

## Iranian feature checklist (in scope)

- UX copy / RTL screens / mobile / تومان display UI: N/A — titles + stubs only
- Persian dashboard labels: required
- Jalali/`Asia/Tehran` presentation stub: required (full picker → ARD-025 UI)
- Analytics & reporting labels: Persian titles checked
- GMV source labeled (PG vs proxy): required

## Risks

- Over-implementing HTTP Route Handlers / chart UI / live Mongo driver → keep contract + in-memory
- Treating Mongo GMV as accounting truth → violate dual-read; label proxy + PG reconcile stance
- Confusing with ADR-063 merchant OLTP dashboards — keep platform_admin + `mos_mgmt` only
- Blocking POS on rollup jobs — must stay off checkout critical path

## Completion notes

- Implemented `src/mgmt-dashboard-analytics` — Mongo `mos_mgmt` rollups, platform_admin + audited access, DAM/MAM/GMV instrument notes (GMV proxy reconciles to PG), freshness SLAs, Persian titles/labels, Jalali/`Asia/Tehran` stub, overview/activation/engagement builders (in-memory); flipped mongodb-analytics / analytics-boundaries pointers; ARD-025 / ARD-021 foundation notes
- Validate green (644 tests); STATUS → completed; Next: ADR-064
- Iranian checklist: UX screens N/A (uiuxpromax N/A → ADR-089 / ARD-025); Persian titles + RTL stubs + Jalali presentation stub + تومان/source labels tested; HTTP APIs / live rollup jobs deferred to ARD-025
