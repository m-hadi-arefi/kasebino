# Plan — ADR-004 Modular Monolith Strategy

## Summary

Encode Phase 1 **modular monolith** topology as an importable contract: single Next.js deployable, module boundaries under `src/modules/*`, no cross-module DB joins in domain services, transactional outbox as the integration spine for extraction, shared security/Persian-RTL shell providers, workers share the same codebase.

## Iranian First impact

- Persian/RTL: shared app-shell i18n + RTL strategy documented (enforced once for all modules; UI not built yet)
- Jalali/تومان: owned by presentation defaults (ADR-001); modules must not invent diverging display rules
- Iranian workflows: modules prioritize POS + storefront; no delivery module
- Checklist: UI N/A for runtime surfaces; shell localization contract tested

## Database design

- N/A schemas; rule encoded: domain services must not perform cross-module SQL joins (composition in application layer / events)

## Domain changes

- `src/modular-monolith/` — topology, boundary rules, outbox spine, shared middleware slots, extraction readiness
- Module registry aligned with ADR-002 `BOUNDED_CONTEXT_MODULES`
- Forbidden: microservices split, delivery module

## API / UI / events / cache

- Shared security middleware slot at composition root
- Persian i18n + RTL providers at app shell (contract only until Next.js ADR-016)
- One outbox feeds many consumers (events → EMQX / Mongo / cache later)

## Gates

- Telemetry: outbox may fan-out to analytics consumers; analytics not on critical path (policy note)
- Persistence: N/A tables
- uiuxpromax: N/A (no UI surfaces)
- No delivery: module list must not include delivery

## Test matrix

- Unit: deploy model = single modular monolith
- Unit: all ADR-002 modules registered; no delivery
- Unit: domain forbidden cross-module joins rule
- Unit: outbox is integration spine; workers share codebase
- Unit: shell locale defaults fa-IR + RTL
- Unit: extraction order documented and non-empty
- `npm run validate`

## Related ARD checkboxes

- Note in progress log (ARD-001 scaffold reference)

## Risks

- Over-coding Next.js shell before ADR-016 → keep contract-only for shell providers
- Premature microservices → forbid until ADR-020 extraction doc criteria met

## Completion notes (2026-08-03)

- Implemented as planned; `npm run validate` green (33 tests)
- Iranian feature checklist: UI N/A; shared fa-IR + RTL shell contract encoded
- STATUS → `completed`
