# Plan — ADR-076 Security Architecture

## Summary

Implement the **Security Architecture** baseline as a binding contract in `src/security-architecture/`: defense-in-depth controls map (authn/authz, tenant isolation, secrets, OTP/token log hygiene, HTTPS + secure cookies, CSRF/XSS/CSP stance, parameterized SQL, validation, audit + rate-limit hooks), Iranian SMS OTP abuse threat notes, Persian user-facing security warning copy, and assert helpers for DoD / ARD security checklists. No UI, no middleware wiring (rate limits → ADR-055; audit wire → ADR-058; pen smoke → ARD-020).

## Iranian First impact

- Persian text: Yes — security block/challenge warnings (`SECURITY_USER_MESSAGES_FA`); no English-only scary blocks for merchants/customers
- RTL: UI N/A this cycle (security settings pages RTL when UI ADR lands; note documented)
- Jalali: N/A (no date display surfaces)
- تومان: N/A
- Iranian workflows: SMS OTP abuse / credential stuffing threat model; OTP never logged; Iranian phone auth safety
- Mobile: HTTPS secure cookies for Iranian mobile browsers (re-export ADR-033 cookie rules)
- Checklist: Auth & communications + Persian security messages in scope; UI/RTL shell N/A

## Schema / API / UI / events / cache

- Persistence: none
- API routes / middleware: deferred (headers checklist + Zod-at-boundary stance only)
- UI: none — uiuxpromax N/A
- Events: audit/security monitoring hooks reference ADR-058 / ARD-026 (non-blocking)
- Cache: N/A (rate-limit Redis keys referenced via ADR-055 / merchant-auth)

## Domain / application changes

- `src/security-architecture/` — decision snapshot, threat→control map, control pillars, security headers checklist, OTP/log hygiene, assert helpers, Iranian OTP safety notes
- Cross-ref existing contracts: `rbac`, `multi-tenant-isolation`, `env-secrets`, `nextauth-jwt` cookies, `api-standards` SECURITY_API_RULES, `merchant-auth` OTP policy, `drizzle-orm-strategy` parameterized SQL
- Sync `docs/architecture/06-security-architecture.md` lightly if headings need contract alignment (minimal)

## Gates

- Telemetry: security monitoring / audit non-blocking; concrete → ADR-058 / ARD-026
- Persistence: N/A
- uiuxpromax: N/A (no pages)
- No delivery: N/A
- Prerequisites ADR-055/058 still todo — contract documents deferred enforcement ports (roadmap PARALLEL / progress-log order)

## Test matrix

- Unit: threat controls map covers OTP spam, session theft, XSS, CSRF, SQLi, IDOR, SSRF
- Unit: authn OTP→JWT + authz RBAC boundary asserted
- Unit: tenant isolation + secrets env-only + OTP/tokens never logged
- Unit: HTTPS + secure cookie rules alignment with ADR-033
- Unit: security headers checklist present (CSP, HSTS staging/prod, etc.)
- Unit: Persian security user messages contain Persian script
- Unit: assert helpers reject unsafe log payloads containing OTP/raw tokens
- `npm run validate`

## Related ARD checkboxes

- ARD-020 Production Hardening — pen smoke / full checklist remain for M6; this ADR supplies baseline Security DoD contract
- ARD-026 Security Monitoring — signal hooks noted; remain todo
- ARD-022 Audit — insert-only audit referenced; remain todo pending ADR-058

## Iranian feature checklist (in scope)

- Auth & communications: OTP not logged; Persian security warnings; phone OTP abuse threat
- Primary UI / RTL / mobile shell / تومان / Jalali display: N/A — contract only

## Risks

- Rate-limit / audit still deferred — must not claim wired middleware
- Full CSP/HSTS application deferred to Next.js middleware / ARD-020

## Completion notes

- Implemented as planned; `npm run validate` green (234 tests)
- Iranian feature checklist: Persian security messages + OTP/log hygiene + Iranian OTP abuse threat; UI/RTL/تومان/Jalali N/A (uiuxpromax N/A)
- `src/security-architecture` contract + tests; 06-security-architecture.md ADR pointer
- Rate-limit / audit / pen-smoke remain deferred (ADR-055 / ADR-058 / ARD-020)
- STATUS → `completed`
- Next: ADR-055 Rate Limiting Strategy
