# Plan — ADR-050 Search and Barcode Scanning Strategy

## Summary

Implement POS search/barcode strategy: `src/search-barcode/` decision contract (B-tree unique merchant+barcode, Redis barcode cache TTL 300s, pg_trgm/client fuzzy path, BarcodeDetector + fallback, scan analytics stubs, NFR budgets). Wire Catalog application `lookupByBarcode` / `searchByName` with tenant scope and lightweight Persian-aware normalize (trim + Eastern/Western digits). No UI this cycle.

## Iranian First gate

| Check | Result |
| --- | --- |
| Persian text? | Yes — UTF-8 fa product name search; Persian empty/scan error messages on contract |
| RTL? | N/A — no UI (uiuxpromax deferred; presentation still RTL later) |
| Jalali? | N/A — no date display |
| تومان? | N/A this ADR — price display already ADR-008 |
| Iranian behavior? | Barcode + name search for local shop catalogs; Persian/Arabic digits normalize |
| Iranian mobile? | Query budgets protect POS mobile latency (≤1s barcode / ≤100ms search feel) |

## Task breakdown

1. `src/search-barcode/` — decision snapshot, index/cache/client scan strategy, normalize helpers, analytics event names, NFR budgets, Persian messages
2. Catalog application — `lookupByBarcode`, `searchByName` using normalize + merchantId tenant scope; soft-delete excluded by default
3. Optionally extend product port with `searchByName` or implement via `listByMerchantId` filter (in-memory OK)
4. Sync deferred pointers: catalog-domain, indexing-standards, query-design-standards (deferred → implemented)
5. Tests: barcode lookup tenant isolation; fa product name find; digit normalize; soft-delete excluded; strategy contract asserts
6. Validate → STATUS completed → progress-log Next ADR-049

## Schema / API / UI / events / cache

- Persistence: unique `(merchant_id, barcode)` already on schema stub; pg_trgm GIN deferred to ARD migration
- API: deferred ARD-005/007 routes
- UI: none — BarcodeDetector contract only; uiuxpromax N/A
- Events: `BarcodeScanSucceeded` / `BarcodeScanFailed` stubs (warehouse emit later)
- Cache: barcode key TTL 300s (`mos:{env}:{merchantId}:barcode:{barcode}`)

## Test matrix

- `lookupByBarcode` finds product for merchant; miss returns null / Persian not-found
- Same barcode different merchant does not leak (tenant scope)
- Soft-deleted product excluded from lookup/search
- `searchByName` finds fa product ("نان بربری") by substring after normalize
- Digits: Persian/Arabic-Indic digits normalize for barcode equality
- Strategy: TTL 300; budgets; no Elasticsearch MVP
- `npm run validate`

## Related ARD checkboxes

- ARD-005 / ARD-007 — search/barcode wiring foundations; remain `todo` until POS/catalog API + cameras

## Iranian feature checklist (in scope)

- Persian product text search without corruption: yes
- Search handles Persian meaningfully (normalize trim/digits + contains): yes
- Error / empty messages Persian on contract: yes
- Primary UI / RTL / mobile shell / Jalali / تومان: N/A — strategy + application only

## Completion notes

- Implemented as planned; `npm run validate` green (269 tests)
- Iranian feature checklist: Persian search messages + UTF-8 fa name find + trim/digit normalize; UI/RTL/Jalali/تومان N/A (uiuxpromax N/A)
- `src/search-barcode` contract; catalog `lookupByBarcode` / `searchByName` tenant-scoped
- Cache TTL 300s + scan analytics stubs; pg_trgm GIN / camera UI → ARD-005/007
- STATUS → `completed`
- Next: ADR-049 Inventory Synchronization Strategy

## Gates

- Telemetry: scan success/fail event name stubs; warehouse emit later
- Persistence: index contract; trgm DDL → ARD migrations
- uiuxpromax: N/A
- No delivery: N/A
