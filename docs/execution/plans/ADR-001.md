# Plan — ADR-001 Product Architecture (Store-First Retention OS)

## Summary

Encode the Accepted Decision as a versioned, testable **product architecture contract** that later ADRs import: store-first ownership, pickup-only fulfillment, no marketplace, dual data planes (PG OLTP / Mongo analytics), and Iranian-native UX defaults. No Next.js app shell yet (ADR-016). No Docker Compose yet (ADR-066). No UI (uiuxpromax N/A).

## Iranian First impact

- Persian text: default locale `fa-IR`; user-facing message strategy noted in contract (Persian at presentation)
- RTL: default `dir=rtl` recorded as product default
- Jalali: calendar policy `jalali` + `Asia/Tehran` as UX default
- تومان: default display currency unit `toman`
- Iranian behavior / mobile: SMS OTP, QR, pickup, POS phone capture encoded as primary acquisition/fulfillment modes
- Checklist items in scope: mostly **N/A — no user-facing UI in this ADR**; locale defaults are locked in code for dependents

## Database design (first)

- N/A for ADR-001 (no tables). Dual-plane policy: PostgreSQL=OLTP SoT, MongoDB=analytics/audit/telemetry only.

## Domain changes

- Product-level enums/constants only (not aggregates):
  - Ownership surface: store-first
  - Fulfillment modes allowed: `pickup` only
  - Forbidden: delivery, courier, marketplace browsing, full accounting
  - Multi-store: enabled (ADR-091)
  - Storefront URL pattern: `/s/{storeSlug}`
  - Locale defaults Iranian First

## API changes (Persian human messages strategy)

- None. Contract documents that human-readable API messages will be Persian (ADR-030).

## Events / cache / realtime

- N/A (instrumentation named as future loops only: QR, PWA, loyalty, membership).

## UI (uiuxpromax)

- N/A — no UI surfaces.

## Test matrix

- Unit: allow only pickup fulfillment
- Unit: reject delivery/marketplace capability flags
- Unit: Iranian locale defaults present
- Unit: multi-store enabled; storefront path pattern
- `npm test` / typecheck / lint green on minimal package

## Related ARD checkboxes

- ARD-001 remains `todo` (foundation spans ADR-016/066/041/…) — only note alignment in progress log

## Risks

- Over-scaffolding Next.js early → avoid; keep minimal TS package
- Thin ADR → ensure tests make Decision enforceable for agents

## Completion notes (2026-08-03)

- Implemented as planned; `npm run validate` green
- Iranian feature checklist: UI/mobile/notification items **N/A — no user-facing surface**; locale defaults enforced in `LOCALE_DEFAULTS`
- STATUS → `completed`
