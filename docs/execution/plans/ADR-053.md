# Plan — ADR-053 Cache Key and TTL Standards

## Summary

Ship canonical **cache key builders** and **TTL class table** in `src/cache-keys/`: pattern `mos:{env}:m:{merchantId}:{domain}:{resource}:{id}` (tenant/`merchantId` always present on business keys); TTL classes entity 300s / analytics(dashboard) 60s / storefront 600s; membership/wallet keys include `storeId`; wire pointers from redis-architecture + cache-aside hints. Invalidation event→key maps remain ADR-054.

## Iranian First impact

- Persian text: N/A UX screens; keys stay ID-based (no fa in key segments)
- RTL: N/A visual
- Jalali: N/A
- تومان: N/A
- Iranian workflows: OTP/rate-limit keys stay on ADR-055 `mos:{env}:rl:…` (separate from business tenant keys)
- Mobile: class TTLs keep hot POS/storefront reads responsive without sticky forever caches
- Checklist: UX N/A (infra/key contract); Unicode values unchanged (ADR-052 serializers)

## Schema / API / UI / events / cache

- No OLTP schema changes
- No HTTP routes or UI (uiuxpromax N/A)
- No event invalidation map (ADR-054)
- Cache: key builders + `CACHE_TTL_SECONDS` + per-resource TTL table (store/product/dashboard + storefront/analytics siblings)
- Sync: `KEY_NAMESPACE` / `REDIS_REQUIREMENTS.keyNamespaceDefer053` → realized; `docs/tech/redis.md` + `cache-strategy.md` wallet/store alignment

## Domain / application changes

- `src/cache-keys/` — pattern, TTL table, builders (store/product/dashboard + catalog examples), `assertMerchantIdInKey` / `assertPositiveTtl`
- `src/cache-aside` — import canonical TTL class seconds from cache-keys
- `src/redis-architecture` — package pointer + defer flag flip
- Optional: `BARCODE_CACHE.keyHint` align to `m:` segment

## Gates

- Telemetry: N/A emit
- Persistence: keys never replace SoT; Redis remain non-SoT
- uiuxpromax: N/A
- No delivery: N/A
- Prerequisites: ADR-052 completed

## Test matrix

- Unit: pattern segments / separators; merchantId required (reject empty / missing tenant)
- Unit: store → 300s; product → 300s; dashboard/analytics → 60s; storefront → 600s
- Unit: built keys contain `mos`, env, `m`, merchantId; wallet/membership include storeId
- Unit: `assertMerchantIdInKey` rejects cross-tenant key misuse
- Unit: cache-aside TTL class hints match cache-keys canon
- `npm run validate`

## Related ARD checkboxes

- ARD-001 note: cache key/TTL contract landed; domain adapters still wire call sites
- ARD-003/004/005/010/013/016 TTL numbers already documented — code canon now exists

## Iranian feature checklist (in scope)

- UX copy / RTL / mobile shell / تومان / Jalali: N/A — no user-facing surfaces
- Keys ID-based; Persian payload safety deferred to serializers (ADR-052) — documented

## Risks

- Pre-existing ad-hoc keys missing `m:` or store on wallet → builders become source of truth; call sites migrate gradually
- Over-building full catalog of every key in cache-strategy → ship builders for class + core resources named in ADR/user brief; others via generic builder

## Completion notes

- Implemented as planned; `npm run validate` green (347 tests)
- Iranian feature checklist: UX N/A (key/TTL contract only); keys ID-based; Persian payload safety remains ADR-052 serializers
- `src/cache-keys` — pattern `mos:{env}:m:{merchantId}:…`, TTL table (store/product 300s, dashboard 60s, storefront 600s), builders, tenant asserts; wallet/membership include storeId
- Pointers: redis-architecture defer flip; cache-aside TTL classes import canon; cache-strategy wallet keys + redis.md folder note
- Invalidation → ADR-054
- STATUS → `completed`
- Next: ADR-054 Cache Invalidation via Domain Events
