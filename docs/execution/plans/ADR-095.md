# Plan — ADR-095 Wire NextAuth/Auth.js App Router Handlers and Sessions

## Summary

Wire Auth.js App Router handlers for merchant + customer JWT sessions; tighten dev OTP leak; protect merchant/admin/customer dashboard routes; reject unauthenticated realtime tokens; include `app/` in lint/typecheck coverage.

## Iranian First impact

- Persian OTP/request/verify error messages for merchant/customer
- RTL auth pages (minimal Persian phone/OTP forms)
- Iranian phone validation (09…)
- Jalali/تومان: N/A on auth screens unless showing dates/money
- Checklist in scope for auth UX surfaces

## Tasks

1. auth.ts + app/api/auth/[...nextauth]/route.ts
2. Merchant + customer OTP HTTP that issues JWT via Auth.js
3. Fix shouldReturnDevOtp (development only or MOS_RETURN_DEV_OTP=1)
4. Middleware/session protection for /dashboard /pos /admin /s/.../dashboard
5. Realtime token: remove x-merchant-id bypass
6. Secure cookies (httpOnly, sameSite, secure in prod)
7. ESLint + typecheck/build cover app/
8. uiuxpromax brief for Persian RTL OTP UI if new pages

## Test matrix

- Unit: shouldReturnDevOtp never true in staging/production
- Integration/e2e-ish: OTP → JWT session claims
- Realtime token rejects without session
- validate + build

## Post-complete

Move ADR-095 → done (parent); next ADR-094 or ADR-113 per critical path

## Implementation notes (2026-08-05)

- Wired Auth.js handlers; OTP leak policy fixed; realtime session auth; lint+build cover `app/`
- Remaining: live Compose e2e smoke with real SMS provider (ADR-083); membership claim enrichment beyond storeId slug
