# Plan — ADR-022 Merchant Staff PWA Architecture

## Summary

Encode MerchantOS **merchant staff PWA** at `src/staff-pwa/`: distinct audience from store customer PWA (ADR-023), MerchantOS branding, static manifest path `/staff/manifest.webmanifest`, `start_url` → `/pos`, Persian RTL install chrome on POS, offline sale-queue policy reserved for ADR-024, merchant staff JWT only, `AppOpened` source=`staff-pwa`. Add minimal manifest Route Handler + install banner stub on merchant POS.

## Iranian First impact

- Persian text: yes — install prompt, dismiss, success, offline banner fully `fa-IR`
- RTL: yes — `dir=rtl` / `lang=fa` on app shell; POS install banner logical layout
- Jalali: N/A this cycle (no date widgets on install chrome)
- تومان: noted in cashier workflow copy for totaling; no live money widgets this cycle
- Iranian workflows: cashier POS install → barcode/phone/totaling (تومان) under pressure
- Mobile: ~390px Android POS; install CTA ≥44px; lightweight manifest/icons
- Checklist (install UX + contract):
  - Persian primary copy — yes
  - RTL/`lang=fa` — yes
  - Loading/dismiss/install/offline states Persian — yes
  - Distinct from store customer PWA — yes
  - Offline: queue deferred to ADR-024; Persian banner documents online-first MVP

## uiuxpromax gate evidence

**Binary:** may be absent. Skill + `docs/uiux/*` (incl. `pwa-experience.md`) evidence used. Gate: brief in this plan before UI.

**Brief (Persian + RTL, mobile-first merchant staff POS PWA install):**

| Item | Detail |
| --- | --- |
| Surface | بنر نصب PWA پرسنل روی صندوق؛ مانیفست برند MerchantOS (نه برند مغازه مشتری) |
| Persona | صندوقدار/مالک `fa-IR`؛ موبایل اندروید پشت پیشخوان؛ می‌خواهد صندوق را روی صفحهٔ اصلی داشته باشد |
| Mobile | عرض ~390px Android؛ بنر پایین منطقی؛ دکمهٔ نصب ≥۴۴px؛ یک‌دستی |
| Screen list | صندوق + بنر نصب · حالت نصب‌شده · حالت رد شده · بنر آفلاین (متن رزرو) |
| States | پیشنهاد: «نصب اپلیکیشن صندوق»؛ در حال: «در حال آماده‌سازی…»؛ رد: مخفی؛ نصب‌شده: «اپلیکیشن صندوق روی دستگاه شماست.»؛ آفلاین: «اتصال قطع است — صف فروش آفلاین به‌زودی» |
| Component map | بنر پایین · عنوان فارسی MerchantOS/کاسبینو · CTA نصب · «الان نه» · بدون برند ویترین مشتری · **هرگز** مانیفست `/s/{slug}` |
| RTL / Persian | `dir=rtl` `lang=fa`؛ کپی کامل فارسی؛ اهداف لمسی بزرگ برای عجلهٔ صندوق |
| Copy samples | «نصب اپلیکیشن صندوق» · «صندوق فروش را سریع از صفحهٔ اصلی باز کنید» · «افزودن به صفحهٔ اصلی» · «الان نه» |
| Iranian retail | صندوقدار · بارکد · شماره موبایل مشتری · جمع به تومان · بدون ارسال پیک |
| A11y | `role="region"`؛ دکمه‌های با نام قابل فهم؛ contrast کافی؛ touch ≥44px |
| Out of scope | Store customer PWA (ADR-023) · full SW + IDB sale queue (ADR-024) · Mongo emit · camera barcode UI |

`assertUiuxGate`: `gatePassed` + skill/docs present + brief Persian/RTL/390/retail/states/a11y.

## Database design

- N/A new tables (offline IDB + sync API → ADR-024 / ARD-017 remainder).

## Domain / application changes

- `src/staff-pwa/` — Decision + helpers:
  - Audience `staff` ≠ `store-customer` (forbid conflation)
  - Manifest path `/staff/manifest.webmanifest`
  - `start_url` = `/pos`; `scope` under merchant staff; `display=standalone`
  - MerchantOS branding (name / short_name / theme_color / icons)
  - Auth: merchant staff JWT only (roles `merchant_owner` / `store_employee`; never `customer`)
  - Offline policy: online-first MVP; sale queue + SW reserved ADR-024
  - Events reserved: `StaffPwaInstallPromptShown`, `StaffPwaInstalled`, `AppOpened` source=`staff-pwa`
  - Persian install + offline banner copy + min touch 44px
  - Cookie isolation note vs customer store PWA
  - `assertUiuxGate` evidence
- App Router:
  - `GET` Route Handler `app/(merchant)/staff/manifest.webmanifest/route.ts`
  - Client install banner on POS; link manifest in POS metadata
  - Default SVG icon under `public/icons/`
- Sync `APP_ROUTER_FILESYSTEM.merchant.manifestRoute` (+ install prompt)
- ARD-017: architecture + manifest route + install UX foundations; SW/IDB/sync remain for ADR-024

## API / UI / events / cache

- Manifest route (JSON `application/manifest+json`)
- UI: Persian RTL install banner on `/pos`
- Events: names reserved (no Mongo emit this cycle)
- Cache: public short max-age on manifest; SW shell deferred ADR-024

## Gates

- Telemetry: event names only (N/A warehouse write) — `AppOpened` source=`staff-pwa`
- Persistence: N/A (IDB → ADR-024)
- uiuxpromax: brief above before install UI
- No delivery / no store customer PWA conflation
- Prerequisites: ADR-016 completed; ADR-024 offline queue explicitly deferred (online-first MVP per `13-pwa-architecture.md`)

## Test matrix

- Unit: staff manifest path + start_url `/pos`
- Unit: audience ≠ store-customer; manifest path ≠ per-store customer pattern
- Unit: MerchantOS branding; reject customer JWT role
- Unit: Persian install + offline banner copy
- Unit: events reserved (`staff-pwa`); uiux gate passes
- Unit: filesystem manifest route + POS has Persian install chrome
- Sync app-router exists checks for new merchant paths
- `npm run validate`

## Related ARD checkboxes

- ARD-017: manifest strategy + install UX foundations; SW/IDB/sync remain (ADR-024)

## Iranian feature checklist (in scope)

- Persian UI copy — yes
- RTL shell — yes (root `lang=fa`/`dir=rtl`)
- Mobile touch ≥44px — yes
- Offline banner Persian — yes
- Jalali on install chrome — N/A
- تومان — cashier workflow note in copy/docs; live totaling → POS domain UI later
- Pickup / no delivery — unchanged

## Risks

- ADR-024 offline queue still `todo` — mitigated by online-first MVP + reserved offline banner/policy
- Installability without full SW — soft “add to home screen” copy always shown
- Cookie path isolation depends on Auth.js naming later — contract documents isolation rule

## Completion notes

- Implemented as planned; `npm run validate` green (521 tests)
- Iranian feature checklist: Persian install/offline copy + RTL shell; mobile CTA ≥44px; تومان noted in cashier hint; Jalali N/A on install chrome
- `src/staff-pwa` — MerchantOS staff scope, manifest `/staff/manifest.webmanifest`, start_url `/pos`, store-customer isolation (≠ ADR-023), merchant staff JWT only, events reserved (`AppOpened` source=`staff-pwa`)
- Manifest route + Persian install banner on POS; offline queue reserved ADR-024
- Default SVG icon; app-router + middleware `/staff` audience synced; ARD-017 foundations
- Next: ADR-024 Offline-First Staff POS Strategy
