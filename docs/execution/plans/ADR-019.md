# Plan — ADR-019 shadcn/ui Strategy

## Summary

Encode shadcn/ui as the exclusive accessible primitive vendor: owned source copied into `src/components/ui` (ADR-018 path; not a parallel DS like MUI/Chakra). Ship `src/shadcn-strategy/` contract + root `components.json` stub + light `cn` utils stub. Defer Tailwind token wiring and CLI-generated Button/Radix primitives to ADR-020 (no inventing styles without the design-system ADR).

## Iranian First impact

- Persian text: yes — contract requires primitives accept `fa` strings without clipping; no new screens/copy this cycle
- RTL: yes — shadcn/RTL-first; logical props mandatory; components.json alias targets RTL-ready path; physical L/R spine forbidden
- Jalali: no Western-only calendar subcomponents without Iranian adapters (assert in contract; no date primitive yet)
- تومان: no Western-only currency subcomponents without Iranian adapters (no money primitive yet)
- Iranian workflows: focus management preserved (a11y); POS density ≥44px reserved via ADR-018 density; no delivery UI
- Mobile: touch density documented; no heavy design-system npm runtime
- Checklist (architecture UX in scope):
  - Primary UI copy Persian — capacity yes; no new screens
  - Root `dir=rtl` / `lang=fa` — unchanged (ADR-016)
  - Logical CSS mandatory — yes (contract + ADR-018)
  - Error/empty/loading Persian — convention; N/A no data views
  - Currency/Jalali/phone — N/A until domain UI ADRs

## uiuxpromax gate evidence

**Binary:** `uiuxpromax` CLI not present in environment. Gate satisfied via MerchantOS `uiuxpromax-integration` skill using `docs/uiux/*` as evidence.

**Brief (Persian + RTL):**

| Item | Detail |
| --- | --- |
| Surface | استراتژی shadcn/ui — قرارداد فروشندهٔ پریمیتیو؛ بدون صفحهٔ بصری جدید |
| Screen list | N/A (قرارداد + `components.json` + utils stub) |
| States | قرارداد: کامپوننت‌های تولیدشده بعدی باید `loading` / `empty` / `error` با متن فارسی پشتیبانی کنند |
| Component map | پریمیتیوها فقط در `src/components/ui` از CLI شادکن؛ کامپوزیت/دامنه طبق ADR-018؛ بدون سیستم موازی |
| RTL / Persian | RTL-first؛ props منطقی؛ تایپوگرافی فارسی بدون برش؛ آینه‌سازی آیکن‌های جهت‌دار |
| Density | POS متراکم + هدف لمسی ≥۴۴px (ADR-018)؛ آنالیتیکس راحت‌تر |
| A11y | Radix/shadcn focus management حفظ شود؛ WCAG AA |
| Motion | بدون انیمیشن در این ADR |
| Out of scope visuals | توکن/تم Tailwind (۰۲۰)؛ Button/رادیکس واقعی تا توکن‌ها؛ صفحات واقعی (ADRs بعدی) |

## Database design

- N/A

## Domain / application changes

- `src/shadcn-strategy/` — vendor = shadcn (owned copy, not npm black-box runtime), paths, CLI generate rules, forbidden parallel DS packages, RTL/Iranian adapter rules, utils placement, assert helpers
- `components.json` — shadcn CLI config stub (`rsc`, aliases → `@/components/ui`, utils → `@/lib/utils`); Tailwind paths reserved for ADR-020
- `src/lib/utils.ts` — light `cn` stub (no new npm deps; ADR-020 may swap to `clsx` + `tailwind-merge`)
- Do **not** add Tailwind / Button / Radix this cycle (prefer contract + stub)
- Align docs: `docs/tech/shadcn-ui.md` if needed; leave `src/components/ui` empty (`.gitkeep`) until CLI generate with tokens

## API / UI / events / cache

- No runtime visual Button / Radix install
- No analytics emit
- Cache / secrets: N/A; forbid secrets in client primitives (ADR-018)

## Gates

- Telemetry: N/A
- Persistence: N/A
- uiuxpromax: docs gate + Persian+RTL brief above (no binary)
- No delivery: unchanged
- Prerequisites: ADR-018 completed; Tailwind tokens ADR-020 next

## Test matrix

- Unit: vendor = shadcn; owned copy into `src/components/ui`
- Unit: legacy `src/shared/ui` superseded (do not target)
- Unit: forbidden parallel DS (MUI, Chakra, Ant, etc.)
- Unit: RTL-first + logical props + Persian no-clip + Iranian adapters
- Unit: `components.json` exists with correct aliases / ui path
- Unit: `src/lib/utils.ts` `cn` stub merges class names
- Unit: assert helpers throw on parallel DS / wrong install path
- `npm run validate`

## Related ARD checkboxes

- Staff PWA / UI packaging: shadcn primitive vendor contract landed; visual tokens remain ADR-020

## Iranian feature checklist (in scope)

- RTL + logical CSS convention — yes
- Persian string capacity — yes
- Other items — N/A until concrete UI components / Tailwind tokens

## Risks

- Temptation to add Tailwind early — blocked until ADR-020
- ADR text mentions `src/shared/ui` — code/docs use ADR-018 `src/components/ui`
- Over-building Button without tokens — skipped this cycle

## Completion notes

- Implemented as planned (2026-08-03)
- uiuxpromax binary absent — gate via `docs/uiux/*` + Persian+RTL brief in this plan
- `src/shadcn-strategy` contract: owned-copy vendor, `src/components/ui` path, forbidden parallel DS, RTL/Iranian adapters, utils deferred list
- Root `components.json` stub (`rtl: true`, aliases → `@/components/ui` / `@/lib/utils`); Tailwind paths reserved for ADR-020
- `src/lib/utils.ts` dependency-free `cn` stub (no cva/clsx/tailwind-merge / Button / Tailwind this cycle)
- Synced `docs/tech/shadcn-ui.md`
- `npm run validate` green (387 tests)
- Iranian checklist: RTL/logical/Persian capacity + no Western-only date/currency subcomponents in contract; no visual screens; Jalali/تومان N/A until primitives + tokens
- STATUS → `completed`; Next: ADR-020
