# Plan — ADR-120 ADR STATUS Truth Realignment

## Summary

Realign STATUS boards and agent docs so **architecture-contract landed** (`adrs/done/` + ADR impl `complete`) is never confused with **product-runtime complete** (API + migrations + tests + ARD delivery). Encode two-axis completeness in `src/governance`, update AGENT.md/README/STATUS notes, link AUDIT_REPORT from README, and assert folder path resolution.

## Iranian First impact

- Persian text / RTL / Jalali / تومان: N/A (governance docs; no new merchant/customer UX)
- Iranian behavior / mobile: N/A — process only
- Checklist: UX N/A (one-line: no user-facing surface)

## Schema / API / UI / events / cache

- No schema, HTTP, UI, events, or cache changes
- uiuxpromax: N/A

## Task breakdown

1. Extend `src/governance` with ADR-120 status-truth contract (folders, two-axis runtime completeness, asserts)
2. Unit tests: no runtime-complete without api+migration+tests evidence; path resolution
3. Update `AGENT.md`: `adrs/done|future|tasks` paths; DoD two-axis wording; ard-to-code queue = `tasks/`
4. Update root `README.md`: link AUDIT_REPORT; honest product-runtime status
5. Tighten `adrs/STATUS.md` legend / notes for two-axis; mark ADR-120 in_progress → complete
6. Clarify `docs/ards/STATUS.md` delivery-SoT note if needed
7. Move ADR-120 tasks → done; REORGANIZATION_INDEX + progress-log

## Test matrix

- Unit: runtime completeness vocab {contract, partial, complete}
- Unit: assertRuntimeComplete rejects missing api / migration / tests
- Unit: folders `adrs/done`, `adrs/future`, `adrs/tasks`, AUDIT_REPORT exist
- Unit: agent docs mention folder layout / do not equate ADR complete with ARD completed
- `npm run validate`

## Related ARD checkboxes

- Cross-cutting notes only — ARD board remains delivery SoT (`todo` until runtime)

## Iranian feature checklist (in scope)

- All UX items: **N/A** — governance/docs/contract only

## Risks

- Agents still treat `done/` as shippable → assert + README/AGENT wording mitigate
- Over-encoding STATUS HTML → keep lean constants + asserts

## Post-complete

- Move `adrs/tasks/ADR-120-adr-status-truth-realignment.md` → `adrs/done/`
- Next critical path: ADR-092
