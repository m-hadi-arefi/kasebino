# Plan — ADR-017 App Router Structure

## Summary

Scaffold App Router **route groups** for clear auth/audience boundaries — `(marketing)`, `(merchant)`, `(storefront)/s/[storeSlug]`, `(admin)`, `api/v1` — and encode the structure as an importable contract in `src/app-router-structure/`. Keep Iranian First root shell (`lang=fa` `dir=rtl`); thin Persian placeholder pages only (no design-system UI). Forbid delivery/courier URL segments. Align storefront path with ADR-091 `/s/{storeSlug}`.

## Iranian First impact

- Persian text: yes — group placeholder copy in `fa`
- RTL: yes — inherit root `dir=rtl` / `lang=fa` (ADR-016); group layouts do not flip LTR
- Jalali: N/A — no date UI
- تومان: N/A — no money UI
- Iranian workflows: storefront under `/s/[storeSlug]`; pickup-only (no delivery routes); merchant POS path reserved at `/pos`
- Mobile: thin shells only; no heavy assets; loading UX deferred to later frontend ADRs
- Checklist (shell UX in scope):
  - Primary UI copy Persian — yes (placeholders)
  - Root `dir=rtl` / `lang=fa` — yes (unchanged root layout)
  - Error/empty/notifications/receipts — N/A (no flows)
  - Logical CSS / icons / forms — N/A (no design system)
  - Currency/Jalali/phone — N/A
  - SEO: marketing + storefront Persian titles/descriptions

## Database design

- N/A

## Domain / application changes

- `src/app-router-structure/` — route groups, URL segments, storefront param `storeSlug`, forbidden delivery segments, middleware coarse-gate matchers, filesystem layout constants, assert helpers
- Filesystem scaffold under `app/`:
  - `(marketing)/page.tsx` → `/` (move from `app/page.tsx`)
  - `(merchant)/layout.tsx` + `dashboard/page.tsx` + `pos/page.tsx`
  - `(storefront)/s/[storeSlug]/page.tsx` (+ thin layout)
  - `(admin)/admin/page.tsx` (+ thin layout)
  - `api/v1/` directory marker (handlers remain ADR-030+)
- Thin `middleware.ts` — classify `x-mos-route-audience` from path (coarse gate stub; no auth enforcement yet)
- Sync `src/nextjs-architecture` `rootPage` → `app/(marketing)/page.tsx`
- Sync `docs/tech/nextjs.md` folder conventions

## API / UI / events / cache

- No business API handlers (zod stays deferred)
- uiuxpromax: N/A beyond thin Persian RTL shells (ADR-021 gates real UI)
- Analytics: path→audience header supports future PageViewed (no emit this cycle)
- Cache: N/A

## Gates

- Telemetry: N/A emit (audience header only)
- Persistence: N/A
- uiuxpromax: shell placeholders only
- No delivery: assert no `/delivery`|/courier`|/shipping` routes; forbid in contract
- Prerequisites: ADR-016 completed

## Test matrix

- Unit: route groups = marketing/merchant/storefront/admin + api/v1
- Unit: storefront pattern `/s/[storeSlug]` aligns with `buildStorefrontPath` / `STOREFRONT_URL`
- Unit: forbidden URL segments include delivery/courier/shipping
- Unit: filesystem pages exist for dashboard, pos, admin, storefront slug, marketing home
- Unit: no `app/**/delivery/**` (etc.); root layout still fa/rtl
- Unit: middleware matcher / audience classification for sample paths
- `npm run validate`
- `npm run build` if feasible

## Related ARD checkboxes

- ARD-001/010/018/029: route group scaffolding landed; real storefront/admin/PWA UX remain

## Iranian feature checklist (in scope)

- Persian copy + RTL root — yes
- Other items — N/A until richer UI ADRs

## Risks

- Over-building UI / auth enforcement — stay at thin shells + coarse middleware header
- Param name `[slug]` vs `[storeSlug]` — use `storeSlug` (product / ADR-091)
- Dual pages at `/` — only marketing owns `/`

## Completion notes

- Implemented as planned (2026-08-03)
- `npm run validate` green (373 tests); `npm run build` green — routes `/`, `/dashboard`, `/pos`, `/admin`, `/s/[storeSlug]`
- Edge `middleware.ts` self-contained (aligned with contract classifier; no NodeNext `.js` Edge import)
- Iranian checklist: Persian placeholder copy + root `lang=fa`/`dir=rtl`; Jalali/تومان/forms N/A
- STATUS → `completed`; Next: ADR-018
