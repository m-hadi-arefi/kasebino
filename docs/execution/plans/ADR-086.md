# Plan — ADR-086 Storefront Architecture

## Summary

Encode MerchantOS dedicated per-store storefront architecture at `src/storefront-architecture/`: path URL `/s/{storeSlug}` (ADR-091), public catalog/about surfaces, store branding, pickup-only checkout CTA, ACL limited to catalog/ordering projections (no marketplace, no POS domain imports). Enhance `app/(storefront)/s/[storeSlug]/` with Persian RTL catalog + about stubs. Cache TTL 600s; `StorefrontVisited` funnel event name reserved.

## Iranian First impact

- Persian text: yes — storefront home/catalog/about + SEO + pickup CTA fully `fa-IR`
- RTL: yes — `dir=rtl` / `lang=fa` composition; logical nav/spacing
- Jalali: N/A this cycle (no hours/date widgets yet; map/hours later ADR-032+)
- تومان: stub catalog notes price unit as تومان when shown; no live pricing this cycle
- Iranian workflows: QR → storefront → pickup (no delivery); customer browse before OTP (ADR-032)
- Mobile: mobile-first ~390px Android; touch targets ≥44px for pickup CTA / nav
- Checklist (storefront stubs + contract):
  - Persian primary copy — yes
  - RTL/`lang=fa` — yes
  - Empty/loading stubs Persian — yes
  - SEO metadata Persian — yes
  - Pickup-only, no delivery — yes
  - Full PDP/API/map — deferred (stubs)

## uiuxpromax gate evidence

**Binary:** may be absent. Skill + `docs/uiux/*` evidence used. Gate: brief in this plan before UI stubs.

**Brief (Persian + RTL, mobile-first storefront):**

| Item | Detail |
| --- | --- |
| Surface | ویترین فروشگاه — خانه، کاتالوگ، دربارهٔ مغازه؛ سفارش فقط پیکاپ |
| Persona | مشتری محلی `fa-IR`؛ مراجعه از QR مغازه؛ خرید حضوری |
| Mobile | عرض ~390px Android؛ ناوبری پایین/بالا منطقی؛ CTA پیکاپ ≥۴۴px |
| Screen list | خانهٔ ویترین · کاتالوگ (stub) · دربارهٔ فروشگاه (stub) |
| States | خالی کاتالوگ: «هنوز کالایی ثبت نشده.»؛ بارگذاری: «در حال بارگذاری…»؛ خطا: «مشکلی پیش آمد. دوباره تلاش کنید.» |
| Component map | هدر برند · ناوبری کاتالوگ/درباره · CTA «سفارش حضوری (پیکاپ)» · بدون کارت شلوغ مارکت‌پلیس |
| RTL / Persian | `dir=rtl` `lang=fa`؛ کپی کامل فارسی؛ بدون پیک/تحویل |
| Copy samples | «ویترین فروشگاه» · «کاتالوگ کالاها» · «سفارش فقط به‌صورت حضوری (پیکاپ)» · «دربارهٔ مغازه» |
| Iranian retail | مغازه · مشتری · پیکاپ در محل (نه صندوقدار POS) |
| A11y | عناوین معنایی؛ لینک‌های nav با نام قابل فهم؛ CTA با contrast کافی |
| Out of scope | PDP زنده · OTP مشتری (ADR-032) · Store PWA (ADR-023) · نقشه/مسیریابی کامل · API handlers |

`assertUiuxGate`: `gatePassed` + skill/docs present + brief Persian/RTL/390/retail/states/a11y.

## Database design

- N/A new tables (reads store/catalog later). Cache key TTL class storefront 600s already exists.

## Domain / application changes

- `src/storefront-architecture/` — Decision + helpers:
  - Path URL strategy (reuse `buildStorefrontPath` / ADR-091)
  - Surfaces: home / catalog / about (+ pickup entry path)
  - Branding fields (displayName, logoKey, primary/accent colors, cover)
  - Public ACL: allowed bounded contexts `catalog` + `ordering` only; forbid POS/CRM secrets
  - Reuse `PUBLIC_DTO_FORBIDDEN_FIELDS` / output minimization from ADR-077
  - Pickup CTA copy + fulfillment assert; forbid delivery UI
  - No imports from POS domain modules
  - Cache TTL 600s; rate-limit audience public storefront
  - Event name `StorefrontVisited` (emit later with analytics ADRs)
  - `assertUiuxGate` evidence export
- Routes under `app/(storefront)/s/[storeSlug]/`:
  - Enhance home page (branding chrome + nav + pickup CTA)
  - Add `catalog/page.tsx` stub (fa)
  - Add `about/page.tsx` stub (fa)
- Sync `APP_ROUTER_FILESYSTEM.storefront` paths for catalog/about
- ARD-010 note: architecture + route stubs landed; APIs remain

## API / UI / events / cache

- No new Route Handlers this cycle (ARD-010 APIs remain)
- UI: modest Persian RTL stubs
- Events: `StorefrontVisited` name reserved (no Mongo emit)
- Cache: honor 600s storefront class

## Gates

- Telemetry: funnel event name only (N/A warehouse write)
- Persistence: N/A
- uiuxpromax: brief above before UI stubs
- No delivery: enforce in contract + pages
- Prerequisites: ADR-006, ADR-016 completed; ADR-082 pickup policy referenced (may remain todo as policy ADR)

## Test matrix

- Unit: path URL `/s/{storeSlug}`; no subdomain MVP
- Unit: ACL allows catalog/ordering only; rejects POS import / forbidden DTO fields
- Unit: branding shape; pickup CTA Persian; delivery forbidden
- Unit: filesystem routes exist (home/catalog/about); Persian copy / RTL lang
- Unit: `assertUiuxGate` / `assertStorefrontUiuxGate` passes
- Unit: cache TTL 600s; StorefrontVisited event name
- `npm run validate`

## Related ARD checkboxes

- ARD-010: architecture contract + Persian route stubs; APIs/cache wiring remain

## Iranian feature checklist (in scope)

- Persian UI copy — yes
- RTL/`lang=fa` — yes
- Empty/loading Persian — yes
- SEO Persian — yes
- Mobile-first / touch CTA — yes
- Pickup not delivery — yes
- تومان on live prices / Jalali hours / OTP — N/A this cycle

## Completion notes

- Implemented as planned (2026-08-03)
- `src/storefront-architecture/` — path URL `/s/{storeSlug}`, catalog/ordering ACL, branding fields, pickup CTA (no delivery), no POS imports, cache 600s, `StorefrontVisited` reserved, `assertUiuxGate` evidence
- Routes: enhanced home + `catalog`/`about` Persian RTL stubs under `app/(storefront)/s/[storeSlug]/`; `APP_ROUTER_FILESYSTEM` synced
- ARD-010 STATUS note updated (APIs remain)
- Tests: 9 new; `npm run validate` green (442 tests)
- Iranian checklist: Persian copy; fa/rtl shell; empty catalog stub; SEO fa; pickup-only; تومان label on catalog stub; Jalali/OTP/live PDP N/A
- STATUS → `completed`; Next: ADR-032 Customer SMS OTP Authentication
