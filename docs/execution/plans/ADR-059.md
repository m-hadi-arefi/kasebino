# Plan — ADR-059 Product Analytics Architecture

## Summary

Encode MerchantOS **product analytics** as Mongo `mos_product` (product analytics plane) via `trackEvent` in `src/product-analytics/`: feature key registry, activation funnels (POS phone capture, QR, pickup, loyalty), Persian metric names for later merchant UX, dual-read money from PG, ingest isolated through ADR-065 fire-and-forget buffer. In-memory store for tests; HTTP track API / admin feature adoption UI remain ARD-023 packaging.

## Iranian First gate

| Check | Impact |
| --- | --- |
| Persian text? | Metric / funnel / feature labels Persian (`PRODUCT_METRIC_LABELS_FA`, funnel `labelFa`); event codes + feature keys English |
| RTL? | Dashboard viewer deferred (uiuxpromax); stubs declare `dir=rtl` / `fa-IR` |
| Jalali? | Store ISO UTC; Jalali/`Asia/Tehran` buckets when merchant UX lands |
| تومان? | Dual-read: money truth from PG only — never claim Mongo as accounting SoT |
| Iranian behavior? | Funnels cover POS phone capture, QR window sticker, pickup (no delivery), loyalty earn/redeem |
| Iranian mobile? | No UI this cycle; phone props hashed/scrubbed; track never blocks POS |

Iranian checklist: UX screens N/A (infra + labels). Persian metric names + RTL stubs + phone privacy required. Merchant/admin product analytics UI → ARD-023 + uiuxpromax later.

## Gates

- Telemetry: this ADR *is* product analytics (`mos_product` / FeatureUsed + funnels); track ingest via ADR-065
- Persistence: Mongo `mos_product` (+ logical `mos_product_rollups`); in-memory this cycle; PG money dual-read stance only
- uiuxpromax: N/A (no dashboard pages this cycle; Persian labels/contracts only)
- Delivery: no courier/delivery; pickup funnel only
- Prerequisites: ADR-056 completed; ADR-065 isolation package completed

## Task breakdown

1. STATUS `in_progress`; write this plan
2. `src/product-analytics/` — decision, feature key registry, four funnels, ProductDocument schema, InMemory store, `trackEvent` via ADR-065 ingest isolation, PII/secret scrub + phone hash, Persian metric names, indexes/API reserve, metrics, assertions
3. Flip mongodb-analytics placement/requirements pointers (product analytics implemented)
4. ARD-023 foundation notes; ARD-021 progress note
5. Tests + `npm run validate` → STATUS completed → progress-log Next: ADR-060

## Schema / API / UI / events / cache

- Schema: logical `mos_product` docs (unique `eventId`); indexes `featureKey+occurredAt`, `merchantId+occurredAt`; rollups collection name `mos_product_rollups`
- API: reserve `POST /api/v1/analytics/track`, `GET /api/v1/admin/product-analytics/features` — implement later
- UI: none this cycle (Persian metric names ready)
- Events: `FeatureUsed`, funnel step events (POS capture, QR, pickup, loyalty), `AppOpened` / POS timing catalog alignment
- Cache: Redis TTL 60–300s for admin widgets later (documented only)

## Test matrix

- Collection is `mos_product` (ADR-056); not OLTP SoT
- Feature key registry covers POS / QR / pickup / loyalty
- Four funnels ordered with Persian labels
- `trackEvent` accepts via ADR-065 buffer; never throws when Mongo/sink down
- Secrets (OTP/JWT) stripped from properties; Iranian phones hashed/scrubbed; Persian property text preserved
- FeatureUsed requires featureKey; idempotent eventId
- Dual-read money stance locked (PG truth)
- Off checkout critical path
- Pointer flips + `npm run validate`

## Related ARD checkboxes

- ARD-023 — track helpers + feature registry + funnels foundation; HTTP track + admin UI + live driver remain
- ARD-021 — product analytics plane consumer note

## Iranian feature checklist (in scope)

- UX copy / RTL screens / mobile / تومان display UI: N/A — labels + stubs only
- Persian metric / funnel labels: required
- Phone privacy for product analytics (hash/scrub Iranian mobiles): required
- Jalali merchant-facing buckets: deferred to ARD-023 UI

## Risks

- Over-implementing track HTTP / admin UI / GA SaaS → keep contract + in-memory + isolated trackEvent
- Confusing `mos_product` with warehouse `mos_events` or audit `mos_audit` — product track writes `mos_product` only
- Blocking OLTP/POS on Mongo failure — must use ADR-065 best_effort_track path

## Completion notes

- Implemented `src/product-analytics` — FeatureUsed + funnels (POS phone capture, QR, pickup, loyalty), feature key registry, Persian metric/feature labels, `trackEvent` via ADR-065 best_effort_track, `mos_product` (+ rollups name), phone hash + secret scrub, dual-read money from PG; in-memory store + tests; flipped mongodb-analytics / analytics-boundaries / event-warehouse pointers; ARD-021/023 foundation notes
- Validate green (605 tests); STATUS → completed; Next: ADR-060
- Iranian checklist: UX screens N/A (uiuxpromax N/A); Persian metric/funnel labels + RTL stubs + phone privacy tested; Jalali merchant viewer deferred to ARD-023
