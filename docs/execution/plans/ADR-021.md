# Plan — ADR-021 uiuxpromax Mandatory for UI

## Summary

Encode the mandatory uiuxpromax gate as an importable contract at `src/uiuxpromax-gate/`: every UI task must pass the gate before code; briefs must specify Persian + RTL; required inputs are `docs/uiux/*` plus `.cursor/skills/uiuxpromax-integration` (and `docs/skills/uiuxpromax-integration.md`). Missing skill/docs = STOP (not Done). Provide `assertUiuxGate` helpers for ard-to-code cycles.

## Iranian First impact

- Persian text: yes — briefs must require Persian copy samples and `fa-IR` persona; Iranian retail context (مغازه، صندوقدار، مشتری)
- RTL: yes — designs must be RTL compositions before coding; briefs must declare `dir=rtl`
- Jalali: N/A this cycle (no date UI); briefs for future date UI must note Jalali via docs/uiux when in scope
- تومان: N/A this cycle (no money UI); process requires Iranian retail context in prompts
- Iranian workflows: gate mandates Iranian retail persona language in briefs
- Mobile: briefs should note 390px Android widths for merchant/customer journeys (encoded as requirement)
- Checklist (process UX gate in scope):
  - Primary UI copy Persian — capacity/process yes; no new screens this cycle
  - Root `dir=rtl` / `lang=fa` — unchanged (ADR-016/017)
  - Logical CSS / RTL compositions — mandatory in brief contract
  - Error/empty/loading Persian — part of uiux protocol outputs for future UI
  - Currency/Jalali/phone — N/A until domain UI ADRs

## uiuxpromax gate evidence

**Binary:** `uiuxpromax` CLI may be absent. Gate is satisfied when MerchantOS `uiuxpromax-integration` skill + `docs/uiux/*` exist and a Persian+RTL brief is produced before UI code.

**Brief (Persian + RTL) — this ADR itself (process only):**

| Item | Detail |
| --- | --- |
| Surface | دروازهٔ اجباری uiuxpromax — قرارداد فرآیند؛ بدون صفحهٔ محصولی جدید |
| Screen list | N/A (قرارداد + assert؛ اسکلت shell موجود) |
| States | پروتکل: هر UI بعدی باید `loading` / `empty` / `error` با متن فارسی قبل از کد داشته باشد |
| Component map | خروجی‌های uiuxpromax: لیست صفحه، نگاشت کامپوننت، یادداشت a11y — سپس پیاده‌سازی |
| RTL / Persian | brief باید صریحاً RTL + کپی فارسی + پرسونای fa-IR را اعلام کند |
| Density / Mobile | mockups در عرض ~۳۹۰px اندروید برای مسیرهای merchant/customer |
| A11y | از `docs/uiux/accessibility.md` و design-rules |
| Motion | طبق design-rules؛ بدون UI جدید در این ADR |
| Out of scope | صفحات واقعی؛ state management (ADR-025)؛ PWA shells (022/023) |

## Database design

- N/A

## Domain / application changes

- `src/uiuxpromax-gate/` — mandatory gate contract:
  - Rule: UI without passing gate = not Done
  - Required paths: skill (`.cursor/skills/uiuxpromax-integration/SKILL.md`), docs skill (`docs/skills/uiuxpromax-integration.md`), `docs/uiux/*` inputs
  - Brief requirements: Persian + RTL + fa-IR persona + Iranian retail context + mobile 390px note
  - Protocol steps (detect UI → load skill → read docs/uiux → produce plan → then code)
  - Fallback when external uiuxpromax binary missing: docs + skill evidence (STOP if those missing)
  - `assertUiuxGate` / related helpers
- Light sync of skill/docs to require Persian+RTL in briefs (ADR Iranian UX section)
- No product screens / no new npm deps

## API / UI / events / cache

- No new product screens
- No analytics emit
- Cache / secrets: N/A

## Gates

- Telemetry: N/A
- Persistence: N/A
- uiuxpromax: this ADR *is* the gate; Persian+RTL brief above
- No delivery: unchanged
- Prerequisites: ADR-018, ADR-020 completed

## Test matrix

- Unit: mandatory-before-UI flag true; non-compliance = not Done
- Unit: required skill + docs skill + each `docs/uiux/*` path listed in contract exist on disk
- Unit: brief validator requires `persian` + `rtl` (+ persona / mobile notes as encoded)
- Unit: `assertUiuxGate` throws when skill missing / brief incomplete / gate skipped
- Unit: `assertUiuxGate` passes with complete evidence
- `npm run validate`

## Related ARD checkboxes

- ARD-001: uiuxpromax process (ADR-021) landed; remaining Kit/repos/adapters

## Iranian feature checklist (in scope)

- RTL compositions required in brief — yes
- Persian copy samples / fa-IR persona in brief — yes
- Mobile 390px Android note — yes
- Root fa/rtl shell — already present; unchanged
- Other items — N/A (no concrete UI screens)

## Risks

- External uiuxpromax binary unavailable — mitigated: docs + skill filesystem gate + STOP if missing
- Agents skipping gate — mitigated: executable contract + tests in validate suite
- Over-scoping into visual pages — out of scope; Next after frontend system Phase D: ADR-025 (+ 026–028)

## Completion notes

- Implemented as planned (2026-08-03)
- `src/uiuxpromax-gate/` — mandatory-before-UI contract, protocol, Persian+RTL brief requirements, `assertUiuxGate` / `assertUiuxBrief`, filesystem path list
- Skill/docs synced: `.cursor/skills/uiuxpromax-integration/SKILL.md`, `docs/skills/uiuxpromax-integration.md`, `docs/uiux/uiux-system.md`
- Tests assert skill + all `docs/uiux/*` exist on disk; assert helpers throw on skip/missing brief
- uiuxpromax binary optional — evidence via skill + docs (STOP if those missing)
- `npm run validate` green (403 tests)
- Iranian checklist: Persian+RTL brief mandate + fa-IR persona + 390px mobile + Iranian retail terms; no product screens; Jalali/تومان N/A
- STATUS → `completed`; Next: ADR-025 (Phase D continues 025–028 after frontend system)
