# Plan — ADR-089 Admin Dashboard Architecture

## Summary

Encode MerchantOS **admin dashboard** architecture in `src/admin-dashboard/`: platform_admin-only Persian RTL shell with merchant list/enforcement stubs (activate/suspend), management analytics portfolio widgets (wired to `mgmt-dashboard-analytics`), security signal stubs, and audit browser stub; every view audited; `assertUiuxGate` evidence. Enhance `(admin)/admin` routes. Live HTTP `/api/v1/admin/*` handlers / Drizzle Kit / chart libraries remain ARD-018 / ARD-025 / ARD-022 / ARD-026.

## Iranian First gate (AGENT 1–6)

- Persian text: yes — admin chrome, privilege warnings, merchant list, enforcement confirms, mgmt/security/audit stubs fully `fa-IR`
- RTL: yes — admin shell `dir=rtl` / `lang=fa`; RTL tables/filters/lists
- Jalali: yes — stubs note ops date ranges / audit filters in Jalali (`Asia/Tehran`)
- تومان: yes — GMV / money portfolio labels use تومان (proxy vs PG truth notes from ADR-062)
- Iranian behavior: internal ops still present Iranian merchant data (phones, تومان, Jalali); plain Persian privilege warnings
- Mobile: laptop-primary ops; critical alerts/privilege copy readable on ~390px Android
- Checklist (stubs + contract): Persian primary copy; RTL shell; empty/auth/privilege states; تومان; Jalali note; platform_admin AuthZ + every view audited

## uiuxpromax gate evidence

**Binary:** may be absent. Skill + `docs/uiux/*` evidence used. Gate: brief in this plan before UI stubs.

**Brief (Persian + RTL, admin ops console):**

| Item | Detail |
| --- | --- |
| Surface | پنل مدیریت پلتفرم — خانه پورتفolio، فهرست فروشندگان، فعال‌سازی/تعلیق، سیگنال امنیتی، مرور حسابرسی؛ فقط `platform_admin` |
| Persona | اپراتور پلتفرم / اعتماد و ایمنی `fa-IR`؛ بررسی فروشندگان کرمان و منطقه؛ بدون نقش صندوقدار/مشتری |
| Mobile | لپ‌تاپ برای جدول‌ها؛ هشدارها و هشدار دسترسی روی ~390px Android خوانا؛ دکمه‌های اضطراری ≥۴۴px |
| Screen list | خانه (نمای کلی mgmt) · فهرست فروشندگان · کارت فعال‌سازی/تعلیق (stub) · سیگنال‌های امنیتی · مرور حسابرسی |
| States | بدون نقش ادمین: «این بخش فقط برای مدیران پلتفرم است…»؛ خالی: «هنوز فروشنده‌ای ثبت نشده.»؛ بارگذاری/خطا فارسی؛ تأیید تعلیق با متن هشدار |
| Component map | هدر پلتفرم · ناو RTL · هشدار دسترسی · کارت‌های پورتفolio (نمای کلی، فعال‌سازی، تعامل، اعتماد) · جدول/فهرست فروشندگان · بدون کروم مغازه/مشتری |
| RTL / Persian | `dir=rtl` `lang=fa`؛ جداول و فیلترها RTL؛ کپی کامل فارسی |
| Copy samples | «مدیریت پلتفرم» · «فهرست فروشندگان» · «تعلیق فروشنده» · «همه اقدامات مدیریتی ثبت می‌شوند» · «مبالغ به تومان» · «تقویم شمسی (تهران)» |
| Iranian retail | دادهٔ مغازه‌ها و مشتریان ایرانی در نمای ops؛ نه پیک/کوریر؛ نه Metabase به‌جای پنل |
| A11y | عناوین معنایی؛ `aria-label` روی ناو و جدول؛ هشدار دسترسی با نقش؛ تأییدهای خطرناک قابل درک |
| Out of scope | Live `/api/v1/admin/merchants*` handlers · chart libraries · SSO · Lighthouse production pass · JWT middleware enforce |

`assertUiuxGate`: `gatePassed` + skill/docs present + brief Persian/RTL/390/retail/states/a11y.

## Database design

- N/A new tables this cycle — consumes ADR-013 admin domain + ADR-062 mgmt rollups + ADR-058 audit browse via ARD-018 APIs later.
- Cache: admin merchant list short TTL 30s (`ADMIN_LIST_CACHE`); mgmt widgets 60–900s (`MGMT_CACHE`).

## Domain / application changes

- `src/admin-dashboard/` — Decision + helpers:
  - Admin shell surfaces: home, merchants, security, audit
  - Auth required: `platform_admin` only; refuse merchant_staff / customer
  - Every view audited (access audit contract)
  - Conceptual wiring to admin merchants APIs + mgmt analytics titles/paths + audit browse path
  - Enforcement stubs labels from `ADMIN_ACTION_LABELS_FA` / privilege warnings
  - Persian copy + تومان + Jalali notes
  - `assertUiuxGate` evidence export
- Routes under `app/(admin)/admin/`:
  - Home portfolio stub (fa)
  - Merchants list + activate/suspend stubs (fa)
  - Security signals stub (fa)
  - Audit browser stub (fa)
- Sync: flip `uiDeferredTo` pointers on admin-domain / mgmt-dashboard-analytics; ARD-018/022/025/026 foundation notes; app-router filesystem merchants/security/audit pages
- ARD-018 remains `todo` until live HTTP / RBAC route wiring / Kit migrations

## API / UI / events / cache

- No Route Handlers this cycle (ARD-018 APIs remain)
- UI: Persian RTL admin stubs (platform_admin + audited)
- Events: `AdminDashboardViewed` / view access audit reserved (no Mongo emit)
- Cache: list TTL 30s + mgmt cache-aside band documented

## Gates

- Telemetry: careful admin usage — view events reserved; do not track merchant PII in client analytics
- Persistence: N/A new schema (ADR-013 stubs already exist)
- uiuxpromax: brief above before UI stubs
- No delivery; never merchant dashboard chrome (ADR-088) / never customer portal (ADR-087)
- Prerequisites: ADR-013, ADR-062 completed; ADR-075 monitoring UI deeper later (security stub only here)

## Test matrix

- Unit: auth required; platform_admin only; rejects merchant_staff / customer
- Unit: every view marked audited
- Unit: merchants surface wires to admin API paths + Persian enforcement labels
- Unit: mgmt widgets wire to mgmt-dashboard-analytics paths/titles
- Unit: audit browse path reserved; security stub present
- Unit: filesystem admin routes exist; Persian / تومان / Jalali / RTL / privilege warnings
- Unit: `assertUiuxGate` / `assertAdminDashboardUiuxGate` passes
- Unit: no merchant/customer chrome / no delivery
- `npm run validate`

## Related ARD checkboxes

- ARD-018: uiuxpromax admin UI stubs landed; HTTP `/api/v1/admin/merchants*` / RBAC route wiring / Kit migrations remain
- ARD-022: audit browser stub in admin; live browse API remain
- ARD-025: mgmt portfolio stubs in admin UI; HTTP / live rollups remain
- ARD-026: security signal stub in admin UI; signal store / EMQX remain

## Iranian feature checklist (in scope)

- Persian UI copy — yes
- RTL/`lang=fa` — yes
- Empty/loading/auth/privilege Persian — yes
- تومان — yes (GMV portfolio)
- Jalali date range / audit filter note — yes
- Mobile-readable critical alerts — yes
- Analytics dashboard labels Persian — yes
- Live admin HTTP / charts — deferred (stubs)

## Risks

- Real JWT session gate not enforced in middleware yet — stubs document platform_admin required; wire with ARD-018
- Temptation to share merchant `/dashboard` chrome — forbidden; admin lives under `(admin)/admin` only
- Over-implementing live Route Handlers / activate TX → keep stubs + contract
- ADR-075 full monitoring/alerting deeper — security stub only here

## Completion notes

- Implemented as planned (2026-08-03)
- `src/admin-dashboard/` — platform_admin shell, every view audited, merchants/enforcement + mgmt portfolio + security + audit surfaces, Persian copy + تومان + Jalali, `assertUiuxGate` evidence
- Routes: `(admin)/admin` (+ merchants/security/audit) Persian RTL stubs; privilege warnings; RTL merchant table with activate/suspend stubs
- Flipped admin-domain / mgmt-dashboard UI pointers to `src/admin-dashboard/`; ARD-018/022/025/026 notes updated
- Tests: 9 new; `npm run validate` green (681 tests)
- Iranian checklist: Persian copy; fa/rtl; privilege/auth/empty; تومان; Jalali; RTL table; mobile-readable alerts; live HTTP N/A
- STATUS → `completed`; Next: ADR-067 Containerization Standards
