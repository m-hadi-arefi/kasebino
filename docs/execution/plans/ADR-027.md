# Plan — ADR-027 Form and Validation Strategy

## Summary

Encode MerchantOS form/validation strategy at `src/forms-validation/`: **React Hook Form + Zod** (shared schemas for UI via `zodResolver` and API boundary). Install `zod`, `react-hook-form`, `@hookform/resolvers`. Provide composable Zod schemas, **Persian** error message maps, **Iranian mobile** phone schema/normalize, and **تومان** money helpers (rial minor ↔ تومان display). Never trust client-only validation.

## Iranian First impact

- Persian text: yes — all Zod UX error messages `fa-IR`; plain-language merchant copy (no EN jargon)
- RTL: yes — form error contract `dir=rtl` / `lang=fa`; field order / inline errors RTL-aware capacity (no new product screens)
- Jalali: N/A this cycle (no date form field UI)
- تومان: yes — money helpers encode integer rial minor units → تومان display/parse; label unit `تومان`
- Iranian workflows: phone/OTP validators encode Iranian mobile (`09…` / `+98`)
- Mobile: RHF mode defaults favor submit/blur; numeric phone/money inputs keyboard-friendly capacity
- Checklist (strategy / capacity; no new product screens):
  - Persian validation/error copy — yes
  - Root `dir=rtl` / `lang=fa` — unchanged
  - Iranian phone normalize + Persian phone errors — yes
  - تومان format/parse helpers — yes
  - Jalali date fields — N/A until domain forms
  - Product form screens — N/A (uiux later)

## uiuxpromax gate evidence

**Binary:** may be absent. No new product pages/components → UI gate N/A for screens; capacity/contract only.

**Brief (Persian + RTL) — capacity only:**

| Item | Detail |
| --- | --- |
| Surface | استراتژی فرم و اعتبارسنجی — قرارداد RHF+Zod؛ بدون صفحهٔ محصولی جدید |
| Screen list | N/A |
| States | پیام‌های خطای فارسی؛ `dir=rtl` / `lang=fa` |
| Component map | schemas + persian error map + phone + money + `createZodFormResolver` |
| RTL / Persian | خطاهای fa-IR؛ قرارداد RTL برای فرم‌های بعدی |
| Out of scope | error UX کامل (028)؛ صفحات POS/CRM فرم بصری؛ env migrate به Zod (اختیاری بعداً) |

## Database design

- N/A

## Domain / application changes

- Dependencies: `zod`, `react-hook-form`, `@hookform/resolvers`
- `src/forms-validation/` — strategy contract:
  - Decision: RHF + Zod; Zod also at API boundary; never trust client alone
  - Persian error maps (`errorMap` / shared message catalog)
  - Iranian phone Zod schema + normalize (national `09…`, E.164 `+98`)
  - Money: rial minor ↔ تومان helpers + Zod positive toman / money schema
  - Shared schemas: required Persian text, OTP digits
  - Light RHF: re-export/`createZodFormResolver` via `@hookform/resolvers/zod`
  - Paths: shared root + `src/modules/*/application/dto` for module DTOs
- Sync `docs/tech/zod.md`, `docs/tech/react-hook-form.md`, `docs/rules/ui-rules.md` lightly
- ARD-001 note: Zod + forms package landed; ARD-007 note: phone/price validators capacity

## API / UI / events / cache

- No new product screens / API routes (schemas ready for both)
- Analytics: N/A
- Cache: N/A

## Gates

- Telemetry: N/A
- Persistence: N/A
- uiuxpromax: no product UI; brief above capacity-only
- No delivery: unchanged
- Prerequisites: ADR-016, ADR-019 completed

## Test matrix

- Unit: decision locks RHF + Zod + zodResolver + API-boundary Zod
- Unit: packages installed flags
- Unit: Persian error map messages contain Arabic-script / no English jargon keys as UX
- Unit: Iranian phone accepts `09…`, `+98…`, strips junk; rejects landline/invalid with Persian message
- Unit: money toman↔rial minor (×10) + format includes تومان; parse Persian digits
- Unit: sample form schema + resolver factory exists
- Unit: OTP / required text schemas emit fa messages
- `npm run validate`

## Related ARD checkboxes

- ARD-001: note Zod / forms-validation landed (env still zod-free until optional migrate)
- ARD-007: phone/price validation capacity note

## Iranian feature checklist (in scope)

- Persian error messages — yes
- RTL/`lang=fa` on form error contract — yes
- Iranian mobile validation/normalize — yes
- تومان helpers — yes
- Root fa/rtl shell — already present; unchanged
- Jalali form fields / new screens — N/A

## Completion notes

- Implemented as planned (2026-08-03)
- Dependencies: `zod`, `react-hook-form`, `@hookform/resolvers` installed
- `src/forms-validation/` — RHF+Zod decision; Persian error catalog + RTL contract; Iranian mobile normalize/schema; تومان↔rial helpers + positive toman schema; OTP/required text; `createZodFormResolver`; forbid Formik/client-only/EN UX patterns
- Synced `docs/tech/zod.md`, `docs/tech/react-hook-form.md`, `docs/rules/ui-rules.md`; ARD-001/007 notes + ARD STATUS
- Tests: 7 new; `npm run validate` green (425 tests)
- Iranian checklist: Persian validation messages; fa/rtl form contract; Iranian phone; تومان format/parse; Jalali fields / product screens N/A; root fa/rtl unchanged
- STATUS → `completed`; Next: ADR-028 Frontend Error Handling UX
