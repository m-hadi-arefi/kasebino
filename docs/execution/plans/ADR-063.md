# Plan — ADR-063 Merchant OLTP Dashboard Analytics

## Summary

Encode MerchantOS **merchant OLTP dashboard analytics** in `src/merchant-oltp-analytics/`: PostgreSQL projection read models for AN-01..04 (overview / revenue / customers / retention North Star), sales + membership **counter ports** (not Mongo money truth), Redis **TTL 60s** cache note aligned with ADR-053, Persian widget titles, and **Jalali/`Asia/Tehran` range helpers stub**. Wire `src/modules/analytics/` projection application layer (in-memory). Live Drizzle migrations, HTTP APIs, and uiuxpromax charts remain ARD-016 / ADR-088.

## Iranian First gate

| Check | Impact |
| --- | --- |
| Persian text? | Widget titles Persian (`MERCHANT_OLTP_TITLES_FA` / overview metric labels); event codes English |
| RTL? | Chart UI deferred (uiuxpromax → ADR-088 / ARD-016); stubs declare `dir=rtl` / `fa-IR` |
| Jalali? | Range helpers stub for Jalali/`Asia/Tehran` merchant buckets; storage day keys ISO/Gregorian-safe for PG |
| تومان? | Revenue figures are integer minor units; display unit note تومان (format at presentation) |
| Iranian behavior? | Merchant skimable overview KPIs from completed sales + membership; North Star Monthly Returning Customers |
| Iranian mobile? | Tablet-skimable notes; avoid desktop-only BI; no huge chart payloads this cycle |

Iranian checklist: UX screens N/A (infra + labels + stubs). Persian titles + Jalali range stub + RTL stubs required. Full dashboard widgets → ARD-016 / ADR-088 + uiuxpromax.

## Gates

- Telemetry: widget-view product analytics remain ADR-059 (`dashboard.widget_viewed`); this ADR is **OLTP PG projections**, not Mongo
- Persistence: logical PG projection tables (`analytics_*`); ports for sales/membership counters; in-memory this cycle; Drizzle Kit → ARD-016
- uiuxpromax: N/A (no dashboard pages this cycle; Persian titles/contracts only)
- Delivery: no courier/delivery metrics
- Prerequisites: ADR-041, ADR-054, ADR-009 completed; ADR-014 boundaries + cache-keys TTL 60s available

## Task breakdown

1. STATUS `in_progress`; write this plan
2. `src/merchant-oltp-analytics/` — decision, AN capability map, overview projection, sales/membership counter ports, Jalali range helpers stub, Persian titles, cache TTL 60s note, in-memory overview builder, asserts
3. `src/modules/analytics/` — domain projection types + application ports/handlers + in-memory infra; re-export contract
4. Flip analytics-boundaries merchantOltpWidgets pointer; ARD-016 foundation note + STATUS board note
5. Tests + `npm run validate` → STATUS completed → progress-log Next: ADR-062

## Schema / API / UI / events / cache

- Schema (logical, illustrative names from ADR-014/ARD-016): `analytics_daily_revenue`, `analytics_customer_stats`, `analytics_retention_stats`; overview aggregates counters from sales + membership ports
- API: reserve paths from `MERCHANT_OLTP_ANALYTICS.apiPaths` (overview/revenue/customers/retention); HTTP wire → ARD-016
- UI: none this cycle (Persian titles ready); uiuxpromax → ADR-088
- Events consumed (projection updates): `SaleCompleted`, `OrderPaid`, `CustomerReturned` (handler stub / apply on SaleCompleted for overview)
- Cache: Redis TTL **60s** (`CACHE_TTL_SECONDS.analytics` / ADR-053); invalidate via ADR-054 SaleCompleted map; note only this cycle (no live Redis wire)

## Test matrix

- Store is PostgreSQL projections; money truth not Mongo
- AN-01 overview builds from sales + membership counter ports
- Cache TTL note = 60s; aligns with cache-keys analytics class
- Persian titles include Persian script; RTL stubs
- Jalali range helper stub returns Asia/Tehran + jalali calendar metadata
- Merchant-scoped overview requires merchantId
- Off checkout critical path (projection apply after commit / ports only)
- Pointer flip + `npm run validate`

## Related ARD checkboxes

- ARD-016 — projection foundations + counter ports + Persian titles + TTL note; Drizzle migrations / APIs / uiuxpromax UI remain
- ARD-013 — AN-01 overview data foundation (shell already exists)

## Iranian feature checklist (in scope)

- UX copy / RTL screens / mobile / تومان display UI: N/A — titles + stubs only
- Persian dashboard titles: required
- Jalali/`Asia/Tehran` range helpers stub: required (full picker → ARD-016 UI)
- Analytics & reporting labels: Persian titles checked

## Risks

- Over-implementing Drizzle migrations / HTTP Route Handlers / chart UI → keep contract + in-memory + ports
- Computing live SUM on request without projections → violate ADR decision; use counters/projections
- Confusing with ADR-062 management dashboards (Mongo/admin) — keep merchant-scoped PG only
- Blocking POS on projection apply — must stay off checkout critical path

## Completion notes

- Implemented `src/merchant-oltp-analytics` — PG AN-01..04 decision, sales/membership counter ports, AN-01 overview builder, Persian titles, Jalali/`Asia/Tehran` range helpers stub, Redis TTL 60s cache note, in-memory counters; `src/modules/analytics` — overview use cases + SaleCompleted daily revenue projection apply (in-memory); flipped analytics-boundaries merchant OLTP pointer; ARD-016 foundation notes
- Validate green (638 tests); STATUS → completed; Next: ADR-062
- Iranian checklist: UX screens N/A (uiuxpromax N/A → ADR-088 / ARD-016); Persian titles + RTL stubs + Jalali range stub + تومان unit note tested; HTTP APIs / Drizzle Kit / live Redis deferred to ARD-016
