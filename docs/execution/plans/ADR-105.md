# Plan — ADR-105 Staff and Store Customer PWA Completion

## Summary

Complete staff POS offline sale queue (IDB + SW background sync → POS API with Idempotency-Key) and reject-and-review stock conflicts (ADR-024/091), while shipping installable **per-store customer** PWA manifests/theme/icons from live store branding with a thin online-first customer SW. **Never conflate** staff (`/staff` manifest, `/pos`, `sw-staff.js`) with store customer (`/s/{slug}` manifest, customer install chrome, `sw-store-customer.js`).

**Status:** `in_progress` — operator requested: do **not** move ADR to `done/` / do **not** commit.

## Iranian First gate

| Check | Result |
| --- | --- |
| Persian text? | Yes — install, offline, queue review, stock-reject, iOS A2HS |
| RTL? | Yes — staff POS + storefront install banners (`lang=fa` / `dir=rtl`, logical CSS) |
| Jalali? | N/A for PWA chrome (no new date UI; POS receipt already Jalali) |
| تومان? | Yes — offline queue note + amounts shown via existing POS تومان format |
| Iranian behavior? | Offline queue for cash-desk peak hours; install-to-home on Android WebView; no delivery |
| Iranian mobile? | Touch ≥44px; one-handed offline CTAs; iOS A2HS Persian fallback |

## uiuxpromax gate (ADR-021) — BEFORE UI code

**Evidence:** `.cursor/skills/uiuxpromax-integration/SKILL.md` + `docs/uiux/pwa-experience.md` present; external binary N/A → skill/docs fallback.

### Brief A — Staff offline queue (fa-IR / RTL / ~390px Android POS)

- **Persona:** صندوقدار مغازه کرمان — فروش را هنگام قطعی ثبت می‌کند؛ بعداً صف را بررسی می‌کند.
- **Terms:** صندوق، آفلاین، صف فروش، همگام‌سازی، کمبود موجودی، بررسی
- **Screens / states:**
  1. Banner online / offline / syncing / queued count
  2. Review queue sheet/list — rejected stock rows + retry failed
  3. Install chrome (existing) with offline note
- **Copy samples:** reuse/extend `POS_OFFLINE_COPY_FA`; stock reject never silent
- **A11y:** `role=status` / `aria-live`; CTAs ≥44px

### Brief B — Store customer install (fa-IR / RTL / ~390px)

- **Persona:** مشتری محلی — اپ مغازه را روی صفحه اصلی می‌گذارد
- **Terms:** اپلیکیشن فروشگاه، افزودن به صفحه اصلی، برند مغازه
- **Screens:** branded install banner + iOS Safari share instructions when BIP missing
- **Isolation:** no staff wording, no `/pos`, no staff SW registration

### Visual direction

Keep existing retail daylight tokens; distinct staff slate theme vs store primaryColor; no purple/cream clichés; no card-heavy install chrome.

## Tasks

1. **Plan + uiuxpromax briefs** (this file) — before code
2. **Staff offline runtime**
   - Browser IndexedDB adapter (`mos-staff-pos` / `sale_drafts`) — no JWT plaintext
   - Client enqueue when CompleteSale fails offline / `navigator.onLine === false`
   - Flush on: `online`, SW sync message, review CTA — `POST /api/v1/pos/sales` with `Idempotency-Key = syncKey` + CSRF cookies
   - Map `INSUFFICIENT_STOCK` → `rejected_for_review` UX (Persian), never silent overwrite
   - Extend `public/sw-staff.js`: precache POS shell; sync tag; optional SW-side flush attempt without storing secrets
3. **Sync acceptance**
   - Confirm POS CompleteSale already idempotent; add focused tests for replay + stock reject path
   - Optionally add `POST /api/v1/sales/sync` batch handler matching ADR-024 contract (thin wrapper)
4. **Staff offline UI**
   - Wire `StaffOfflineStatus` to live queue depth + rejected list + actionable CTAs
   - Wire `PosRegister` offline enqueue path
5. **Customer PWA**
   - Manifest route loads live store branding (`displayName`, `primaryColor`, icon when URL/key usable; else default SVG)
   - Thin `public/sw-store-customer.js` online-first; scope under `/s/` only; register from store home only
   - Polish install prompt: `STORE_CUSTOMER_PWA_COPY_FA` + iOS A2HS Persian hint; theme accent
6. **Assets** — default SVG icons for staff + customer under `public/icons/`
7. **Tests + `npm run validate`** with `DATABASE_URL=postgres://merchantos:merchantos@localhost:5433/merchantos`
8. **STATUS / progress-log** notes only; **do not** move ADR to `done/`

## Schema / API / UI / events / cache

| Layer | Change |
| --- | --- |
| Schema | None |
| API | POS sales idempotency proven; optional `/api/v1/sales/sync` batch |
| UI | Staff offline queue review; customer install + iOS hint; branded manifest |
| Events | SaleCompleted on successful sync only (existing); metrics names already reserved |
| Cache | Staff SW shell precache; customer SW thin network-first; manifests `max-age=600` |

## Test matrix

- Staff vs customer manifest paths / start_url / SW scripts never collide
- Customer manifest reads live branding (name/theme)
- IDB enqueue + flush success with idempotency replay
- Stock shortage → rejected_for_review, not synced
- Persian copy present on offline + install surfaces
- uiuxpromax gates still assert
- SW files present with distinct audiences

## Related ARD / ADR

- ADR-022 staff PWA / ADR-023 store customer PWA
- ADR-024 offline queue / ADR-091 reject-and-review
- ADR-096 online CompleteSale (POS API)
- ADR-100 storefront home install chrome
- ADR-111 icon assets when MinIO branding lands (fallback defaults now)
- ARD-017 / ARD-029

## Iranian feature checklist (in scope)

- [x] Persian UI / errors / empty / offline / install / iOS hint
- [x] RTL + logical layout
- [x] Mobile ≥44px offline/install CTAs
- [x] تومان note on offline queue
- N/A: Jalali new UI, SMS templates, analytics charts, address forms

## Risks

- Background Sync uneven on Iranian Android WebViews → rely on `online` event + manual «بررسی صف»
- CSRF in SW without document → primary flush from open client; SW notifies clients
- Branding icons pending MinIO (ADR-111) → default SVG until `logoObjectKey` is a usable URL/`/` path

## Post-complete (deferred — operator request)

When AC confirmed: move `adrs/tasks/ADR-105-*` → `adrs/done/`, update STATUS + REORGANIZATION_INDEX + progress-log.

## Acceptance mapping

| AC | Plan |
| --- | --- |
| Staff PWA installable; offline sale queued and syncs when online | IDB + SW + PosRegister enqueue + flush → POS API |
| Stock conflict = reject-and-review, no silent overwrite | Map `INSUFFICIENT_STOCK` → rejected list UX |
| Customer PWA installable per store with name/theme | Live branding on manifest + install prompt |
| Staff and customer manifests/start URLs remain distinct | Assert isolation tests + separate SW scripts |
