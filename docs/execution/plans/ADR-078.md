# Plan — ADR-078 Testing Strategy

## Summary

Encode MerchantOS testing strategy as an importable contract in `src/testing-strategy/`: classic pyramid (many domain unit → fewer integration → few e2e), Vitest as the unit runner, mandatory tenant isolation tests when data is touched, AuthZ tests when auth is touched, Persian string / Iranian format regression notes for UX paths, performance smoke budgets for POS/barcode when those paths land, and CI `validate` gate (`typecheck` + `lint` + `test`). Align with `docs/rules/testing-rules.md` and `docs/testing/strategy.md`. Layer tooling depth (Testcontainers/Playwright harnesses) is ADR-079 — stance encoded here only.

## Iranian First impact

- Persian text: Yes — strategy requires fa-string regression coverage when UX/copy in scope; fixtures may include Persian names
- RTL: Note — layout/RTL tests or screenshots for primary shells where feasible (ADR-079 e2e)
- Jalali / تومان: Format helper tests required when those helpers exist / paths touch money/dates
- Iranian workflows: Critical journeys include auth OTP, POS CompleteSale, storefront pickup order
- Mobile: E2E on mobile viewports when UI e2e runs (ADR-079)
- Checklist: No merchant/customer UI shell this cycle; Persian regression policy in contract yes

## Schema / API / UI / events / cache

- No OLTP schema, HTTP routes, UI, analytics emit, or cache changes
- Telemetry gate: N/A
- uiuxpromax: N/A (strategy/docs/contract only)
- No delivery journeys in must-cover e2e list

## Domain changes

- `src/testing-strategy/` — pyramid layers, Vitest unit tool, integration/e2e/perf stance, tenant isolation mandatory, AuthZ mandatory when data/auth touched, Persian/RTL/Jalali/تومان regression notes, CI validate scripts, CompleteSale must-cover, assert helpers
- Light sync: `docs/rules/testing-rules.md`, `docs/testing/strategy.md`, `docs/testing/test-pyramid.md`
- Ensure `package.json` `validate` remains `typecheck && lint && test` with Vitest `test` script

## Gates

- Telemetry: N/A
- Persistence: N/A (integration DB tools deferred ADR-079)
- uiuxpromax: N/A
- No delivery: e2e must-cover excludes courier/delivery

## Test matrix

- Unit: pyramid order unit > integration > e2e
- Unit: unit runner is Vitest (Jest alternative noted but Vitest selected)
- Unit: tenant isolation tests required whenever data/tenant queries touched
- Unit: AuthZ tests required when auth/authorization touched
- Unit: Persian string regression note + fixture policy encoded
- Unit: تومان / Jalali format tests required when helpers/UX in scope
- Unit: CI validate gate matches package.json scripts
- Unit: POS CompleteSale / barcode-checkout perf smoke stance
- `npm run validate`

## Related ARD checkboxes

- Cross-cutting testing convention for all ARD test matrices — fulfilled at standards layer; per-ARD matrices remain per-ARD

## Iranian feature checklist (in scope)

- Persian string regression policy for UX-in-scope features: yes
- Primary UI / RTL layout / mobile UX shells: N/A — no UI this ADR (policy encoded for later ADRs)

## Risks

- Installing Playwright/Testcontainers this cycle → avoid; ADR-079 owns layer tooling wiring; stance only here
- Overbuilding CI YAML before ADR-069 → encode validate scripts as the gate; CI workflow file may wait for ADR-069

## Completion notes

- Implemented as planned; `npm run validate` green (170 tests)
- Iranian feature checklist: Persian string regression policy + fa fixtures; UI/RTL shell N/A; تومان/Jalali format tests mandated when helpers/UX touch
- `src/testing-strategy` contract + helpers; synced strategy / pyramid / testing-rules docs
- Playwright/Testcontainers wiring deferred to ADR-079; CI workflow YAML deferred to ADR-069 (scripts binding now)
- Next: ADR-031 Merchant Authentication
