# Plan — ADR-099 Loyalty Points, Rewards, Coupons, Wallet Runtime

## Summary

Land loyalty **product runtime** on existing domain (ADR-010) + Drizzle repos (ADR-093) + HTTP surface (ADR-094): earn on POS CompleteSale publishes `PointsEarned`; redeem API/UI publishes `PointsRedeemed`; expiry scheduled job runs `expireStaleWallets` and publishes `PointsExpired`; merchant configures rules; CRM + customer portal show per-store wallet (Persian RTL, تومان labels, Jalali dates).

**Status:** `in_progress` — do **not** move ADR to `done/` until operator confirms (this cycle leaves file in `adrs/tasks/`).

## Iranian First gate

| Check | Result |
| --- | --- |
| Persian text? | Yes — merchant loyalty settings, POS redeem, CRM wallet, customer wallet, errors/empty/loading |
| RTL? | Yes — `lang=fa` / `dir=rtl` shells; logical CSS |
| Jalali? | Yes — last earn / ledger / expiry dates via `fa-IR-u-ca-persian` + `Asia/Tehran` |
| تومان? | Yes — rule editor shows earn threshold as تومان (from IRR minor); points labeled «امتیاز» |
| Iranian behavior? | Earn after حضوری sale; redeem at صندوق; wallet scoped to store membership |
| Iranian mobile? | Merchant + customer surfaces ~390px, ≥44px taps |

## uiuxpromax gate (ADR-021) — BEFORE UI code

**Evidence:** skill + `docs/uiux/*` present; external binary N/A → fallback via skill/docs.

### Brief (fa-IR / RTL / ~390px Android)

- **Persona:** صاحب مغازه کرمانی — قانون امتیاز را عوض می‌کند؛ صندوقدار هنگام تسویه امتیاز کم می‌کند؛ مشتری در پنل همان مغازه موجودی می‌بیند.
- **Context terms:** مغازه، صندوق، مشتری، امتیاز، کیف امتیاز، قانون امتیاز
- **Composition:** existing merchant daylight tokens; storefront dashboard column
- **Screens:**
  1. **تنظیمات باشگاه** (`/loyalty`) — انتخاب فروشگاه، آستانه تومان به‌ازای امتیاز، ماه انقضا (یا غیرفعال)
  2. **صندوق — اعمال امتیاز** — با شماره موبایل موجودی؛ ورود تعداد امتیاز؛ خطا فارسی
  3. **پروفایل مشتری CRM** — بلوک کیف امتیاز + تاریخ آخرین کسب (جلالی)
  4. **کیف امتیاز مشتری** (`/s/[slug]/dashboard/wallet`) — موجودی + چند ردیف دفترچه
- **States (Persian):** loading / empty («هنوز امتیازی ندارید») / insufficient balance / network
- **Component map:** `LoyaltySettingsClient`, POS redeem panel in `PosRegister`, CRM wallet strip, customer wallet client/SSR; FA copy under `src/modules/loyalty/ui`
- **A11y:** labels فارسی؛ `aria-live`; targets ≥44px
- **Assert:** `assertUiuxGate` in `src/modules/loyalty/ui/index.test.ts`

### Visual direction

Reuse catalog/CRM bordered surfaces — utility merchant UI, not marketing hero; no purple/cream clichés.

## Tasks

1. **Plan + uiuxpromax brief** (this file) — before code
2. **Events wiring:** enqueue `PointsEarned` after POS earn (composition/outbox); enqueue `PointsRedeemed` in redeem handler; return event from handlers for tests
3. **Expiry job:** replace loyalty stub with `runLoyaltyPointsExpiryJob` calling `expireStaleWallets` + outbox enqueue; update `SCHEDULED_JOB_HOOKS` + tests
4. **Redeem hardening:** optional idempotency by `referenceId` (skip double debit); merchant membership lookup by phone for POS
5. **HTTP:** keep `/api/v1/loyalty/rules|wallets|redeem`; add customer storefront wallet read `GET /api/v1/storefront/[slug]/wallet` (customer session + store scope)
6. **UI:** `/loyalty` settings; POS redeem controls; CRM profile wallet; customer wallet live data
7. **Coupons:** thin create/redeem deferred as gap — schema stub lacks discount payload; POS uses **points redeem** for LYL-03 MVP path
8. **Tests + `npm run validate`** with `DATABASE_URL=postgres://merchantos:merchantos@localhost:5433/merchantos`
9. **STATUS / progress-log** notes; **do not** move ADR to `done/`

## Schema / API / UI / events / cache

| Layer | Change |
| --- | --- |
| Schema | None new (`point_rules`, `wallets`, `points_ledger`; coupons stub unused) |
| API | Event publish on redeem; storefront customer wallet GET; optional membership-by-phone helper via CRM |
| UI | `/loyalty`, POS redeem, CRM wallet strip, customer wallet |
| Events | `PointsEarned` / `PointsRedeemed` / `PointsExpired` → outbox |
| Cache | Invalidate mapping deferred ADR-108; events still enqueued |

## Test matrix

- Sale complete → earn per default 100_000 IRR = 1 point; idempotent saleId
- Redeem decreases balance + enqueues/publishes `PointsRedeemed`
- Expiry job zeroes stale wallet + `PointsExpired` (12m / disabled rule)
- Customer wallet read only for membership of that store (phone ↔ store)
- Merchant rule configure GET/PUT; Persian error codes
- uiuxpromax gate + FA copy smoke (امتیاز، تومان، جلالی)
- Iranian checklist items below

## Related ARD / ADR

- ADR-010 loyalty domain; ADR-091 expiry default 12m
- ADR-093/094 Drizzle + HTTP
- ADR-096 POS CompleteSale already calls `LoyaltyEarnPort`
- ADR-098 CRM profile receives wallet strip
- ADR-100 order-paid earn port exists but **not wired** (storefront orders still open)
- ADR-103 customer OTP session used for wallet ACL
- ADR-109 full EMQX workers — job function wired here; broker scale remains ADR-109

## Iranian feature checklist (in scope)

- [x] Persian UI / errors / empty / loading
- [x] RTL + logical layout
- [x] Mobile ≥44px
- [x] تومان on rule threshold display
- [x] Jalali last-earn / ledger dates
- [x] Iranian mobile on POS redeem lookup
- N/A: SMS templates (ADR-103/115); maps; coupon campaign UX (schema stub)

## Risks

- Concurrent redeem without strong version compare — mitigate with ledger append + optional referenceId idempotency
- CustomerIdentity `id` ≠ membership `customerId` historically — resolve membership via **store + phoneNational** from identity
- Order-paid earn deferred to ADR-100
- Coupons FR-3 partial — points redeem at POS covers redeem path; coupon codes deferred

## Post-complete (deferred — operator request)

When AC confirmed: move `adrs/tasks/ADR-099-*` → `adrs/done/`, update STATUS + REORGANIZATION_INDEX + progress-log.

## Acceptance mapping

| AC | Plan |
| --- | --- |
| Completing a sale awards points per configured rule | POS `loyaltyEarn` already wired; ensure rule + event + tests |
| Redeem decreases wallet and publishes redeem event | Handler + outbox enqueue + POS UI |
| Expiry job removes expired points and publishes `PointsExpired` | `runLoyaltyPointsExpiryJob` non-stub |
| Customer portal shows balance for that store membership only | Storefront wallet page + API scoped by store+phone |
