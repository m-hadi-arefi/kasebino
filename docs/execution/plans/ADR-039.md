# Plan — ADR-039 Realtime Client Strategy MQTT with Poll Fallback

## Summary

Ship browser/staff realtime client strategy in `src/realtime-client/`: prefer MQTT over WebSocket to EMQX (using short-lived creds from ADR-038 mint), invalidate TanStack Query on envelope messages, fall back to HTTP poll of critical lists on disconnect, exponential reconnect backoff tuned for Iranian mobile networks, and Persian reconnect/offline toast copy. Mint HTTP Route Handler at `POST /api/v1/realtime/token`.

## Iranian First gate

| Check | Impact |
| --- | --- |
| Persian text? | Reconnect / offline / poll-fallback toast copy fa-IR (extends `EVENT_UX_FA`) |
| RTL? | Toast/drawer consumers `dir=rtl`; no full drawer UI this cycle |
| Jalali? | N/A — no datetime display surfaces |
| تومان? | N/A — no money display |
| Iranian behavior? | POS/pickup boards stay live; poll fallback keeps counter wait perception when MQTT flakes |
| Iranian mobile? | Prefer MQTT; poll only on disconnect; reconnect backoff + modest poll interval (battery/bandwidth) |

Iranian checklist: Persian realtime toast copy + RTL dir; full UI screens N/A (uiuxpromax N/A).

## Gates

- Telemetry: reconnect/disconnect metric name stubs; full metrics → ADR-074
- Persistence: N/A (uses ADR-038 mint; no new tables)
- uiuxpromax: N/A (copy + client strategy only; no visual toast/drawer component)
- Delivery: no courier/delivery
- Prerequisites: ADR-038 + ADR-025 completed

## Task breakdown

1. Mark STATUS `in_progress`; write this plan
2. `src/realtime-client/` — decision, MQTT-over-WS preference, transport port + in-memory adapter, reconnect backoff, poll fallback controller, TanStack Query invalidation by topic channel, Persian UX copy, token mint handler contract, metrics stubs
3. `app/api/v1/realtime/token/route.ts` — thin Route Handler using package mint handler (session merchant inject)
4. Flip ADR-038 deferred client pointers → package; sync `08-real-time`, `docs/tech/emqx.md`, ARD-015/001 notes
5. Tests + `npm run validate` → STATUS completed → progress-log Next ADR-090

## Schema / API / UI / events / cache

- Schema: none
- API: `POST /api/v1/realtime/token` → short-lived MQTT creds (+ broker WS URL hint)
- UI: Persian toast copy contract only (render deferred)
- Events: client consumes canonical envelopes; invalidates Query caches
- Cache: TanStack Query invalidate/refetch; poll fallback refetches critical keys

## Test matrix

- Decision locks MQTT preferred + poll fallback + Query invalidation
- Token mint handler returns short-lived creds; ACL merchant-scoped; unauthorized → 401 Persian message
- MQTT message on sales topic → invalidate `sales` query keys for merchant scope
- Disconnect → enter poll fallback + Persian offline/reconnecting copy; poll ticks invalidate critical keys
- Reconnect succeeds → leave poll fallback; backoff delays grow (capped)
- WebSocket URL derived from MQTT/WS broker hint
- Persian UX strings contain fa script + `dir=rtl`
- Cross-tenant topic forbidden via ADR-038 ACL still held
- `npm run validate`

## Related ARD checkboxes

- ARD-015 — Token API + MQTT client + poll fallback landed (full ARD may remain todo until compose e2e / live boards)
- ARD-001 — realtime client package note
- ARD-014 — in-app MQTT toast path stubbed via Persian reconnect copy

## Iranian feature checklist (in scope)

- Persian reconnect/offline/poll-fallback copy: yes
- RTL dir on UX contract: yes
- Mobile / bandwidth: yes (backoff + poll only when disconnected)
- UX screens / تومان / Jalali / phone: N/A

## Risks

- Real `mqtt.js` WebSocket socket deferred to thin connect-options / transport port (in-memory for tests) — production adapter wires when boards land
- Session authorizer on token route is injectable (Auth.js wiring may still be thin)

## Completion notes

- Implemented `src/realtime-client` (MQTT-over-WS preferred, transport port + in-memory adapter, TanStack Query invalidation, poll fallback, reconnect backoff, Persian RTL toast copy) + `POST /api/v1/realtime/token` Route Handler
- Flipped ADR-038 client deferrals; synced `08-real-time`, `17-message-broker`, `docs/tech/emqx.md`, ARD-015/001/014 notes
- Validate green (546 tests); STATUS → completed; Next: ADR-090
