# Plan — ADR-016 Next.js Application Architecture

## Summary

Scaffold Next.js 15+ App Router as the modular-monolith host (UI + Route Handlers + Server Actions), with Iranian First root shell defaults (`lang=fa`, `dir=rtl`), and encode the architecture decision as an importable contract in `src/nextjs-architecture/`. Preserve existing domain contracts under `src/` and keep `npm run validate` green.

## Iranian First impact

- Persian/RTL: root `app/layout.tsx` defaults `lang=fa` + `dir=rtl`; Persian metadata/title on shell page
- Jalali/تومان: not rendered yet (ADR-017+ UI); defaults remain on ADR-001 `LOCALE_DEFAULTS`
- Iranian workflows: App Router host for POS/storefront later; no delivery surfaces
- Mobile: minimal shell only — full loading/mobile UX deferred to ADR-017+
- Checklist (shell UX in scope):
  - Primary UI copy Persian — yes (home title)
  - Root `dir=rtl` / `lang=fa` — yes
  - Error/empty/notifications/receipts — N/A (no flows yet)
  - Logical CSS / icons / forms — N/A (no design system)
  - Currency/Jalali/phone — N/A (no money/date UI)
  - SEO Persian title — yes (shell metadata)

## Database design

- N/A (no persistence in this ADR)

## Domain changes

- `src/nextjs-architecture/` — App Router-only, RSC default, shell locale rules, composition boundaries, forbids Pages Router
- Filesystem scaffold: `app/layout.tsx`, `app/page.tsx` (Persian)
- No Pages Router `pages/` directory

## API / UI / events / cache

- Next.js scripts: `dev` / `build` / `start`
- Typecheck stays on contracts via `tsconfig.contracts.json`; Next uses root `tsconfig.json`
- Middleware/correlationId: contract-encoded; implementation deferred
- uiuxpromax: N/A beyond shell defaults (ADR-021 gates full UI)

## Gates

- Telemetry: correlationId middleware noted as future middleware slot (N/A emit)
- Persistence: N/A
- uiuxpromax: shell-only defaults; no component system
- No delivery: no delivery routes/modules

## Test matrix

- Unit: router = App Router; Pages Router forbidden
- Unit: rendering model defaults to Server Components
- Unit: shell lang/dir/locale align with `LOCALE_DEFAULTS` / ADR-004 shell
- Unit: `app/layout.tsx` exists with `lang="fa"` and `dir="rtl"`; no `pages/`
- Unit: presentation calls application; business rules not in pages
- `npm run validate`

## Related ARD checkboxes

- ARD-001 “Next.js 15+ App Router” partially satisfied (scaffold); foundation ARD remains open for Docker/health/etc.

## Risks

- Dual tsconfig (contracts vs Next) — document scripts clearly
- Next auto-edits `tsconfig.json` — keep contracts typecheck on dedicated project
- Over-building UI / Tailwind / shadcn — explicitly out of scope (ADR-017+)

## Completion notes (2026-08-03)

- Implemented as planned; `npm run validate` green (54 tests); `npm run build` green
- Iranian feature checklist: shell UX — Persian title + `lang=fa`/`dir=rtl`; other UI items N/A until ADR-017+
- Dual tsconfig: contracts via `tsconfig.contracts.json`, Next via root `tsconfig.json`
- STATUS → `completed`
