# Plan — ADR-060 User Behavior and Clickstream Tracking

## Summary

Encode MerchantOS **clickstream** as Mongo `mos_behavior` (ADR-056 locked name; colloquial `mos_clickstream`) via beacon batch helpers in `src/clickstream/`: best-effort `PageViewed` / `ElementClicked` / optional sampled `ScrollDepth`, store-scoped events, POS critical UX at 100% sample, noisy storefront sampling configurable, PII/secret scrub, ingest isolated through ADR-065 fire-and-forget buffer. In-memory store for tests; HTTP beacon / client SDK wiring / session heartbeats remain ARD-027 packaging (sessions → ADR-061).

## Iranian First gate

| Check | Impact |
| --- | --- |
| Persian text? | Metric / path labels Persian (`CLICKSTREAM_METRIC_LABELS_FA`); event codes English |
| RTL? | Dashboard viewer deferred (uiuxpromax); stubs declare `dir=rtl` / `fa-IR` |
| Jalali? | Store ISO UTC; Jalali/`Asia/Tehran` buckets when merchant UX lands |
| تومان? | N/A — no money in clickstream props; never claim Mongo accounting |
| Iranian behavior? | POS critical UX 100%; storefront QR/path analysis; pickup-only (no delivery paths) |
| Iranian mobile? | Viewport class mobile/desktop; phone props hashed/scrubbed; beacon never blocks POS |

Iranian checklist: UX screens N/A (infra + labels). Persian metric names + RTL stubs + phone privacy required. Path dashboards → ARD-027 / uiuxpromax later.

## Gates

- Telemetry: this ADR *is* clickstream (`mos_behavior` / PageViewed + ElementClicked); track ingest via ADR-065
- Persistence: Mongo `mos_behavior` time-series stance; TTL 90–180d; in-memory this cycle
- uiuxpromax: N/A (no dashboard pages this cycle; Persian labels/contracts only)
- Delivery: no courier/delivery path events
- Prerequisites: ADR-056 completed; ADR-059 completed; ADR-065 isolation package completed

## Task breakdown

1. STATUS `in_progress`; write this plan
2. `src/clickstream/` — decision, event catalog, sampling policy (POS 100% / noisy sample), ClickstreamDocument schema, InMemory store, `trackClickstream` / `ingestBeaconBatch` via ADR-065, PII/secret scrub + phone hash, Persian metric labels, indexes/API/CORS reserve, metrics, assertions
3. Flip mongodb-analytics / analytics-boundaries / event-warehouse placement pointers
4. ARD-027 foundation notes; ARD-021 progress note; docs/ards STATUS
5. Tests + `npm run validate` → STATUS completed → progress-log Next: ADR-061

## Schema / API / UI / events / cache

- Schema: logical `mos_behavior` docs (unique `eventId`); indexes `sessionId+occurredAt`, `merchantId+occurredAt`, `eventType+occurredAt`
- API: reserve `POST /api/v1/analytics/beacon` (batch); session API → ADR-061 / ARD-027 remain
- UI: none this cycle (Persian metric names ready)
- Events: `PageViewed`, `ElementClicked`, optional `ScrollDepth` (sampled); storefront `StorefrontViewed` / `ProductDetailViewed` companions; sessions deferred ADR-061
- Cache: client may queue offline then beacon — server Redis N/A
- CORS: locked to app origins (staff PWA + storefront)

## Test matrix

- Collection is `mos_behavior` (ADR-056); not OLTP SoT; colloquial alias `mos_clickstream` documented
- POS critical events sampleRate 1; funnel companions never dropped by sampler
- Noisy events respect configured sample rate (drop when unsampled)
- `trackClickstream` / beacon batch accept via ADR-065 buffer; never throws when Mongo/sink down
- Secrets stripped; Iranian phones hashed/scrubbed; Persian property text preserved
- Store-scoped require merchantId (+ storeId when storefront)
- Idempotent eventId
- Off checkout critical path
- Pointer flips + `npm run validate`

## Related ARD checkboxes

- ARD-027 — clickstream track + sampling + PII scrub foundation; HTTP beacon + client SDK + session heartbeats + live driver remain
- ARD-021 — clickstream plane consumer note

## Iranian feature checklist (in scope)

- UX copy / RTL screens / mobile / تومان display UI: N/A — labels + stubs only
- Persian metric / path labels: required
- Phone privacy for clickstream (hash/scrub Iranian mobiles): required
- Jalali merchant-facing buckets: deferred to ARD-027 UI

## Risks

- Over-implementing beacon HTTP / session APIs / heatmaps → keep contract + in-memory + isolated track
- Using colloquial `mos_clickstream` as collection name — must write ADR-056 locked `mos_behavior`
- Blocking OLTP/POS on Mongo failure — must use ADR-065 best_effort_track path
- Confusing with product `mos_product` or warehouse `mos_events` — clickstream writes `mos_behavior` only

## Completion notes

- Implemented `src/clickstream` — PageViewed/ElementClicked/ScrollDepth + POS/storefront companions, sampling (POS/funnel 100%, noisy default 10%), `trackClickstream` + `ingestBeaconBatch` via ADR-065, `mos_behavior` (+ colloquial `mos_clickstream`), PII scrub, Persian metric labels, CORS/API reserve, TTL stance; in-memory store + tests; flipped mongodb-analytics / analytics-boundaries / event-warehouse pointers; ARD-021/027 foundation notes
- Validate green (616 tests); STATUS → completed; Next: ADR-061
- Iranian checklist: UX screens N/A (uiuxpromax N/A); Persian metric labels + RTL stubs + phone privacy tested; Jalali path viewer deferred to ARD-027; sessions deferred to ADR-061
