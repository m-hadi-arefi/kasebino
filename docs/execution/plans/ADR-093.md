# Plan — ADR-093 Replace In-Memory Repositories with Drizzle

## Summary

Implement Drizzle repository classes for all MVP ports under module `infrastructure/persistence`, honor soft-delete + tenant filters, retain in-memory only for unit tests, add integration tests against Compose Postgres, wire a production composition root that never selects in-memory repos.

## Iranian First impact

- Persian UTF-8 text round-trips via repositories (names, notes, product titles)
- Phone national/e164 stored and looked up for identity/membership
- UX copy N/A (no new UI); checklist UX N/A

## Schema / API / UI

- Schema already from ADR-092; no new tables unless mapper gaps
- No HTTP/UI in this ADR (composition root only)

## Task breakdown

1. Inventory all repository interfaces + in-memory impls
2. Shared helpers: soft-delete filter, tenant scope asserts
3. Drizzle repos per module (identity, merchant, store, crm, catalog, inventory, pos, loyalty, ordering, payments, notifications, admin, outbox)
4. Composition root `src/infrastructure/persistence/compose-repositories.ts` for production
5. Integration tests CRUD+isolation+soft-delete (DATABASE_URL)
6. Unit tests: production compose forbids InMemory*

## Test matrix

- Integration per aggregate when DATABASE_URL set
- Soft-deleted excluded from default lists
- Cross-tenant isolation (merchantId)
- `npm run validate`

## Risks

- Outbox co-transaction with mutations — share client/tx helper
- Large surface — prioritize ports used by MVP use cases

## Post-complete

- Move ADR-093 → done; next ADR-095 (auth) per critical path after 093, then 094
