# Execution Record: ADR-142 (Ordering ↔ Inventory Wiring)

## Implementation Summary
- Verified existing inventory application use cases (`decrementForPickupPaid`, `restorePickupStock`) were fully implemented per ADR-049.
- Created `ordering-inventory-adapter.ts` with `InventoryReservePort` and `InventoryReleasePort` implementations.
- Implemented `DrizzleSyncIdempotency` to provide idempotency using `stock_movements.note` (storing the `syncKey`) and `source='pickup'`.
- Updated composition root (`create-api-context.ts`) to wire the real adapters to `createOrderingUseCases` in production contexts, replacing the stubs.
- Added a production boot guard (`assertProductionInventoryWiring`) to ensure stubs are never deployed to production.
- Created unit tests verifying adapter correctness and idempotency logic.
- Verified storefront Persian labels ('ناموجود' and `fa.availableQty`) were correctly in place.

## Pre-work Completed
- Verified ERPNext contracts and moved ADR-135 through ADR-139 to `adrs/done/`.
