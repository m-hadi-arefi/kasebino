# Plan — ADR-035 Background Jobs and Transactional Outbox

## Summary

Implement the transactional outbox **worker skeleton**: enqueue in same conceptual TX, poll pending messages, dispatch to fan-out handlers at-least-once with an idempotent processed set, and register scheduled job hooks for pickup unpaid cancel + loyalty points expiry (timer values from ADR-091 `mvp-policies`). Workers share the modular-monolith codebase (ADR-004). Align with ADR-036 envelope / `OUTBOX_EVENT_ROW` / consumers. No EMQX publish or Mongo warehouse adapter this cycle (ADR-038 / ADR-057).

## Iranian First gate

| Check | Impact |
| --- | --- |
| Persian text? | Job/notification log-safe stubs reuse ADR-036 `EVENT_UX_FA` Persian copy; wire schemas English |
| RTL? | Notification drawer note only (no UI this cycle) |
| Jalali? | N/A — timers stored as duration minutes/hours UTC; display deferred |
| تومان? | N/A — no money display |
| Iranian behavior? | Pickup unpaid 30m cancel + loyalty 12m expiry hooks honor ADR-091 |
| Iranian mobile? | N/A — backend worker; battery-friendly poll interval contracted |

Iranian checklist: UX N/A (no merchant/customer UI); Persian notification stubs contracted.

## Gates

- Telemetry: lag metrics contracted as hooks/constants; warehouse mirror → ADR-057
- Persistence: in-memory store + Drizzle schema stubs for `outbox_events` / `processed_events`; Kit migrations → ARD-001
- uiuxpromax: N/A (no UI)
- Delivery: no delivery/courier jobs

## Task breakdown

1. `src/outbox/` — decision, `OutboxMessage`, store port + `InMemoryOutboxStore`, processed set, poll/dispatch worker, consumer dispatcher (spine feeds), scheduled job hooks, Persian worker notes, asserts
2. Cross-link ADR-036 `TRANSACTIONAL_OUTBOX.workerDeferredTo` → implemented in `src/outbox`
3. Drizzle schema stubs `outbox_events` + `processed_events` under `src/infrastructure/database/schema/platform.ts`
4. Tests: enqueue+dispatch; idempotent re-dispatch; mark published; job hooks read mvp-policies timers; workersShareCodebase
5. ARD-001/015/024 foundation notes; validate → STATUS completed → progress-log Next ADR-052

## Schema / API / UI / events / cache

- Schema: Drizzle stubs only (migrations deferred ARD-001)
- API/UI: none
- Events: consume ADR-036 envelopes; dispatch stubs for cache / emqx / warehouse / notifications
- Cache: invalidation channel stub only (ADR-054)

## Test matrix

- Enqueue pending → poll → dispatch → `publishedAt` set
- Idempotent processed set skips duplicate consumer work
- Job hooks expose ADR-091 unpaid 30m + loyalty 12m
- No PII payload in scrubbed worker logs
- `DEPLOYABLE.workersShareCodebase === true`

## Related ARD checkboxes

- ARD-001 — outbox worker poll + repository iface foundations (migrations remain)
- ARD-015 — outbox poll/dispatch path noted; EMQX publisher remains
- ARD-024 — outbox-driven delivery path noted; Mongo mirror remains

## Risks

- Full PG poll locking / SKIP LOCKED deferred to ARD-001 migrations
- Real EMQX / Mongo adapters deferred — stubs only

## Completion notes (2026-08-03)

- Implemented `src/outbox` + schema stubs; validate green (333 tests)
- STATUS → completed; Next: ADR-052 Cache-Aside Read Strategy
