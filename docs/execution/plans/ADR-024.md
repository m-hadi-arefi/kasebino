# Plan — ADR-024 Offline-First Staff POS Strategy

## Summary

Encode staff POS **online-first P0 / offline sale-queue P1** at `src/pos-offline/`: idempotent sync keys, stock shortage = reject-and-review (ADR-091 / ADR-049), Persian shop-floor banners/messages, in-memory offline sale queue helper (IDB port model), sync batch API contract (`POST /api/v1/sales/sync`), `SaleCompleted` on successful sync, SW/IDB policy for staff PWA only (≠ store customer ADR-023). Wire live Persian offline status copy on `/pos`; update `src/staff-pwa` offline reservation to ADR-024 package.

## Iranian First impact

- Persian text: yes — offline/online/queue/conflict/sync failure banners + errors `fa-IR`
- RTL: yes — POS banner uses logical layout under staff `dir=rtl` shell
- Jalali: N/A this cycle (no date widgets on sync banners)
- تومان: queued sale drafts carry IRR minor amounts; display labels use تومان wording in shop-floor copy
- Iranian workflows: cashier flaky-network: queue sale → sync → reject shortage for review (never silent overwrite / double charge)
- Mobile: Android POS ~390px; offline banner readable one-handed; touch ≥44px for “بررسی صف”
- Checklist (offline UX + contract):
  - Persian primary / error / empty / conflict copy — yes
  - RTL shell — yes (staff POS)
  - POS path remains fast under pressure — online P0 unchanged
  - Distinct from store customer PWA offline catalog note — yes

## uiuxpromax gate evidence

**Binary:** may be absent. Skill + `docs/uiux/*` (incl. `pwa-experience.md`) evidence used. Gate: brief in this plan before UI.

**Brief (Persian + RTL, mobile-first merchant staff offline POS banners):**

| Item | Detail |
| --- | --- |
| Surface | بنر وضعیت اتصال/صف آفلاین روی صندوق پرسنل؛ همگام‌سازی پس‌زمینه P1 |
| Persona | صندوقدار `fa-IR` پشت پیشخوان با اینترنت قطعه‌قطعه‌ای کرمان؛ می‌خواهد فروش را از دست ندهد ولی موجودی اشتباه ثبت نشود |
| Mobile | عرض ~390px Android؛ بنر بالای محتوا یا نوار وضعیت؛ دکمه‌ها ≥۴۴px |
| Screen list | صندوق آنلاین · صندوق آفلاین+صف · همگام‌سازی · رد کمبود موجودی برای بررسی · خطای همگام‌سازی |
| States | آنلاین: «صندوق آنلاین»؛ آفلاین: «اتصال قطع است — فروش در صف»؛ صف خالی آفلاین: «آفلاین هستید؛ هنوز فروشی در صف نیست»؛ تعارض: «فروش به‌خاطر کمبود موجودی رد شد — بررسی کنید»؛ همگام: «در حال همگام‌سازی صف…» |
| Component map | نوار وضعیت · شمارندهٔ صف · متن تعارض · بدون UI ویترین مشتری · **هرگز** SW/queue مشترک با ADR-023 |
| RTL / Persian | `dir=rtl` `lang=fa`؛ کپی کامل فارسی پیشخوان |
| Copy samples | «اتصال قطع است — فروش در صف آفلاین ذخیره شد.» · «به‌خاطر کمبود موجودی، فروش برای بررسی نگه داشته شد.» · «این فروش قبلاً ثبت شده است.» |
| Iranian retail | صندوقدار · بارکد · جمع تومان · بدون CRDT/overwrite خاموش |
| A11y | `role="status"` / `aria-live="polite"`؛ پیام تعارض قابل فهم برای کاسب |
| Out of scope | Full camera barcode UI · Mongo sync metrics emit · real IndexedDB browser adapter · store customer PWA · EMQX |

`assertUiuxGate`: `gatePassed` + skill/docs present + brief Persian/RTL/390/retail/states/a11y.

## Database design

- Server: reuse `sales` unique `(merchant_id, idempotency_key)` (ADR-009) — sync keys = Idempotency-Key
- Optional `sale_sync_batches` audit — deferred (document reserved); no new Drizzle table this cycle
- Client IDB: modeled as port + in-memory adapter for tests; browser IDB wiring later in ARD-017

## Domain / application changes

- `src/pos-offline/` — Decision + helpers:
  - Online P0 / offline queue P1 (ADR-091 restatement)
  - Conflict = `reject_and_review` on stock shortage; silent overwrite / silent double charge forbidden
  - Idempotent sync keys required per queued draft
  - Sync API: `POST /api/v1/sales/sync` batch; each item → CompleteSale UoW; success → `SaleCompleted`
  - SW scope staff-only; IDB drafts (SaleDraft client model)
  - Sync failure metric names reserved (warehouse later)
  - Persian shop-floor copy + `assertUiuxGate`
  - `createInMemoryOfflineSaleQueue` — enqueue / list / sync flush / review rejected
- Update `src/staff-pwa` `STAFF_PWA_OFFLINE` → points at ADR-024 package (queue live)
- POS page: wire offline status banner copy from `pos-offline`
- Minimal staff SW stub under `public/sw-staff.js` (precache POS shell note)
- ARD-017: mark SW/IDB/sync/banner checklist items for this slice

## API / UI / events / cache

- API contract: `POST /api/v1/sales/sync` (handler stub reserved; sync helper tested in-memory)
- UI: Persian RTL offline status on `/pos`
- Events: `SaleCompleted` on successful sync item; sync failure metrics reserved
- Cache: client catalog snapshot versioning note; server cache unchanged

## Gates

- Telemetry: sync failure metric names only (N/A warehouse write) — N/A deep analytics
- Persistence: PG reuse sales idempotency; IDB via in-memory port
- uiuxpromax: brief above before banner UI
- No delivery / no store customer PWA conflation
- Prerequisites: ADR-022 completed; ADR-009 CompleteSale + idempotency; ADR-091 reject-and-review

## Test matrix

- Unit: online P0 / offline P1 priorities locked
- Unit: reject-and-review on stock shortage; forbids last_write_wins
- Unit: idempotent sync keys; duplicate key does not double-complete
- Unit: in-memory queue enqueue → sync success → SaleCompleted; shortage → rejected_for_review
- Unit: Persian shop-floor messages (Arabic script)
- Unit: staff-only audience / ≠ store customer SW
- Unit: uiux gate passes; POS page references pos-offline banner copy
- `npm run validate`

## Related ARD checkboxes

- ARD-017: SW / IDB queue model / sync API contract / offline banners; e2e browser queue remains stretch
- ARD-007: CompleteSale idempotency reused (no domain change)

## Iranian feature checklist (in scope)

- Persian UI copy — yes
- Persian errors / conflict — yes
- RTL shell — yes
- Mobile touch — yes (≥44px review CTA)
- تومان — wording in shop-floor copy; amounts IRR minor in drafts
- Jalali — N/A banners
- POS fast path — online P0 preserved
- Pickup / no delivery — unchanged

## Risks

- Full browser IDB/Background Sync not exercised in Node tests — mitigated by port + in-memory adapter
- Route Handler for `/api/v1/sales/sync` deferred — contract + sync helper prove Decision
- SW precache minimal — installability already ADR-022; SW files prove staff scope

## Completion notes

- Implemented as planned; `npm run validate` green (529 tests)
- Iranian feature checklist: Persian offline/online/queue/conflict copy + RTL shell; touch ≥44px review CTA; تومان wording; Jalali N/A on banners
- `src/pos-offline` — online P0 / offline queue P1, reject-and-review stock shortage (ADR-091), idempotent sync keys, `POST /api/v1/sales/sync` contract, `SaleCompleted` on flush, in-memory queue helper, Persian shop-floor messages, staff SW stub
- POS `StaffOfflineStatus` + install-prompt offline copy wired; `staff-pwa` offline points at ADR-024 package
- ARD-017 SW/IDB/sync/banner checkboxes updated; browser IDB e2e + HTTP sync route remain
- Next: ADR-038 EMQX Event Bus / Realtime Architecture
