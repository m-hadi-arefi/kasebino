# Plan — ADR-005 Merchant Domain

## Summary

Implement the **Merchant** tenant aggregate as multi-tenant root: lifecycle `draft → active → suspended`, profile fields (Persian trade name, slug, contact phone), multi-store enabled (ADR-091), domain events `MerchantCreated` / `MerchantActivated` / `MerchantUpdated`, application use cases create/activate/update-settings with Persian error messages, in-memory repository for tests, optional Drizzle OLTP schema stub for `merchants` (+ `merchant_settings`) aligned with ADR-043. Thin contract in `src/merchant-domain/`. No UI / API routes / outbox wiring this cycle.

## Iranian First impact

- Persian text: Yes — user-facing `MerchantDomainError` messages (`messageFa`)
- RTL: UI N/A; merchant admin shells RTL deferred to UI ADRs
- Jalali / تومان: N/A (no date/money display surfaces); defaults encoded on settings stub (`fa-IR`, `Asia/Tehran`)
- Iranian workflows: phone-centric contact on profile; multi-store under one merchant (ADR-091)
- Mobile: N/A runtime UI; domain validates Iranian mobile for contact when provided
- Checklist: Language/errors + phone validation in scope; UI/RTL/Jalali/تومان display N/A

## Schema / API / UI / events / cache

- Persistence: domain `MerchantRepository` port + in-memory impl; Drizzle schema stub `merchants` / `merchant_settings` (no Drizzle Kit migration generate this cycle — ARD-003 remainder)
- API routes: deferred (ARD-003 `/api/v1/merchants*`)
- UI: none — uiuxpromax N/A
- Events: `MerchantCreated`, `MerchantActivated`, `MerchantUpdated` domain shapes (outbox emit later)
- Cache: `merchant:profile` TTL policy noted on contract; invalidation deferred

## Domain / application changes

- `src/merchant-domain/` — lifecycle, multi-store, activation-gates, event names, assert helpers
- `src/modules/merchant/domain/` — aggregate, status VO, events, repository port
- `src/modules/merchant/application/` — create / activate / updateSettings use cases, Persian errors
- `src/modules/merchant/infrastructure/persistence/` — in-memory repository
- `src/infrastructure/database/schema/merchants.ts` — optional Drizzle stub; re-export from schema index
- Update ADR-042 schema emptiness assertion to allow domain tables from domain ADRs

## Gates

- Telemetry: activation funnel noted; no warehouse emit this cycle
- Persistence: schema stub + ports; migrations when ARD-003 acceptance finishes
- uiuxpromax: N/A (no pages)
- No delivery: merchant owns stores; inventory/membership remain store-scoped later (ADR-006+)
- Multi-store: `multiStoreEnabled: true` always for MVP

## Test matrix

- Unit: create merchant → draft + `MerchantCreated`; Persian trade name accepted
- Unit: activate draft → active + `MerchantActivated`; invalid transitions Persian errors
- Unit: update settings/profile → `MerchantUpdated`; slug uniqueness
- Unit: Iranian contact phone normalize / reject with Persian message
- Unit: merchant-domain contract — multi-store, lifecycle, activation gates POS/storefront
- Unit: in-memory repository round-trip
- Unit: drizzle schema exports `merchants` with ADR-043 columns (uuid, timestamptz, Persian text)
- `npm run validate`

## Related ARD checkboxes

- ARD-003 Merchant Management — domain model + events foundations; remain `todo` until API + migration + outbox + cache

## Iranian feature checklist (in scope)

- Error messages Persian: yes
- Phone validation Persian: yes (contact)
- Primary UI / RTL / mobile shell / تومان / Jalali display: N/A — domain/app only
- Multi-store merchant cognition: yes (enabled flag + contract)

## Risks

- Generating Drizzle migration without live DB → schema stub only this cycle
- Suspend/admin path deferred to ADR-013 (status VO supports `suspended` + deny activate)

## Completion notes (2026-08-03)

- Implemented as planned; `npm run validate` green (217 tests)
- Iranian feature checklist: Persian domain errors + Iranian contact phone; UI/RTL/تومان/Jalali N/A (uiuxpromax N/A)
- `src/merchant-domain` contract + `src/modules/merchant` aggregate/use cases/events/in-memory repo
- Drizzle schema stub `merchants` + `merchant_settings` (migrations deferred ARD-003)
- Multi-store enabled (ADR-091); activation gates POS/storefront
- STATUS → `completed`
- Next: ADR-006 Store Domain
