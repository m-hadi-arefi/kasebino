# Plan — ADR-064 Data Retention Strategy

## Summary

Encode MerchantOS **data retention** as an importable contract in `src/data-retention/`: Mongo TTL tables for clickstream / sessions / audit / warehouse (plus matrix siblings), legal-hold purge pause, soft-deleted membership grace longer than analytics streams, and Persian privacy copy keys for Iranian operators. Align ADR-047 purge pointer and per-package TTL stances to this ADR. Live Mongo TTL index create / purge worker jobs remain ARD-019 / ARD-021 packaging.

## Iranian First gate

| Check | Impact |
| --- | --- |
| Persian text? | `RETENTION_PRIVACY_COPY_FA` keys for operators (hold, purge, soft-delete, notice) |
| RTL? | Privacy messaging stubs declare `dir=rtl` / `fa-IR`; UI deferred |
| Jalali? | Retention windows stored as UTC/day counts; Jalali display when ops UI lands |
| تومان? | N/A — no money in retention contract |
| Iranian behavior? | Privacy/compliance messaging for Iranian merchants/ops; soft-delete membership grace |
| Iranian mobile? | N/A for UI this cycle; copy usable on modest ops devices later |

Iranian checklist: UX screens N/A (infra + Persian privacy keys). Persian privacy copy + RTL stubs required.

## Gates

- Telemetry: retention governs analytics/audit TTLs (references ADR-056–062 collections); no new telemetry emit
- Persistence: Mongo TTL index specs + OLTP soft-delete membership grace; no live driver createIndexes this cycle
- uiuxpromax: N/A (no pages; Persian privacy keys/stubs only)
- Delivery: no delivery data classes
- Prerequisites: ADR-056, ADR-058, ADR-041 completed

## Task breakdown

1. STATUS `in_progress`; write this plan
2. `src/data-retention/` — decision, retention matrix, TTL tables (clickstream/sessions/audit/warehouse + siblings), legal hold, membership soft-delete grace longer, Persian privacy copy, purge mechanics notes, asserts, tests
3. Flip data-integrity purge pointer; clickstream/session/audit/warehouse ttl notes → ADR-064; mongodb-analytics placement; architecture doc note
4. ARD-021/022/024/027 foundation notes; docs/ards STATUS
5. Tests + `npm run validate` → STATUS completed → progress-log Next: ADR-088

## Schema / API / UI / events / cache

- Schema: logical Mongo TTL indexes on `occurredAt` / `ingestedAt` / `startedAt`; OLTP soft-delete columns unchanged; no PG TTL on sales
- API: none this cycle (ops admin retention/hold UI → later)
- UI: none (Persian privacy keys ready)
- Events: none required; legal-hold pause is policy, not event stream
- Cache: N/A

## Test matrix

- Matrix matches ADR decision + `data-retention-architecture.md`
- Clickstream/sessions 90–180d; warehouse 24m; audit 24–36m
- Legal hold overrides TTL purge
- Soft delete ≠ analytics delete
- Soft-deleted memberships held longer than analytics streams; hard purge Phase-2 + grace + no hold
- Never TTL PostgreSQL sales via analytics policies
- Persian privacy copy keys present (fa-IR / rtl)
- Env-overridable documented defaults
- Pointer flips + `npm run validate`

## Related ARD checkboxes

- ARD-021 — retention TTL contract foundation; live createIndexes / purge worker remain
- ARD-022 — audit TTL stance → ADR-064; live TTL job remain
- ARD-024 — warehouse TTL stance → ADR-064; live TTL remain
- ARD-027 — clickstream/session TTL → ADR-064; live TTL remain

## Iranian feature checklist (in scope)

- UX copy / RTL screens / mobile / تومان: N/A — privacy keys + stubs only
- Persian privacy messaging keys: required
- Jalali presentation of retention windows: deferred to ops UI

## Risks

- Over-implementing live Mongo TTL jobs / admin hold CRUD → keep contract + unit tests
- Contradicting locked per-package TTL ranges — align to ADR-064 canonical table
- Allowing hard purge of memberships without grace/hold checks — encode assert helpers

## Completion notes

- Implemented `src/data-retention` — retention matrix, Mongo TTL table (clickstream/sessions/audit/warehouse), legal-hold registry, membership soft-delete grace ≥36m (longer than analytics), Persian privacy copy keys + RTL stubs; flipped data-integrity / mongodb-analytics / clickstream / session / audit / warehouse pointers; ARD-021/022/024/027 + architecture notes
- Validate green (653 tests); STATUS → completed; Next: ADR-088
- Iranian checklist: UX screens N/A (uiuxpromax N/A); Persian privacy keys + RTL stubs tested; Jalali ops UI deferred
