# Plan — ADR-029 Backend Clean Architecture Layering

## Summary

Encode Clean Architecture for the modular monolith as a testable contract in `src/backend-layering/`: presentation (App Router Route Handlers / Server Actions / pages) → application use cases → domain ← infrastructure (Drizzle / Mongo / EMQX / MinIO / SMS). Align with ADR-002 `DDD_STRATEGY` (domain forbidden imports, repository ports/adapters) and ADR-016 composition rules. Document request-boundary `correlationId` for later middleware ADRs. No persistence schemas and no UI surfaces in this ADR.

## Iranian First impact

- Persian text: human-readable API / presentation messages must be Persian (or stable codes + Persian client maps); domain ubiquitous language remains English
- RTL: N/A — no new UI; internal JSON keys not localized
- Jalali / تومان: N/A in this ADR (formatting stays presentation-layer convention from ADR-001/002)
- Iranian workflows: AuthZ errors and rate-limit messages user-safe in Persian when presentation surfaces land
- Mobile: payload/latency budgets noted as presentation concern; no payloads shipped here
- Checklist: UX N/A (contract-only); Persian presentation-message strategy encoded in contract

## Database design

- N/A — no schemas; infrastructure role documented (Drizzle repository implementations only when SQL appears later)

## Domain changes

- `src/backend-layering/` — Clean Architecture layers, call rules, forbidden bypasses, AuthZ/TX/analytics responsibilities, `correlationId` request boundary, composition-root note
- Optional thin `src/shared/application` — composition-root documentation slot only (no use cases yet)
- Reuse `DOMAIN_FORBIDDEN_IMPORTS` / repository placement from ADR-002 — do not fork rules

## API / UI / events / cache

- No new HTTP routes or UI
- Events/analytics: use cases emit via ports (contract rule); implementation deferred
- Middleware/`correlationId`: contract-encoded; implementation deferred to middleware / observability ADRs

## Gates

- Telemetry: `correlationId` on request boundary noted; no emit yet
- Persistence: N/A schemas; drizzle-only when SQL appears
- uiuxpromax: N/A (no UI)
- No delivery: no delivery presentation or use cases

## Test matrix

- Unit: dependency direction `presentation → application → domain ← infrastructure`
- Unit: presentation may call application only (not domain mutation / not infra persistence bypass)
- Unit: domain forbidden imports align with `DDD_STRATEGY`
- Unit: repositories — interfaces in domain, implementations in infrastructure
- Unit: application owns TX + AuthZ orchestration; analytics via ports
- Unit: request boundary includes `correlationId` (deferred middleware)
- Unit: Persian presentation-message strategy documented
- `npm run validate`

## Related ARD checkboxes

- Foundation layering partially satisfied; no specific ARD owns ADR-029 alone

## Risks

- Overlap with ADR-002/016 composition rules → assert alignment rather than duplicate conflicting truths
- Fat Route Handlers later → contract + asserts guide reviews until lint architecture plugin exists

## Completion notes (2026-08-03)

- Implemented as planned; `npm run validate` green (62 tests)
- Iranian feature checklist: UX N/A (contract-only); Persian presentation-message strategy + English domain language encoded
- Composition root documented; thin `src/shared/application` slot added
- STATUS → `completed`; Next: ADR-066 Docker Compose
