# Plan — ADR-058 Audit Logging Architecture

## Summary

Encode MerchantOS **audit logging** as insert-only Mongo `mos_audit` via `AuditPort` in `src/audit-logging/`: async-after-commit emit that never blocks OLTP, PII phone scrubbing in before/after/metadata, Persian action labels for human viewers, sensitive-action matrix, in-memory store for tests (official Mongo driver optional later). Align ADR-047/056/065/076 deferrals that pointed here. Admin HTTP browse + UI remain ARD-022 packaging.

## Iranian First gate

| Check | Impact |
| --- | --- |
| Persian text? | Action labels for humans Persian (`AUDIT_ACTION_LABELS_FA`); API keys English; admin titles stubbed Persian |
| RTL? | Viewer shell deferred (uiuxpromax); stubs declare `dir=rtl` / `fa-IR` |
| Jalali? | Store ISO UTC; Jalali display when admin viewer lands |
| تومان? | N/A money SoT; money summaries stay opaque IDs / amounts only if already present and not phone |
| Iranian behavior? | Auth / stock / sale / admin actions audited; phone PII scrubbed (09… / +98…) |
| Iranian mobile? | No UI this cycle; never block POS/OLTP when Mongo down |

Iranian checklist: UX screens N/A (infra + labels). Persian labels + phone scrub + RTL stub required. Admin browser UI → ARD-022 + uiuxpromax later.

## Gates

- Telemetry: this ADR *is* evidence audit (`mos_audit`); metric write failures
- Persistence: Mongo `mos_audit` insert-only (in-memory store this cycle); optional thin PG not required
- uiuxpromax: N/A (no admin pages this cycle; labels/contracts only)
- Delivery: no courier/delivery
- Prerequisites: ADR-056, ADR-034 completed

## Task breakdown

1. STATUS `in_progress`; write this plan
2. `src/audit-logging/` — decision, document schema, sensitive matrix, InMemory store (insert-only), AuditPort (async / fail-open for OLTP), phone PII scrub, Persian labels, search filters, access-itself-audited helper, indexes/TTL stance, metrics
3. Flip deferred pointers: data-integrity, security-architecture, mongodb-analytics placement, event-warehouse audit stream pointer, rate-limiting emit, multi-tenant comment if needed
4. ARD-022 foundation notes (AuditPort + mos_audit; admin API/UI remain)
5. Tests + `npm run validate` → STATUS completed → progress-log Next: ADR-059

## Schema / API / UI / events / cache

- Schema: logical `mos_audit` docs (unique `eventId`); indexes from audit-architecture / ADR-056
- API: reserve `GET /api/v1/admin/audit` (+ `/:eventId`) — implement later
- UI: none this cycle (Persian labels ready)
- Events: `AuditRecordWritten` logical; sensitive matrix documented
- Cache: do not cache audit search

## Test matrix

- Insert-only; no update/delete API
- Duplicate `eventId` idempotent no-op
- Phone scrubbing masks `09…` / `+98…` in before/after/metadata; Persian text preserved
- Persian action labels present for matrix actions
- Query by merchant / actor / action / time; tenant isolation
- Viewing audit records itself via AuditPort (`audit.view`)
- Never throws to OLTP caller when store fails (fail-open + metric)
- Off checkout critical path
- Pointer flips + `npm run validate`

## Related ARD checkboxes

- ARD-022 — AuditPort → `mos_audit` foundation; admin API/UI + live driver + retention job remain
- ARD-021 — audit plane consumer note

## Iranian feature checklist (in scope)

- UX copy / RTL screens / mobile / تومان / Jalali display UI: N/A — labels + stubs only
- Persian action labels: required
- Phone PII scrubbing (Iranian mobiles): required

## Risks

- Over-implementing admin HTTP/UI / hash chain / PG `audit_logs` → keep contract + in-memory
- Confusing warehouse stream `audit` with `mos_audit` — AuditPort writes `mos_audit` only
- Blocking OLTP on Mongo failure — must fail-open like ADR-065

## Completion notes

- Implemented `src/audit-logging` — insert-only `mos_audit` via AuditPort, phone PII scrub, Persian action labels, fail-open OLTP, access-itself-audited, search filters, retention/index stance; in-memory store + tests; flipped data-integrity / security / mongodb-analytics / event-warehouse / rate-limiting / multi-tenant pointers; ARD-021/022 foundation notes
- Sensitive action matrix: auth.*, merchant.*, catalog/stock.*, sale.*, loyalty.*, order/payment.*, admin.*, privacy.*, audit.view, rate_limit.triggered
- Validate green (595 tests); STATUS → completed; Next: ADR-059
- Iranian checklist: UX screens N/A (uiuxpromax N/A); Persian labels + RTL stubs + phone scrub tested; Jalali admin viewer deferred to ARD-022
