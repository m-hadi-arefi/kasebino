# Plan — ADR-018 Frontend Component Architecture

## Summary

Encode Frontend Component Architecture as an importable contract in `src/frontend-components/` and scaffold empty `src/components/` folder conventions (primitives deferred to ADR-019 shadcn; domain/composite shells only). Enforce DDD-friendly UI: layers (primitives → composites → domain), no business logic / DB / domain imports in presentational primitives, RTL-first logical props, Persian string capacity. Prefer contracts + empty structure over heavy UI.

## Iranian First impact

- Persian text: yes — contract requires presentational components to accept `fa` strings without clipping; placeholder `.gitkeep` dirs hold no copy yet
- RTL: yes — logical CSS props mandatory for primitives/composites; physical L/R spine forbidden in component conventions
- Jalali: adapters required for date UI (empty folders only this cycle; no Gregorian-only subcomponents without Iranian adapters)
- تومان: money display via Iranian adapters when domain money UI lands (N/A empty shells)
- Iranian workflows: domain folders reserve POS density + storefront compositions; no delivery UI shell
- Mobile: density variants `pos` (≥44px) vs `analytical`; touch-first documented in contract
- Checklist (architecture UX in scope):
  - Primary UI copy Persian — convention yes; no new screens
  - Root `dir=rtl` / `lang=fa` — unchanged (ADR-016)
  - Logical CSS mandatory — yes (contract asserts)
  - Error/empty/loading Persian — convention documented; N/A no data views
  - Currency/Jalali/phone — N/A until domain UI ADRs

## uiuxpromax gate evidence

**Binary:** `uiuxpromax` CLI not present in environment. Gate satisfied via MerchantOS `uiuxpromax-integration` skill using `docs/uiux/*` as evidence.

**Brief (Persian + RTL):**

| Item | Detail |
| --- | --- |
| Surface | معماری کامپوننت فرانت‌اند — قرارداد لایه‌ها؛ بدون صفحهٔ بصری جدید |
| Screen list | N/A (فقط اسکلت پوشه + قرارداد) |
| States | قرارداد: حالت‌های `loading` / `empty` / `error` با متن فارسی در کامپوننت‌های بعدی |
| Component map | `primitives` (`ui/`) ← ADR-019 شادکن؛ `composites/`؛ `domain/{pos,crm,loyalty,catalog,storefront,ordering,analytics,admin,identity}` هم‌راستا با `component-library.md` |
| RTL / Persian | همهٔ کامپوننت‌های آینده RTL-first؛ props منطقی (`margin-inline` …)؛ تایپوگرافی فارسی بدون برش |
| Density | POS متراکم + هدف لمسی ≥۴۴px؛ آنالیتیکس راحت‌تر |
| A11y | WCAG AA؛ فوکوس قابل‌رؤیت؛ بدون وابستگی صرف به رنگ |
| Motion | بدون انیمیشن در این ADR |
| Out of scope visuals | shadcn primitives (۰۱۹)، Tailwind tokens (۰۲۰)، صفحات واقعی (ADRs بعدی) |

## Database design

- N/A

## Domain / application changes

- `src/frontend-components/` — layers, paths, domain folder ids, presentational rules, RTL/logical props, forbidden imports in presentational layer, density variants, assert helpers, FeatureUsed CTA note (analytics emit via application — not in primitives)
- `src/components/` scaffold:
  - `ui/` — empty (ADR-019 fills shadcn primitives)
  - `composites/` — empty shared compositions
  - `domain/<context>/` — empty domain composition shells
- Sync light docs: `docs/tech/react.md`, `docs/uiux/component-library.md` paths → `src/components/ui` (was `src/shared/ui`)
- Align `src/nextjs-architecture` / module UI glob unchanged (`src/modules/*/ui` remains module-owned compositions)

## API / UI / events / cache

- No runtime UI components / no visual design system
- Analytics: contract notes `FeatureUsed` on key domain CTAs later; no emit this cycle
- Cache: N/A
- Secrets: forbid secrets in client/presentational bundles (assert convention)

## Gates

- Telemetry: N/A emit (CTA convention only)
- Persistence: N/A; forbid DB/ORM in presentational
- uiuxpromax: docs gate + brief above (no binary)
- No delivery: no delivery domain folder
- Prerequisites: ADR-016/017 completed; ADR-019 fills primitives (roadmap order — contract reserves path)

## Test matrix

- Unit: layers = primitives / composites / domain
- Unit: paths under `src/components/{ui,composites,domain}`
- Unit: domain folders include POS/CRM/loyalty/storefront/…; exclude delivery
- Unit: filesystem dirs exist (`.gitkeep`)
- Unit: presentational rules — no business logic, no domain imports in primitives, no Drizzle/DB
- Unit: RTL / logical props mandatory; physical spine helpers throw
- Unit: density variants include `pos` + `analytical`
- `npm run validate`

## Related ARD checkboxes

- Frontend packaging ARDs: component folder contract landed; visual library remains ADR-019+

## Iranian feature checklist (in scope)

- RTL + logical CSS convention — yes
- Persian string capacity — yes
- Other items — N/A until concrete UI components

## Risks

- Path drift vs historical `src/shared/ui` — contract + docs sync to `src/components/ui`
- Over-building UI — stay at empty folders + contract
- ADR text lists ADR-019 as prereq; roadmap/user order is 018 then 019 — reserve empty `ui/` only

## Completion notes

- Implemented as planned (2026-08-03)
- uiuxpromax binary absent — gate via `docs/uiux/*` + Persian+RTL brief in this plan
- `src/frontend-components` contract + empty `src/components/{ui,composites,domain/*}` scaffold
- Synced react/nextjs/shadcn/component-library paths to `src/components/ui`
- `npm run validate` green (380 tests)
- Iranian checklist: RTL/logical/Persian capacity conventions; no visual screens; Jalali/تومان N/A empty shells
- STATUS → `completed`; Next: ADR-019
