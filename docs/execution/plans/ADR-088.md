# Plan — ADR-088 Merchant Dashboard Architecture

## Summary

Encode MerchantOS **merchant dashboard** architecture in `src/merchant-dashboard/`: merchant-only shell with Persian RTL AN overview widget stubs conceptually wired to `src/merchant-oltp-analytics` (AN-01..04 API paths + titles + TTL 60s cache-aside); `DashboardWidgetViewed` reserved; تومان + Jalali/`Asia/Tehran` presentation notes; enhance `(merchant)/dashboard` route stubs. Live HTTP overview handlers / Drizzle projection pages remain ARD-013 / ARD-016.

## Iranian First gate (AGENT 1–6)

- Persian text: yes — dashboard chrome, KPI titles, empty/loading/error/auth stubs fully `fa-IR`
- RTL: yes — merchant shell `dir=rtl` / `lang=fa`; mirrored nav
- Jalali: yes — overview stubs note date ranges in Jalali (`Asia/Tehran`)
- تومان: yes — revenue / money KPI labels use تومان
- Iranian behavior: merchant daily retention pulse; North Star monthly returning customers; pickup-only (no delivery BI)
- Mobile: mobile-first ~390px Android; touch nav ≥44px; tablet-skimable KPIs
- Checklist (stubs + contract): Persian primary copy; RTL shell; empty/auth states; تومان; Jalali note; merchant authZ contract

## uiuxpromax gate evidence

**Binary:** may be absent. Skill + `docs/uiux/*` evidence used. Gate: brief in this plan before UI stubs.

**Brief (Persian + RTL, mobile-first merchant dashboard):**

| Item | Detail |
| --- | --- |
| Surface | داشبورد فروشنده — خانه با ویجت‌های AN-01..04؛ فقط نقش بازرگان (`merchant_staff`) |
| Persona | صاحب مغازه / مدیر محلی `fa-IR`؛ صندوقدار پس از شیفت؛ نگاه سریع به فروش، عضویت، بازگشت مشتری |
| Mobile | عرض ~390px Android؛ کارت‌های KPI لمسی؛ لینک‌ها ≥۴۴px؛ بدون BI دسکتاپ‌محور |
| Screen list | خانهٔ داشبورد (نمای کلی) · ویجت درآمد · ویجت مشتریان · ویجت وفاداری/بازماندگی (stubs روی همان صفحه) |
| States | بدون ورود: «برای مشاهدهٔ داشبورد وارد شوید.»؛ خالی: «هنوز داده‌ای برای نمایش نیست.»؛ بارگذاری/خطا فارسی |
| Component map | هدر مغازه · ناو RTL · کارت‌های KPI (نمای کلی، درآمد، مشتریان، بازماندگی) · بدون کروم مشتری / بدون POS cart |
| RTL / Persian | `dir=rtl` `lang=fa`؛ کپی کامل فارسی؛ بدون delivery |
| Copy samples | «داشبورد فروشگاه» · «نمای کلی» · «مبالغ به تومان» · «بازهٔ تقویم شمسی» · «مشتریان بازمانده ماهانه» |
| Iranian retail | مغازه · صندوقدار · مشتری · حفظ مشتری (نه پیک/کوریر) |
| A11y | عناوین معنایی؛ nav با `aria-label`؛ کارت‌های KPI با `aria-live` برای حالت خالی |
| Out of scope | Live `/api/v1/analytics/*` handlers · chart libraries · CRM-04 deep · Lighthouse production pass · JWT middleware enforce |

`assertUiuxGate`: `gatePassed` + skill/docs present + brief Persian/RTL/390/retail/states/a11y.

## Database design

- N/A new tables — consumes ADR-063 PG projections / counter ports via ARD-016 APIs later.
- Cache: overview cache-aside Redis TTL **60s** (`MERCHANT_OLTP_CACHE` / ADR-053).

## Domain / application changes

- `src/merchant-dashboard/` — Decision + helpers:
  - Merchant shell overview widgets (AN-01..04)
  - Auth required: merchant_staff audience only; refuse customer role
  - Conceptual wiring to `merchant-oltp-analytics` API paths + Persian titles
  - Cache-aside TTL 60s documented
  - Event: `DashboardWidgetViewed` reserved (+ feature key `dashboard.widget_viewed`)
  - Persian copy + تومان + Jalali range hint
  - `assertUiuxGate` evidence export
- Routes under `app/(merchant)/dashboard/`:
  - Home overview stub (fa) with four AN widget cards
- Sync docs: ARD-013 / ARD-016 foundation notes; flip ADR-063 `uiDeferredTo` note where applicable
- ARD-013 remains `todo` until live overview load / Lighthouse / CRM surfaces

## API / UI / events / cache

- No Route Handlers this cycle (ARD-016 APIs remain)
- UI: Persian RTL dashboard overview stubs (auth-required messaging)
- Events: `DashboardWidgetViewed` name reserved (no Mongo emit)
- Cache: cache-aside TTL 60s class documented (reuse merchant-oltp-analytics)

## Gates

- Telemetry: DashboardWidgetViewed / `dashboard.widget_viewed` names only
- Persistence: N/A (read models via ADR-063 / ARD-016)
- uiuxpromax: brief above before UI stubs
- No delivery; never customer portal chrome (ADR-087) / never staff-PWA offline conflation
- Prerequisites: ADR-063, ADR-016, ADR-021 completed

## Test matrix

- Unit: auth required; merchant audience only; rejects customer role
- Unit: AN widget map wires to merchant-oltp-analytics paths/titles
- Unit: cache TTL 60s cache-aside
- Unit: `DashboardWidgetViewed` reserved
- Unit: filesystem dashboard route exists; Persian copy / تومان / Jalali / RTL
- Unit: `assertUiuxGate` / `assertMerchantDashboardUiuxGate` passes
- Unit: no customer storefront dashboard chrome / no delivery
- `npm run validate`

## Related ARD checkboxes

- ARD-013: architecture contract + Persian AN overview route stubs; live overview API load / Lighthouse / CRM-04 remain
- ARD-016: uiuxpromax merchant dashboard widget stubs landed; HTTP APIs / Drizzle Kit / live Redis remain

## Iranian feature checklist (in scope)

- Persian UI copy — yes
- RTL/`lang=fa` — yes
- Empty/loading/auth Persian — yes
- تومان — yes
- Jalali date range note — yes
- Mobile-first / touch KPI cards — yes
- Analytics dashboard labels Persian — yes
- Live overview HTTP / chart polish — deferred (stubs)

## Risks

- Real JWT session gate not enforced in middleware yet — stubs document auth required; wire with ARD-030/033 remainder
- Temptation to share customer `/s/.../dashboard` — forbidden; merchant lives under `(merchant)/dashboard` only
- Over-implementing live analytics Route Handlers / charts → keep stubs + contract

## Completion notes

- Implemented as planned (2026-08-03)
- `src/merchant-dashboard/` — merchant shell decision, AN-01..04 widgets wired to `merchant-oltp-analytics` API paths/titles, auth `merchant_staff` only, cache-aside TTL 60s, `DashboardWidgetViewed` reserved, Persian copy + تومان + Jalali note, `assertUiuxGate` evidence
- Route: `(merchant)/dashboard` Persian RTL AN overview stubs (auth/empty/KPI cards); POS nav link; pickup-only hint
- ARD-013 / ARD-016 STATUS notes updated (live APIs / Lighthouse / charts remain)
- Tests: 9 new; `npm run validate` green (662 tests)
- Iranian checklist: Persian copy; fa/rtl shell; auth/empty stubs; تومان; Jalali range note; mobile KPI cards ≥44px; dashboard analytics labels; live overview HTTP N/A
- STATUS → `completed`; Next: ADR-013 Admin Domain
