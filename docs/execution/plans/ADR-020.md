# Plan — ADR-020 Tailwind Design System Strategy

## Summary

Install Tailwind CSS v4 (Next 15 / PostCSS) as the styling system with CSS-variable design tokens in `app/globals.css`, RTL-friendly logical-property conventions, Persian typography (Vazirmatn), and an importable contract at `src/tailwind-design-system/`. Wire root layout to import globals + font; upgrade `cn` to `clsx` + `tailwind-merge`. Avoid purple-default AI aesthetic; use Iranian retail daylight utility tokens from `docs/uiux/design-system.md`.

## Iranian First impact

- Persian text: yes — Vazirmatn font stack; tokens include `--font-sans` / `--font-display` for Persian; no clipping conventions retained
- RTL: yes — root `dir=rtl` unchanged; contract mandates logical CSS (`ms`/`me`/`ps`/`pe` / `start`/`end`); forbid physical L/R spine
- Jalali: N/A (no date UI this cycle); tokens reserve density modes for later date adapters
- تومان: N/A (no money UI this cycle); no Western currency tokens forced into primitives
- Iranian workflows: POS density (`--density-pos-tap-min: 44px`) vs analytical comfortable density
- Mobile: mobile-first breakpoints; font payload via `next/font` subsetting
- Checklist (design-system UX in scope):
  - Primary UI copy Persian — capacity yes; shell metadata already fa (ADR-016/017)
  - Root `dir=rtl` / `lang=fa` — yes (layout)
  - Logical CSS mandatory — yes (contract + token docs)
  - Error/empty/loading Persian — convention; N/A no new data views
  - Currency/Jalali/phone — N/A until domain UI ADRs

## uiuxpromax gate evidence

**Binary:** `uiuxpromax` CLI not present in environment. Gate satisfied via MerchantOS `uiuxpromax-integration` skill using `docs/uiux/*` as evidence.

**Brief (Persian + RTL):**

| Item | Detail |
| --- | --- |
| Surface | سیستم طراحی Tailwind — توکن‌ها + فونت فارسی؛ بدون صفحهٔ محصولی جدید |
| Screen list | N/A (توکن‌ها / globals / قرارداد؛ اسکلت layout موجود) |
| States | قرارداد: حالت‌های بعدی `loading` / `empty` / `error` با متن فارسی روی توکن‌های سطح / muted |
| Component map | توکن‌ها برای ADR-019 shadcn؛ primitives هنوز CLI؛ فونت Vazirmatn روی body |
| RTL / Persian | RTL-first؛ props منطقی؛ پشتهٔ فونت فارسی؛ بدون Inter/Roboto به‌عنوان تنها فونت |
| Density | POS ≥۴۴px؛ آنالیتیکس راحت‌تر (`--density-*`) |
| A11y | کنتراست WCAG AA روی جفت‌های fg/bg و primary |
| Motion | بدون انیمیشن تزئینی در این ADR |
| Visual direction | خرده‌فروشی روز (`modern retail utility`): فیروزه‌ای عمیق + خاکستری خنک؛ بدون بنفش پیش‌فرض AI |
| Out of scope | ADR-021 فرآیند؛ Button واقعی shadcn؛ صفحات بصری کامل |

## Database design

- N/A

## Domain / application changes

- Dependencies: `tailwindcss`, `@tailwindcss/postcss`, `postcss`, `clsx`, `tailwind-merge` (+ types as needed)
- `postcss.config.mjs` — `@tailwindcss/postcss`
- `tailwind.config.ts` — thin compatibility stub for shadcn `components.json` (CSS-first theme in globals)
- `app/globals.css` — `@import "tailwindcss"` + `:root` / `@theme` tokens per design-system.md
- `app/layout.tsx` — import globals; Vazirmatn via `next/font/google`; apply font variable/class on `html`/`body`
- `src/tailwind-design-system/` — contract (paths, token names, RTL/logical rules, banned aesthetics, density, assert helpers) + tests
- `src/lib/utils.ts` — real `cn` with `clsx` + `tailwind-merge`
- Update `src/shadcn-strategy` deferred flags paths now landed; util packages no longer deferred
- Sync `docs/tech/tailwindcss.md` (+ light shadcn note)

## API / UI / events / cache

- No new product screens
- No analytics emit
- Cache / secrets: N/A

## Gates

- Telemetry: N/A
- Persistence: N/A
- uiuxpromax: docs gate + Persian+RTL brief above (no binary)
- No delivery: unchanged
- Prerequisites: ADR-019 completed

## Test matrix

- Unit: contract exports token names matching design-system.md
- Unit: paths `app/globals.css`, `tailwind.config.ts`, postcss config exist
- Unit: RTL-first + logical props mandatory; banned purple-default aesthetic flag
- Unit: Persian font family Vazirmatn wired
- Unit: density POS tap ≥44px encoded
- Unit: `cn` merges conflicting Tailwind classes via twMerge
- Unit: layout imports globals + fa/rtl retained
- Unit: package.json has tailwindcss / @tailwindcss/postcss
- `npm run validate` + `npm run build`

## Related ARD checkboxes

- ARD-001 / staff PWA chain: Tailwind tokens landed (ADR-020); UI generation process still ADR-021

## Iranian feature checklist (in scope)

- RTL + logical CSS — yes
- Persian typography (Vazirmatn) — yes
- Root fa/rtl shell — yes
- Other items — N/A until concrete UI screens

## Risks

- Tailwind v4 CSS-first vs shadcn expecting config file — mitigate with thin `tailwind.config.ts` stub
- Font payload on Iran mobile — use `next/font` with subset/`display: swap`
- Temptation to generate full shadcn Button set — defer visual components to later ADRs after ADR-021 process

## Completion notes

- Implemented as planned (2026-08-03)
- uiuxpromax binary absent — gate via `docs/uiux/*` + Persian+RTL brief in this plan
- Tailwind v4 + `@tailwindcss/postcss`; tokens in `app/globals.css`; stub `tailwind.config.ts`
- Vazirmatn via `next/font/google` on root layout; `cn` = clsx + tailwind-merge
- Contract `src/tailwind-design-system/`; Iranian retail teal tokens (no purple AI default)
- `npm run validate` green (396 tests); `npm run build` green
- Iranian checklist: RTL/logical + Vazirmatn + fa/rtl shell; Jalali/تومان N/A (no domain money/date UI)
- STATUS → `completed`; Next: ADR-021
