# Plan — ADR-090 Notification Architecture

## Summary

Ship notification architecture: persist in-app notifications off the checkout critical path (outbox consumer), SMS channel ports (transactional/OTP-aware; campaigns later via credits), Persian templates, and OTP log hygiene (never log codes). Module + architecture package; HTTP list/read + full UI center deferred to ARD-014.

## Iranian First gate

| Check | Impact |
| --- | --- |
| Persian text? | All in-app + SMS templates fa-IR; Persian API errors |
| RTL? | Notification center UX contract `dir=rtl` (drawer/center UI → ARD-014 + uiuxpromax) |
| Jalali? | N/A this cycle — no datetime display surfaces |
| تومان? | N/A — no money in notification templates this cycle |
| Iranian behavior? | Pickup-ready + low-stock alerts; SMS length-conscious Persian; Iranian MSISDN on SMS port |
| Iranian mobile? | SMS templates short; never block POS/checkout TX |

Iranian checklist: Persian notification templates + RTL dir on UX contract; full center UI screens N/A (uiuxpromax N/A → ARD-014).

## Gates

- Telemetry: notification-sent / failed metric name stubs; full metrics → ADR-074 / analytics plane
- Persistence: Drizzle `notifications` schema stub (Kit migration → ARD-014); in-memory repo for tests
- uiuxpromax: N/A (ports + templates + outbox; no visual center)
- Delivery: no courier/delivery
- Prerequisites: ADR-036, ADR-038 completed

## Task breakdown

1. Mark STATUS `in_progress`; write this plan
2. `src/notifications-architecture/` — decision (persist in-app, SMS ports, campaigns later, never block TX), channel map, Persian UX contract, OTP log hygiene, outbox placement, API path reserves, metrics stubs
3. `src/modules/notifications/` — Notification entity, repository, in-app + SMS channel ports, Persian templates, use cases (create-from-event, list, mark read), outbox consumer handler (idempotent), mock/console adapters (OTP codes redacted in logs), in-memory repo
4. Drizzle `notifications` schema stub + schema index note; flip `OUTBOX_CONSUMERS.notifications` deferral; sync outbox worker package pointer; ARD-014 foundation note
5. Tests + `npm run validate` → STATUS completed → progress-log Next ADR-014

## Schema / API / UI / events / cache

- Schema: `notifications` stub (`merchant_id`, `user_id` nullable, `read_at`, indexes per ARD-014); migrations → ARD-014
- API: reserve `GET /api/v1/notifications`, `POST /api/v1/notifications/:id/read` (handlers → ARD-014)
- UI: Persian + RTL UX contract only (bell/center → ARD-014 + uiuxpromax)
- Events consumed: `OrderCreated`, `OrderReadyForPickup`, `InventoryLowDetected`, `InventoryDepleted` (+ catalog alias awareness)
- Cache: optional short list TTL reserved; invalidate deferred

## Test matrix

- Decision: persist in-app + SMS ports; campaigns not blocking; never block checkout
- Persian templates contain fa script; SMS length-conscious
- Outbox `notifications` consumer creates in-app row from OrderCreated / InventoryLowDetected / OrderReadyForPickup
- Idempotent on `sourceEventId` (at-least-once)
- SMS channel mock records Persian body; console adapter redacts OTP codes
- List + mark-read tenant scoped; cross-tenant read forbidden
- OTP log hygiene: never log codes
- `npm run validate`

## Related ARD checkboxes

- ARD-014 — foundation: module + architecture + outbox consumer + Persian templates + SMS ports; HTTP/UI/migrations remain

## Iranian feature checklist (in scope)

- Persian notification (in-app/SMS) templates: yes
- RTL dir on UX contract: yes
- SMS OTP log hygiene: yes (never log codes)
- UX screens / تومان / Jalali / phone form: N/A (ports only)

## Risks

- Full MQTT in-app push already via EMRX `notifications` channel (ADR-038); this consumer persists rows — UI subscribe remains ARD-014
- Real SMS provider → ADR-083 Proposed (mock/console only)
- Staff user targeting: store-broadcast `userId=null` until staff roster wiring

## Completion notes

- Implemented `src/notifications-architecture` (decision, channels, Persian RTL UX, OTP log hygiene, API reserves, campaign stub) + `src/modules/notifications` (entity, repo, in-app + SMS ports, Persian templates, list/mark-read, outbox consumer, mock/console adapters with OTP redaction) + Drizzle `notifications` stub
- Flipped `OUTBOX_CONSUMERS.notifications` deferral; outbox worker package pointer; ARD-014 foundation note
- Validate green (556 tests); STATUS → completed; Next: ADR-014
