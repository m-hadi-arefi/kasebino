# Plan — ADR-065 Analytics Ingest Failure Isolation

## Summary

Encode MerchantOS **analytics/audit ingest failure isolation** in `src/analytics-ingest-isolation/`: after OLTP commit, ingest is fire-and-forget via buffer/queue; **CompleteSale / SaleCompleted must succeed when Mongo is down**; retry then drop/DLQ policy; ingest failure metrics. Align ADR-056 / event-driven pointers that deferred isolation here. Warehouse protocol driver remains ADR-057.

## Iranian First gate

| Check | Impact |
| --- | --- |
| Persian text? | No merchant/customer UX this cycle; ops metric labels / future DLQ messages may use Persian stubs for operators |
| RTL? | N/A visual; audit viewers RTL deferred to ADR-058 / ops UI |
| Jalali? | N/A display; enqueue timestamps stay UTC ISO |
| تومان? | N/A; money truth stays PG; isolation does not touch pricing |
| Iranian behavior? | POS checkout on flaky networks must never lose a sale because telemetry/Mongo is down |
| Iranian mobile? | Isolation keeps analytics off critical path → POS remains fast on Iranian Android devices |

Iranian checklist: UX N/A (infra reliability). Preserve fail-open sale path for Iranian cashiers.

## Gates

- Telemetry: this ADR *is* the ingest failure-isolation gate; emit/deliver still buffered until ADR-057 sink
- Persistence: no new PG/Mongo schema; buffer is in-process queue (domain events still prefer PG outbox)
- uiuxpromax: N/A (no UI)
- Delivery: no courier/delivery
- Prerequisites: ADR-056, ADR-035, ADR-009 completed

## Task breakdown

1. STATUS `in_progress`; write this plan
2. `src/analytics-ingest-isolation/` — decision, fire-and-forget/buffer queue, drop/retry policy, metrics, isolating `AnalyticsIngestPort`, CompleteSale after-commit helper, asserts
3. Flip ADR-056 `failureIsolationDefer065` → implemented package; event-driven `isolationDeferredTo` → packaged
4. Optional CompleteSale `analyticsAfterSale` port (fail-open after persist)
5. ARD-021 checkbox note: OLTP success when Mongo down
6. Tests + `npm run validate` → STATUS completed → progress-log Next: ADR-057

## Schema / API / UI / events / cache

- Schema: none (queue in-memory; durable domain mirror remains outbox → ADR-057)
- API: track ingest may accept + buffer (202 semantics documented); no HTTP route this cycle
- UI: none (ops DLQ dashboards → future)
- Events: SaleCompleted enqueue after OLTP; never sync Mongo in CompleteSale TX
- Cache: N/A

## Test matrix

- CompleteSale / SaleCompleted succeeds when Mongo sink throws / unavailable
- Enqueue never throws; buffer depth + metrics increment
- Retry then dead-letter / drop after max attempts
- Sync Mongo write on critical path forbidden (assert)
- Align retry backoff with event-driven delivery guarantees where applicable
- ADR-056 / event-driven pointers flip
- Iranian: UX N/A; POS sacred for Iranian checkout
- `npm run validate`

## Related ARD checkboxes

- ARD-021 — OLTP success when Mongo down (buffer/retry); POS complete unaffected when Mongo stopped

## Iranian feature checklist (in scope)

- UX copy / RTL / mobile / تومان / Jalali display: N/A — reliability contract only
- Iranian POS behavior: sale path never blocked by analytics plane

## Risks

- Over-implementing warehouse Mongo driver / index bootstrap → defer ADR-057
- Confusing track best-effort drop with outbox at-least-once — document dual policy
- Awaiting sink inside CompleteSale by mistake — asserts + fail-open port

## Completion notes

- Implemented `src/analytics-ingest-isolation` — fire-and-forget buffer/queue, retry→dead_letter/dropped policy (aligned with ADR-036 delivery guarantees), ingest failure metrics, isolating `AnalyticsIngestPort`, `createAnalyticsAfterSalePort`; CompleteSale optional fail-open after OLTP persist; ADR-056 / event-driven / pos-sales pointers flipped; ARD-021 OLTP-when-Mongo-down + POS-unaffected checkboxes
- Validate green (581 tests); STATUS → completed; Next: ADR-057
- Iranian checklist: UX N/A (reliability contract); Persian ops stubs for future DLQ; POS sacred for Iranian cashiers
