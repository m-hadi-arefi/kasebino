# Plan — ADR-061 Session Analytics

## Summary

Encode MerchantOS **session analytics** in `src/session-analytics/`: client-generated UUID `sessionId`, heartbeat with **30-minute idle timeout**, Session* lifecycle events (`SessionStarted` / `SessionHeartbeat` / `SessionEnded`), session aggregates with **duration** + **device class** into Mongo `mos_sessions`. Best-effort ingest via ADR-065. Iran timezone notes: store UTC ISO; merchant-facing buckets Jalali/`Asia/Tehran`. In-memory store for tests; HTTP session API / client SDK wiring remain ARD-027 packaging.

## Iranian First gate

| Check | Impact |
| --- | --- |
| Persian text? | Metric labels Persian (`SESSION_METRIC_LABELS_FA`); event codes English |
| RTL? | Dashboard viewer deferred (uiuxpromax); stubs declare `dir=rtl` / `fa-IR` |
| Jalali? | Store UTC ISO; Jalali/`Asia/Tehran` buckets when merchant UX lands |
| تومان? | N/A — no money in session props |
| Iranian behavior? | Engagement KPIs for POS/storefront; pickup-only (no delivery sessions) |
| Iranian mobile? | Device class mobile/desktop/tablet; idle timeout suits intermittent Iranian mobile; never blocks POS |

Iranian checklist: UX screens N/A (infra + labels). Persian metric names + RTL stubs + Iran TZ notes required. Session KPI dashboards → ARD-027 / uiuxpromax later.

## Gates

- Telemetry: this ADR *is* session analytics (`mos_sessions` + Session* events); track ingest via ADR-065
- Persistence: Mongo `mos_sessions` session aggregates (+ relation to ADR-056 behavior plane); TTL alignment 90–180d; in-memory this cycle
- uiuxpromax: N/A (no dashboard pages this cycle; Persian labels/contracts only)
- Delivery: no courier/delivery session types
- Prerequisites: ADR-060 completed

## Task breakdown

1. STATUS `in_progress`; write this plan
2. `src/session-analytics/` — decision, Session* catalog, idle timeout (30m), device class, SessionDocument (`sessionId`, duration, paths, eventCount), InMemory store, start/heartbeat/end via ADR-065, Persian metric labels, Iran TZ notes, API reserve, metrics, assertions
3. Flip mongodb-analytics / analytics-boundaries / event-warehouse / clickstream session-deferral pointers
4. ARD-027 foundation notes; ARD-021 progress note; docs/ards STATUS
5. Tests + `npm run validate` → STATUS completed → progress-log Next: ADR-063

## Schema / API / UI / events / cache

- Schema: logical `mos_sessions` docs (unique `sessionId` for aggregate; Session* events idempotent by `eventId`); indexes `sessionId`, `merchantId+startedAt`, `status+lastHeartbeatAt`
- API: reserve `POST /api/v1/analytics/session` (start/heartbeat/end); HTTP wire → ARD-027
- UI: none this cycle (Persian metric names ready)
- Events: `SessionStarted`, `SessionHeartbeat`, `SessionEnded`; identity stitch (`IdentityMerged`) deferred
- Cache: client may hold sessionId in storage for multi-tab — server Redis N/A

## Test matrix

- Collection is `mos_sessions`; not OLTP SoT; related to ADR-056 behavior plane
- Client UUID sessionId required; duration derived from startedAt→endedAt/last activity
- Device class classified (mobile/desktop/tablet/unknown)
- Heartbeat extends activity; 30m idle → timed_out
- Multi-tab: same sessionId continues; new UUID = new session
- `trackSession` / start/heartbeat/end accept via ADR-065 buffer; never throws when Mongo/sink down
- Merchant-scoped require merchantId; store-scoped when storefront
- Off checkout critical path
- Iran TZ notes + Persian labels
- Pointer flips + `npm run validate`

## Related ARD checkboxes

- ARD-027 — session start/heartbeat/end foundation; HTTP session API + client SDK + live driver remain
- ARD-021 — session plane consumer note

## Iranian feature checklist (in scope)

- UX copy / RTL screens / mobile / تومان display UI: N/A — labels + stubs only
- Persian metric labels: required
- Jalali/`Asia/Tehran` merchant-facing time notes: required (presentation deferred to ARD-027 UI)

## Risks

- Over-implementing HTTP session Route Handlers / client SDK → keep contract + in-memory + isolated track
- Conflicting with locked ADR-056 `mos_*` list — `mos_sessions` is dedicated session-aggregate collection (parallel to `mos_product_rollups`); clickstream path events stay `mos_behavior`
- Blocking OLTP/POS on Mongo failure — must use ADR-065 best_effort_track path
- Treating server-only sessions as source of truth — client UUID is authoritative id

## Completion notes

- Implemented `src/session-analytics` — client UUID sessionId, SessionStarted/SessionHeartbeat/SessionEnded, 30m idle timeout, duration + device class → `mos_sessions`, Iran TZ notes (UTC / Jalali Asia/Tehran), Persian metric labels, `trackSession` via ADR-065; in-memory store + tests; flipped mongodb-analytics / analytics-boundaries / event-warehouse / clickstream pointers; ARD-021/027 foundation notes
- Validate green (629 tests); STATUS → completed; Next: ADR-063
- Iranian checklist: UX screens N/A (uiuxpromax N/A); Persian metric labels + RTL stubs + Iran TZ notes tested; Jalali session KPI viewer deferred to ARD-027
