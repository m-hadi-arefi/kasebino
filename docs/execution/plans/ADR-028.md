# Plan — ADR-028 Frontend Error Handling UX

## Summary

Encode MerchantOS frontend error UX at `src/frontend-error-ux/`: typed error envelope → Persian UI mapping; toast / inline / boundary presentation patterns; never expose English stacks or OTP secrets to users; barcode-miss recovery path; optional `correlationId` for support; lightweight React `ErrorBoundary` (createElement — contracts `.ts`-only). Optimistic UI allowed only where marked safe.

## Iranian First impact

- Persian text: yes — all user-visible error/toast/boundary copy `fa-IR`; map API/domain codes → plain-language Persian
- RTL: yes — error presentation contract `dir=rtl` / `lang=fa`; inline field errors + icons logical/RTL-aware
- Jalali: N/A this cycle (no date-error UI)
- تومان: N/A (no money formatting in error surfaces this ADR; money validation stays ADR-027)
- Iranian workflows: phone/OTP-safe messages (never leak OTP); barcode miss recovery for POS صندوقدار
- Mobile: inline errors `placement: above_keyboard`; toast brief/readable on ~390px Android
- Checklist (capacity + ErrorBoundary; no full product screens):
  - Persian error messages — yes
  - RTL/`lang=fa` error contract — yes
  - No English stack / jargon in UX — yes
  - OTP-safe — yes
  - Root fa/rtl shell — unchanged
  - New product pages — N/A

## uiuxpromax gate evidence

**Binary:** may be absent. Skill + `docs/uiux/*` evidence used. Gate: brief in this plan before UI capacity code.

**Brief (Persian + RTL):**

| Item | Detail |
| --- | --- |
| Surface | قرارداد خطای فرانت — toast / پیام درون‌فرم / مرز خطا؛ بدون صفحهٔ محصولی کامل |
| Persona | صندوقدار مغازه / مشتری؛ `fa-IR` |
| Mobile | عرض ~390px Android؛ پیام کوتاه؛ هدف لمسی ≥۴۴px برای «تلاش مجدد» |
| Screen list | N/A صفحات کامل؛ الگوهای: toast سراسری، inline فیلد، boundary بخش/صفحه |
| States | خطا: Toast کوتاه فارسی؛ inline زیر/بالای فیلد؛ boundary با دکمهٔ «تلاش مجدد»؛ بدون لودینگ/خالی جدید مگر ارجاع به ADR-025 |
| Component map | `mapApiCodeToUi` · `toUiError` · `selectPresentation` · `sanitizeUserErrorMessage` · `ErrorBoundary` · `FRONTEND_ERROR_UX` |
| RTL / Persian | `dir=rtl` `lang=fa`؛ آیکن خطا با logical inset؛ کپی fa بدون اصطلاح انگلیسی |
| Copy samples | «کالا با این بارکد یافت نشد.» · «لطفاً دوباره تلاش کنید.» · «کد تأیید را دوباره درخواست کنید.» (بدون افشای OTP) |
| A11y | `role=alert` / `aria-live=assertive` برای toast و boundary؛ رنگ تنها شاخص نیست |
| Out of scope | صفحات کامل POS؛ کتابخانه toast بصری shadcn؛ offline staff banners (Future Evolution) |

`assertUiuxGate` conceptual: `gatePassed` + skill/docs present + brief Persian/RTL/390/retail/states/a11y.

## Database design

- N/A

## Domain / application changes

- `src/frontend-error-ux/` — strategy + helpers:
  - Decision: typed envelope → Persian UI; toast/inline/boundary; no `alert()` spam; optimistic only when safe
  - Map ADR-030 `API_ERROR_CODES` (+ UI domain codes: network, barcode miss) → Persian copy / presentation mode
  - Sanitize: strip stack traces, English jargon, OTP-like secrets from user messages
  - Optional support hint with `correlationId` (collapsible / secondary — not primary copy)
  - Barcode miss recovery: re-scan / search-by-name CTA Persian
  - `ErrorBoundary` via `createElement` (no JSX; contracts `.ts`-only)
- Sync `docs/rules/ui-rules.md` lightly
- ARD-001 / ARD-007 notes: error UX capacity landed

## API / UI / events / cache

- Consumes ADR-030 envelope shape (`code`, `message`, `correlationId`)
- No new API routes / analytics warehouse (sampling noted as capacity flag only)
- Cache: N/A

## Gates

- Telemetry: error events sampled (contract flag only; no Mongo writer)
- Persistence: N/A
- uiuxpromax: brief above before ErrorBoundary
- No delivery: unchanged
- Prerequisites: ADR-018, ADR-021 completed

## Test matrix

- Unit: decision locks toast/inline/boundary + no alert spam + optimistic-safe-only
- Unit: API codes map to Persian UI messages (Arabic script; no EN UX jargon)
- Unit: barcode miss → inline/toast + recovery CTAs Persian
- Unit: sanitize strips stack / OTP digits patterns / English Exception text
- Unit: neverReturnOtp / no stack in user-facing presentation
- Unit: ErrorBoundary renders Persian fallback + retry; swallows child throw
- Unit: RTL/`lang=fa` contract; correlationId optional support only
- `npm run validate`

## Related ARD checkboxes

- ARD-001: note frontend-error-ux landed
- ARD-007: POS barcode miss recovery capacity note

## Iranian feature checklist (in scope)

- Persian error messages — yes
- RTL/`lang=fa` on error contract — yes
- Mobile above-keyboard inline — yes (contract)
- OTP-safe auth messages — yes
- Root fa/rtl shell — already present; unchanged
- Jalali / تومان / full screens — N/A

## Completion notes

- Implemented as planned (2026-08-03)
- `src/frontend-error-ux/` — typed UiError; API/domain code → Persian map; toast/inline/boundary selection; sanitize stacks/OTP; barcode-miss + OTP recovery CTAs; optimistic allowlist; `MerchantErrorBoundary` (createElement); uiux gate evidence
- Synced `docs/rules/ui-rules.md`; ARD-001/007 STATUS notes
- Tests: 8 new; `npm run validate` green (433 tests)
- Iranian checklist: Persian error copy; fa/rtl contract; OTP-safe; above-keyboard inline; no English stacks; Jalali/تومان/full screens N/A
- STATUS → `completed`; Next: ADR-086 Storefront Architecture
