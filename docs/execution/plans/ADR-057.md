# Plan — ADR-057 Event Warehouse Architecture

## Summary

Encode MerchantOS **event warehouse** as an append-only Mongo `mos_events` mirror of domain events in `src/event-warehouse/`: outbox `mongodb_warehouse` consumer, idempotent by `eventId`, tenant fields (`merchantId` / `storeId`), lag metrics, TTL 24 months retention stance, Persian UTF-8 payload safety. In-memory store for tests (official Mongo driver remains optional later). Align ADR-035/036/014/056/065 deferrals that pointed here.

## Iranian First gate

| Check | Impact |
| --- | --- |
| Persian text? | No merchant/customer UX this cycle; event codes English; dashboard titles/exports Persian later; preserve UTF-8 Persian in payloads |
| RTL? | N/A visual this cycle; chart/filter RTL deferred to dashboard ADRs |
| Jalali? | Ingest stores ISO UTC; merchant-facing buckets remain Jalali/`Asia/Tehran` (ADR-014 presentation) |
| تومان? | N/A; money truth stays PG; warehouse never money SoT |
| Iranian behavior? | Admin browse only; investigation without scanning PG sales; POS/checkout stays off path |
| Iranian mobile? | N/A screens; mirror stays off checkout critical path |

Iranian checklist: UX N/A (infra mirror). UTF-8 Persian payload preservation mandatory. Admin investigation API/UI Persian+Jalali → later.

## Gates

- Telemetry: this ADR *is* the domain event warehouse mirror + outbox bridge
- Persistence: Mongo `mos_events` append-only (in-memory store this cycle); PG outbox remains publish SoT
- uiuxpromax: N/A (no UI)
- Delivery: no courier/delivery
- Prerequisites: ADR-056, ADR-035, ADR-036 completed

## Task breakdown

1. STATUS `in_progress`; write this plan
2. `src/event-warehouse/` — decision, warehouse document (stream `domain`), InMemory store (idempotent eventId, append-only), outbox handler, lag metrics, tenant filter asserts, TTL 24m stance, Persian UTF-8 asserts, catalog mapping
3. Flip deferred pointers: event-driven `mongodb_warehouse`, outbox, analytics-boundaries, mongodb-analytics, analytics-ingest-isolation, event-naming
4. ARD-024 foundation notes (idempotent mirror + lag; admin browse API/UI remain)
5. Tests + `npm run validate` → STATUS completed → progress-log Next: ADR-058

## Schema / API / UI / events / cache

- Schema: logical `mos_events` docs (unique `eventId`); indexes principles from ADR-056; no live Mongo driver/index bootstrap required this cycle
- API: reserve admin browse `GET /api/v1/admin/warehouse/events` (implement later)
- UI: none
- Events: full MVP domain catalog mirrored via outbox consumer
- Cache: N/A

## Test matrix

- Append-only insert; duplicate `eventId` is no-op (idempotent)
- Outbox `mongodb_warehouse` handler writes tenant fields + envelope + `ingestedAt`
- Persian UTF-8 in payload round-trips without corruption
- Merchant queries require `merchantId` filter; admin browse gate noted
- Lag metric increments (occurredAt → ingestedAt)
- Mapping covers catalog domain events → `mos_events` / stream `domain`
- Off checkout critical path; Mongo down does not block OLTP (outbox retry)
- Pointer flips + `npm run validate`

## Related ARD checkboxes

- ARD-024 — idempotent outbox→`mos_events` mirror + lag metrics foundation; admin API/UI + driver remain
- ARD-021 — outbox warehouse bridge foundation note

## Iranian feature checklist (in scope)

- UX copy / RTL / mobile / تومان / Jalali display UI: N/A — warehouse mirror only
- UTF-8 Persian in payloads: tested
- Merchant Jalali reports: remains ADR-014 / dashboard ADRs

## Risks

- Over-implementing official `mongodb` driver / TTL index bootstrap / admin UI → keep in-memory + contract
- Mutating warehouse docs (updates) — forbid; append-only + idempotent insert only
- Confusing product/audit streams — this ADR mirrors domain stream to `mos_events` only

## Completion notes

- Implemented `src/event-warehouse` — append-only `mos_events` domain mirror, idempotent by `eventId`, outbox `mongodb_warehouse` consumer, tenant fields, lag metrics, TTL 24m stance, Persian UTF-8 payload preservation; in-memory store + tests; flipped event-driven / outbox / analytics-boundaries / mongodb-analytics / analytics-ingest-isolation / event-naming pointers; ARD-021/024 foundation notes
- Validate green (588 tests); STATUS → completed; Next: ADR-058
- Iranian checklist: UX N/A (warehouse mirror); UTF-8 Persian payload round-trip tested; Jalali/`Asia/Tehran` presentation remains ADR-014 / dashboards
