# Plan — ADR-069 CI/CD Strategy

## Summary

Automate MerchantOS quality gates via GitHub Actions CI on pull requests (`npm run validate`) and encode the binding CI/CD strategy in `src/cicd-strategy/`: required PR gates (lint, typecheck, test, build), Drizzle migration review step, no secret leakage in logs, no skippable hooks, and CD promotion staging → prod with human approvals (deploy mechanics deferred to ADR-070).

## Iranian First impact

- Persian text: N/A for merchant/customer UX — ops CI docs/keys English; no merchant-visible pipeline status UI this cycle
- RTL / Jalali / تومان: N/A
- Iranian workflows: pipeline must not regress mobile asset path (build remains Next standalone; no heavyweight CI artifact bloat)
- Iranian hosting/latency: noted for later ops ADRs; this ADR locks quality automation only
- Checklist: UX N/A (infra/contract-only)

## Schema / API / UI / events / cache

- No OLTP schema / UI / events / cache
- uiuxpromax: N/A
- Telemetry: N/A
- Persistence: N/A (migration *review* gate is process/CI checklist, not a new migrate job — ADR-070)

## Domain / files

- `.github/workflows/ci.yml` — on `pull_request` (+ `push` to `main`): checkout, Node 20, `npm ci`, `npm run validate`, `npm run build`; no secrets echoed
- `src/cicd-strategy/` — gate order, workflow path, CD promotion rules (staging→prod + approvals), no-skip-hooks, secret-log prohibition, migration-review requirements, assert helpers
- Sync `src/testing-strategy` — point CI workflow at shipped YAML (no longer deferred)
- Light sync: `docs/architecture/11-deployment-architecture.md` release-gate pointer; ARD-019 CI deploy scaffolding partial note

## Gates

- Telemetry: N/A
- Persistence: N/A
- uiuxpromax: N/A
- No delivery: N/A

## Test matrix

- Unit: CI gates include lint, typecheck, test, build (+ migration review policy)
- Unit: workflow file exists and runs `validate` on pull_request
- Unit: build gate references `npm run build`
- Unit: CD requires staging before prod + approval
- Unit: no skip hooks / no secrets in CI logs policies assertable
- Unit: testing-strategy CI workflow pointer resolved to ADR-069 artifact
- `npm run validate`

## Related ARD checkboxes

- ARD-019: CI quality-gate scaffolding (deploy CD remains open → ADR-070)
- ARD-020: partial DoD automation via CI gates (Lighthouse CI later)

## Iranian feature checklist (in scope)

- UI copy / RTL / mobile UX / تومان / Jalali: N/A — no merchant UI
- Mobile asset performance: build gate keeps Next standalone contract from ADR-067

## Risks

- Full `npm run build` in local validate may be slow — keep `validate` as typecheck+lint+test; run build as separate CI job step (still binding gate in contract)
- CD workflow YAML deferred to ADR-070 — only encode promotion policy now
- Secret scan tooling (gitleaks) optional later; contract + workflow discipline sufficient this cycle

## Completion notes

- Implemented as planned; `npm run validate` green (694 tests)
- Iranian feature checklist: UX N/A (infra/CI-only); no merchant status UI
- `.github/workflows/ci.yml` on PR/main — `npm ci` → `validate` → `build` + migration-review reminder; `src/cicd-strategy` (gates, CD staging→prod approvals, no skip hooks, no secrets in logs)
- `src/testing-strategy` CI workflow pointer resolved to ADR-069 artifact
- ARD-019 CI quality-gate scaffolding checked; CD deploy scaffolding remains ADR-070
- Next: ADR-070 Deployment and Zero-Downtime Strategy
