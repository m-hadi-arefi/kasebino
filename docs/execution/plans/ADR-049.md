# Plan — ADR-049 Inventory Synchronization Strategy

## Summary

Implement event-driven inventory sync: `src/inventory-sync/` decision contract (CompleteSale same-TX decrement; pickup hard-decrement on `paid` with idempotent `preparing`; optimistic `stock_items.version`; Inventory* cache/realtime invalidation; offline stock shortage = reject-and-review per ADR-091; store-scoped). Wire inventory application hooks (`decrementForSale`, `decrementForPickupPaid`, optimistic update) + InventoryChanged/Low/OutOfStock event factories + tests. No UI this cycle.

## Iranian First gate

| Check | Result |
| --- | --- |
| Persian text? | Yes — shop-floor Persian errors for insufficient stock / version conflict / offline reject |
| RTL? | N/A — no UI (uiuxpromax deferred; presentation still RTL later) |
| Jalali? | N/A — no date display |
| تومان? | N/A — quantity sync only |
| Iranian behavior? | POS rush + pickup prevent oversell; offline reject-and-review matches ADR-091 / Iranian staff review |
| Iranian mobile? | Query budgets protect POS/stock write latency; store-scoped isolation for multi-store |

## Task breakdown

1. `src/inventory-sync/` — sale TX decrement, pickup paid decrement policy, optimistic lock, offline reject-and-review, cache/realtime invalidation, Persian messages, assert helpers
2. Inventory module — InventoryChanged/Low/OutOfStock factories; `updateWithOptimisticLock` on repo; `decrementForSale` / `decrementForPickupPaid` (and restore hooks doc); emit Inventory* + StockAdjusted
3. Point `inventory-domain` sync pointer from deferred → ADR-049 implemented
4. Tests: sale decrement store-scoped; pickup paid idempotent; version conflict; insufficient stock Persian; offline policy contract; threshold events
5. Validate → STATUS completed → progress-log Next ADR-007
6. Sync ARD-006 note (sync foundations); ARD-007/034 remain todo for POS/order wiring

## Schema / API / UI / events / cache

- Persistence: `stock_items.version` already on schema stub; optimistic `WHERE version = expected`
- API: deferred ARD-006/007/034
- UI: none — uiuxpromax N/A
- Events: InventoryChanged (primary sync), InventoryLow, InventoryOutOfStock; StockAdjusted retained for manual adjust
- Cache: invalidate stock key on Inventory* (TTL 300s); realtime subscribers consume InventoryChanged

## Test matrix

- `decrementForSale` decrements store-scoped qty inside sync contract; tenant store isolation
- `decrementForPickupPaid` decrements once; second call with same order line key is idempotent / no double sell
- Optimistic version mismatch → VERSION_CONFLICT Persian error
- Insufficient stock → INSUFFICIENT_STOCK Persian
- Reorder threshold → InventoryLow; zero → InventoryOutOfStock
- Offline conflict policy = reject_and_review + idempotent sync keys
- Pickup decrementOn = `paid` documented
- `npm run validate`

## Related ARD checkboxes

- ARD-006 — sync foundations; remain `todo` until API/migrations/UI
- ARD-007 / ARD-034 — CompleteSale / OrderPaid wiring deferred

## Iranian feature checklist (in scope)

- Persian shop-floor sync/conflict errors: yes
- Primary UI / RTL / mobile shell / Jalali / تومان: N/A — strategy + application only

## Completion notes

- Implemented as planned; `npm run validate` green (278 tests)
- Iranian feature checklist: Persian shop-floor sync/conflict errors; UI/RTL/Jalali/تومان N/A (uiuxpromax N/A)
- `src/inventory-sync` contract: CompleteSale same-TX decrement; pickup hard-decrement on `paid`; optimistic `version`; offline reject-and-review; Inventory* cache invalidation
- Inventory hooks: `decrementForSale`, `decrementForPickupPaid`, `restorePickupStock`, `rejectOfflineStockConflict`; InventoryChanged / InventoryLowDetected / InventoryDepleted
- ARD-006/007/034 remain todo for API + CompleteSale/OrderPaid wiring
- STATUS → `completed`
- Next: ADR-007 Customer Membership Model

## Gates

- Telemetry: Inventory* + out-of-stock tracking stubs; warehouse emit later
- Persistence: version column already present; Drizzle optimistic update → ARD migrations/adapters
- uiuxpromax: N/A
- No delivery: pickup-only stock path
