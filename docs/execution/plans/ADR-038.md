# Plan — ADR-038 EMQX Event Bus / Realtime Architecture

## Summary

Ship EMQX MQTT realtime fan-out as an importable contract in `src/emqx-realtime/`: tenant-scoped topic builders, event→topic map, QoS1 publish port, ACL model, short-lived client credential mint (contract), outbox `emqx_realtime` publisher handler, and an in-memory broker for tests. Compose already provides `emqx` (ADR-066) — verify, do not re-implement. Never block checkout on MQTT publish (outbox-only after commit). MQTT browser client + poll fallback → ADR-039.

## Iranian First gate

| Check | Impact |
| --- | --- |
| Persian text? | Realtime toast/offline copy reused from ADR-036 `EVENT_UX_FA` (fa-IR); wire schemas English |
| RTL? | Notification drawer `dir=rtl` contracted; no drawer UI this cycle |
| Jalali? | N/A — no datetime display surfaces |
| تومان? | N/A — no money display |
| Iranian behavior? | POS/pickup events fan-out for counter wait perception; publish never stalls checkout |
| Iranian mobile? | Battery-friendly: QoS1 + outbox poll; MQTT client battery strategy → ADR-039 |

Iranian checklist: UX screens N/A; Persian realtime copy stubs + RTL drawer note in scope.

## Gates

- Telemetry: disconnect-metrics hooks/constants only; full metrics → ADR-074
- Persistence: uses existing `outbox_events`; no new OLTP tables
- uiuxpromax: N/A (no UI)
- Delivery: no courier/delivery topics or workflows
- Prerequisites: ADR-036 completed; outbox worker ADR-035 completed

## Task breakdown

1. `src/emqx-realtime/` — engine decision, topic builders (merchant + admin), event→channel map, QoS1, ACL helpers, short-lived cred mint, `EmqxPublishPort`, `InMemoryMqttBroker`, outbox publish handler, Persian UX stubs, never-block-checkout asserts
2. Thin stub `src/infrastructure/emqx/client.ts` — resolve `MQTT_URL` (no import-time network)
3. Flip ADR-036/035 deferred pointers → `src/emqx-realtime`
4. Verify compose `emqx` + `.env.example` MQTT/EMQX keys
5. Tests + validate → STATUS completed → progress-log Next ADR-039
6. ARD-015 foundation note (token HTTP route + client hook remain for ADR-039)

## Schema / API / UI / events / cache

- Schema: none new
- API: token path reserved (`POST /api/v1/realtime/token`) — mint helper in package; Route Handler → ADR-039/ARD-015
- UI: none (client subscribe → ADR-039)
- Events: outbox consumer publishes lean envelope payloads to MQTT topics
- Cache: N/A

## Test matrix

- Compose ships `emqx` (1883/8083/18083, health, `MQTT_URL`)
- `.env.example` documents MQTT/EMQX keys
- Topics include `merchantId` (tenant isolation); cross-tenant subscribe denied
- Event map: SaleCompleted→sales, OrderCreated→orders, InventoryChanged→inventory, PointsEarned→loyalty, MerchantCreated→admin/merchants
- In-memory publish → subscriber receives envelope; QoS1 contracted
- Outbox handler publishes after enqueue/dispatch; CompleteSale sync-publish forbidden
- Short-lived client creds expire; subscribe ACL merchant-scoped
- Persian `EVENT_UX_FA` / package UX stubs contain fa script + `dir=rtl`
- `npm run validate`

## Related ARD checkboxes

- ARD-015 — EMQX publisher + topics + ACL + cred mint contracted; token HTTP + MQTT client + poll fallback remain (ADR-039)
- ARD-001 — EMQX client stub beyond compose service

## Iranian feature checklist (in scope)

- UX copy / RTL screens / mobile shell / تومان / Jalali: N/A — no user-facing screens
- Persian notification/realtime copy stubs: yes (`EVENT_UX_FA` + package UX)
- POS critical path not blocked by publish: yes

## Risks

- Real mqtt.js protocol client deferred (port + in-memory + env stub only)
- ACL enforcement at EMQX broker config remains ops (contract + mint ACL filters documented)
- Multi-topic fan-out for dual admin+merchant events: primary topic map MVP; expand later

## Completion notes (2026-08-03)

- Implemented `src/emqx-realtime` (topics, ACL, QoS1 port, in-memory broker, outbox handler, short-lived cred mint) + `src/infrastructure/emqx/client.ts`
- Compose `emqx` verified; ADR-035/036 pointers flipped to package
- Validate green (540 tests); STATUS → completed; Next: ADR-039
