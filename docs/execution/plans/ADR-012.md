# Plan — ADR-012 Payment Domain

## Summary

Implement Payments bounded context: **PaymentIntent** aggregate, **PaymentGateway** port + sandbox/mock adapter (ADR-084 remains Proposed — no real PSP), webhook signature verification stub, OrderPaid wiring via Ordering `PaymentConfirmPort` adapter, Persian error messages, and inactive fee extension points for free Kerman pilot (ADR-091). No checkout UI (uiuxpromax N/A). In-memory repo + Drizzle `payments` stub.

## Iranian First gate

| Check | Result |
| --- | --- |
| Persian text? | Yes — payment/domain error messages fa |
| RTL? | N/A — no UI this cycle (checkout → ARD-012/034 + uiuxpromax) |
| Jalali? | N/A — no date display; timestamps UTC in domain |
| تومان? | Money as IRR minor units; presentation formatting later; fee display inactive |
| Iranian behavior? | Iranian PSP port (no Stripe-as-default); pickup OrderPaid path |
| Iranian mobile? | N/A domain; PSP redirect/WebView patterns → ADR-084 |

## Task breakdown

1. `src/payments-domain/` — decision snapshot (statuses, PaymentGateway, fees inactive, Persian copy)
2. `src/modules/payments/domain/` — PaymentIntent aggregate, statuses, repo port, events (PaymentIntentCreated / optional)
3. Persian `PaymentsDomainError` messages
4. Application: PaymentGateway port; createIntent; confirm via sandbox; verifyWebhookSignature; refund stub; FeePolicy inactive (ADR-091)
5. Sandbox/mock PaymentGateway adapter; `createSandboxPaymentConfirmPort` for Ordering
6. Wire Ordering default `paymentConfirm` → sandbox adapter (no circular import)
7. In-memory PaymentRepository; Drizzle `payments` stub; schema index + drizzle test
8. Tests: intent happy path, sandbox confirm → markPaid, webhook verify fail, fees off, Persian errors, no real PSP
9. Validate → STATUS completed → progress-log **Next: ADR-087**
10. ARD-012 — domain foundations; remain `todo` until API routes + migrations

## Schema / API / UI / events / cache

- Persistence: ports + in-memory; Drizzle stub `payments` (migrations → ARD-012)
- API: use cases ready; HTTP `/api/v1/payments/*` → ARD-012
- UI: none — uiuxpromax N/A
- Events: OrderPaid via ordering integration; PaymentIntentCreated reserved/optional
- Cache: no aggressive intent caching
- Fees: `FeePolicy.active = false` / zero charged for Kerman pilot

## Test matrix

- Create PaymentIntent → requires_payment / pending
- Sandbox confirm → succeeded + PaymentConfirmPort.confirmed
- Ordering markPaid with sandbox adapter → OrderPaid
- Webhook signature reject when invalid
- Idempotent confirm by provider_ref / intent id
- Fee policy inactive (tx fee 0 / not charged)
- Persian error codes/messages
- Reject Stripe-as-provider assumption in contract
- Drizzle payments schema export
- `npm run validate`

## Related ARD checkboxes

- ARD-012 — port + mock + OrderPaid wire; remain `todo` until intents/webhooks HTTP + migrations
- ADR-084 — remains Proposed (sandbox only)

## Iranian feature checklist (in scope)

- Error messages Persian: yes
- تومان via IRR minor money: yes
- Primary UI / RTL / mobile shell / Jalali: N/A — domain only
- Iranian PSP port (mock), no Western PSP default: yes

## Gates

- Telemetry: OrderPaid already named; payment failure analytics later
- Persistence: schema stub + ports
- uiuxpromax: N/A
- No real PSP / ADR-084 Proposed: enforced (sandbox provider id only)

## Completion notes

- Implemented as planned; `npm run validate` green (506 tests)
- Iranian feature checklist: Persian domain errors + status labels; IRR minor money; fees inactive (پایلوت رایگان کرمان); UI/RTL/Jalali N/A (uiuxpromax N/A); no Stripe-as-default
- `src/payments-domain` contract; `src/modules/payments` PaymentIntent + PaymentGateway sandbox/mock + webhook verify + Ordering PaymentConfirmPort adapter; Ordering default markPaid → sandbox
- Events: PaymentIntentCreated/Succeeded/Failed/Refunded + OrderPaid includes paymentId
- Drizzle stub `payments` (migrations → ARD-012)
- STATUS → `completed`
- Next: ADR-087 Customer Dashboard Architecture
