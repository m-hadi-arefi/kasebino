# Plan — ADR-108 Redis Cache-Aside and Rate-Limit Runtime

## Summary

Wire a live **`redis` (node-redis) client** from Compose `REDIS_URL` into MerchantOS: Redis adapters for `CacheAsideStorePort` + `RateLimitRedisPort`, swap production composition defaults from in-memory to Redis, keep OTP/auth **fail-closed** and cache reads **fail-open**, enforce Persian `RATE_LIMITED` envelopes on OTP routes, and invalidate storefront/product keys on ProductUpdated (existing ADR-054 maps + mutation enqueue).

**Status:** `in_progress` — operator asked **not** to move ADR to `adrs/done/` this cycle.

## Iranian First gate

| Check | Result |
| --- | --- |
| Persian text? | Yes — OTP/login surfaces existing `RATE_LIMIT_MESSAGES_FA` / envelope `messageFa`; UI already displays `error.message` |
| RTL? | Existing login shells `lang=fa` / `dir=rtl`; no new LTR layout |
| Jalali? | N/A (rate-limit / cache infra; no date surfaces) |
| تومان? | N/A |
| Iranian behavior? | OTP abuse protection 3/min fail-closed; hashed phone/IP subjects in Redis keys |
| Iranian mobile? | Login already ≥44px; 429 Persian copy readable on mobile |

## Telemetry / Persistence / uiuxpromax gates

| Gate | Result |
| --- | --- |
| Telemetry | Optional `RateLimitTriggered` already defined in ADR-055; emit polish → ADR-116. N/A for cache hits. |
| Persistence | None (Redis only; PG remains SoT). |
| uiuxpromax | Brief below — light UX surface (error message display already present); no new page composition. |
| Delivery | N/A. |

## Dependency notes

| Dep | Status | ADR-108 approach |
| --- | --- | --- |
| ADR-051–055 | done | Ports, policies, key builders, invalidation maps exist |
| ADR-094 | done | Reuse `enforceRateLimit` + OTP handlers |
| ADR-109 | tasks | Inline best-effort invalidation via `enqueueDomainEvent` already; worker consumers later |
| ADR-112 | tasks | Ready probe can ping Redis later; this ADR documents connect + fail modes |

## uiuxpromax brief (fa-IR / RTL — rate-limit errors only)

- **Persona:** صندوقدار کرمانی وقتی بیش از ۳ بار در دقیقه درخواست OTP می‌زند پیام فارسی محدودیت نرخ می‌بیند.
- **Context:** ورود با پیامک، کد تایید، محدودیت نرخ
- **Screens:** merchant `/login`, customer storefront login — no layout redesign; ensure 429 `error.message` Persian remains visible
- **States:** Rate limited → «تعداد درخواست‌های کد تایید بیش از حد مجاز است…» / auth variant; fail-closed Redis down → «سرویس موقتاً در دسترس نیست…»
- **Assert:** envelope FA scripts in tests; login forms already bind `body.error?.message`

## Tasks

1. **Plan** (this file) — before code
2. **Dependency:** add `redis` package; extend `src/infrastructure/redis` connect + adapters
3. **Composition:** `createProductionApiContext` uses Redis cache + rate limiter when `REDIS_URL` connects; document mock/memory path for tests
4. **HTTP:** update rate-limit meta notes (`redis` vs memory); ensure OTP 429 envelope + Retry-After
5. **Invalidation:** confirm ProductUpdated → storefront/product key deletes against Redis store (catalog enqueue path)
6. **Cache-aside:** production analytics/catalog paths use Redis store; hit path integration test
7. **Persian UX:** verify OTP login shows Persian rate-limit message (existing binding + FA messages)
8. **Tests:** Redis hit, OTP threshold 429, ProductUpdated invalidation; skip-live when Redis down with documented mock path
9. **`npm run validate`** with `DATABASE_URL=postgres://merchantos:merchantos@localhost:5433/merchantos`
10. Update STATUS notes + tech docs — **do not** move ADR to `done/`

## Schema / API / UI / events / cache

| Layer | Change |
| --- | --- |
| Schema | None |
| API | Behavior: Redis-backed rate limits; meta.rateLimit reflects mode; 429 `RATE_LIMITED` FA |
| UI | Surface existing FA error messages (no new screens) |
| Events | ProductUpdated invalidation via existing enqueue → `invalidateOnEvent` |
| Cache | Live Redis GET/SETEX/DEL adapters; compose `REDIS_URL` |

## Fail policies (documented)

| Path | Redis down |
| --- | --- |
| Cache-aside reads | **fail-open** → loader/PostgreSQL |
| OTP / auth rate limit | **fail-closed** → deny + Persian unavailable message when `deniedFailClosed` |
| default / admin / storefront RL | **fail-open** |

## Mock + live Redis paths

| Mode | When | How |
| --- | --- | --- |
| **Live** | Compose `redis` up; `REDIS_URL=redis://localhost:6379` (host) or `redis://redis:6379` (in-network) | Production composition connects on boot |
| **Mock / memory** | Unit tests; `MOS_REDIS_MODE=memory`; or Redis unreachable + explicit memory fallback for non-prod | `InMemoryCacheAsideStore` + `InMemoryRateLimitStore` |
| **Injected** | HTTP handler tests | `rateLimitMode: "injected"` + custom limiter |

## Test matrix

- [ ] Cache `getOrLoad` miss→SET then hit served from Redis (`kind: "hit"`)
- [ ] OTP request >3/min → 429 + `RATE_LIMITED` + Persian message
- [ ] ProductUpdated deletes storefront + product keys from Redis
- [ ] `createRedisConfigFromEnv` / production wire uses `REDIS_URL`
- [ ] Fail-closed OTP when Redis port throws
- [ ] `npm run validate`

## Related ARD / ADR

- ADR-051–055 contracts; ADR-094 OTP routes; ADR-109 worker still deferred for outbox consumers
- Update `docs/tech/redis.md` with live client placement

## Iranian feature checklist (in scope)

- [x] Persian rate-limit / OTP abuse messages
- [x] RTL login shells unchanged
- N/A: Jalali, تومان, maps

## Risks

- Incorrect fail-open on auth/OTP — mitigated by fail-closed policy + tests
- Redis not running in CI — dual path tests: always run memory/adapter unit; live Redis suite runs when ping succeeds
- Thundering herd — keep in-process single-flight (ADR-052)

## Post-complete (deferred — operator request)

When AC confirmed: move `adrs/tasks/ADR-108-*` → `adrs/done/`, update STATUS + REORGANIZATION_INDEX + progress-log.

## Acceptance mapping

| AC | Plan evidence |
| --- | --- |
| Cache hit from Redis (integration) | Live Redis `getOrLoad` test (+ mock adapter parity) |
| OTP rate-limit envelope after threshold | HTTP OTP handler test with Redis or memory limiter at policy limits |
| Product update invalidates storefront/product keys | `invalidateOnEvent` / catalog enqueue against Redis store |
| Compose `REDIS_URL` used | Production composition + `createRedisConfigFromEnv` |
