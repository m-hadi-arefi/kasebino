# Plan — ADR-087 Customer Dashboard Architecture

## Summary

Encode store-scoped customer portal architecture in `src/customer-dashboard/`: membership-scoped profile/points/history/rewards/receipts visibility; auth required (`role=customer`); no cross-store leak; Persian RTL stubs under storefront `/s/{storeSlug}/dashboard` with `orders` + `wallet` routes; تومان formatting + Jalali date note; reserve `LoyaltyWalletViewed` / `ReceiptViewed` analytics events. APIs remain ARD-035 remainder.

## Iranian First gate (AGENT 1–6)

- Persian text: yes — dashboard chrome, nav, empty/loading/error/auth stubs fully `fa-IR`
- RTL: yes — inherits storefront `dir=rtl` / `lang=fa`; mirrored nav
- Jalali: yes — history stub notes dates in Jalali (`Asia/Tehran`)
- تومان: yes — wallet/history money labels use تومان
- Iranian behavior: SMS OTP member self-serve; store-scoped membership; pickup order history (no delivery)
- Mobile: PWA-first ~390px Android; touch nav ≥44px
- Checklist (stubs + contract): Persian primary copy; RTL shell; empty/auth states; تومان; Jalali note; cross-store authZ contract

## uiuxpromax gate evidence

**Binary:** may be absent. Skill + `docs/uiux/*` evidence used. Gate: brief in this plan before UI stubs.

**Brief (Persian + RTL, mobile-first customer dashboard):**

| Item | Detail |
| --- | --- |
| Surface | پنل مشتری فروشگاه — خانهٔ داشبورد، سفارش‌ها، کیف امتیاز؛ فقط با عضویت همان مغازه |
| Persona | مشتری محلی `fa-IR`؛ عضو فروشگاه از OTP/QR؛ مشاهده امتیاز و تاریخچه بدون مراجعه به صندوقدار |
| Mobile | عرض ~390px Android؛ ناوبری داشبورد منطقی؛ لینک‌ها ≥۴۴px؛ PWA-first |
| Screen list | داشبورد · سفارش‌ها (stub) · کیف امتیاز (stub) |
| States | بدون ورود: «برای مشاهدهٔ پنل وارد شوید.»؛ خالی سفارش: «هنوز سفارشی ندارید.»؛ خالی کیف: «هنوز امتیازی ندارید.»؛ بارگذاری/خطا فارسی |
| Component map | هدر عضویت · ناو RTL · کارت‌های خلاصه امتیاز/سفارش · بدون کروم بازرگان |
| RTL / Persian | `dir=rtl` `lang=fa`؛ کپی کامل فارسی؛ بدون delivery |
| Copy samples | «پنل من» · «سفارش‌های من» · «کیف امتیاز» · «مبالغ به تومان» · «تاریخ‌ها به تقویم شمسی» |
| Iranian retail | مغازه · مشتری · امتیاز وفاداری · سفارش پیکاپ (نه صندوقدار POS) |
| A11y | عناوین معنایی؛ nav با `aria-label`؛ حالت بدون ورود قابل درک با صفحه‌خوان |
| Out of scope | Live wallet/history APIs · JWT middleware wire · receipt PDF · redeem UI · merchant chrome |

`assertUiuxGate`: `gatePassed` + skill/docs present + brief Persian/RTL/390/retail/states/a11y.

## Database design

- N/A new tables — read models over membership/wallet/orders/receipts later via ARD-035 APIs.
- Cache: wallet/history TTL 60–300s (reuse `CACHE_RESOURCE_TTL.wallet` / hot entity 300s).

## Domain / application changes

- `src/customer-dashboard/` — Decision + helpers:
  - Store-scoped portal via active StoreMembership
  - Surfaces: home / orders / wallet (+ rewards/receipts path contracts)
  - Auth required: customer JWT role only; refuse merchant/staff
  - AuthZ: membership storeId must match route store; no cross-store leak
  - API path contracts from ARD-035 (handlers deferred)
  - Events: `LoyaltyWalletViewed`, `ReceiptViewed` reserved
  - Persian copy + تومان + Jalali date note
  - `assertUiuxGate` evidence export
- Routes under `app/(storefront)/s/[storeSlug]/dashboard/`:
  - Home stub (fa)
  - `orders/page.tsx` stub (fa)
  - `wallet/page.tsx` stub (fa)
- Sync `APP_ROUTER_FILESYSTEM.storefront` dashboard paths
- Link from storefront home nav to dashboard
- ARD-035 note: architecture + route stubs landed; APIs remain

## API / UI / events / cache

- No Route Handlers this cycle (ARD-035 APIs remain)
- UI: Persian RTL dashboard stubs (auth-required messaging)
- Events: names reserved (no Mongo emit)
- Cache: wallet TTL class documented

## Gates

- Telemetry: WalletViewed / LoyaltyWalletViewed + ReceiptViewed names only
- Persistence: N/A (read models later)
- uiuxpromax: brief above before UI stubs
- No delivery; never merchant staff chrome / ADR-022 paths
- Prerequisites: ADR-007, ADR-010, ADR-032, ADR-023 completed

## Test matrix

- Unit: membership-scoped; rejects cross-store access
- Unit: auth required; customer role only; rejects merchant role
- Unit: API path contracts; events reserved
- Unit: filesystem routes exist; Persian copy / تومان / Jalali note / RTL lang
- Unit: `assertUiuxGate` / `assertCustomerDashboardUiuxGate` passes
- Unit: no merchant dashboard chrome / no delivery
- `npm run validate`

## Related ARD checkboxes

- ARD-035: architecture contract + Persian route stubs; APIs/auth middleware/live data remain

## Iranian feature checklist (in scope)

- Persian UI copy — yes
- RTL/`lang=fa` — yes
- Empty/loading/auth Persian — yes
- تومان — yes
- Jalali date note — yes
- Mobile-first / touch nav — yes
- Live receipt/API/OTP screen polish — deferred (stubs)

## Risks

- Real JWT session gate not enforced in middleware yet — stubs document auth required; wire with ARD-030 remainder
- Temptation to share merchant `/dashboard` — forbidden; customer portal lives under storefront only

## Completion notes

- Implemented as planned (2026-08-03)
- `src/customer-dashboard/` — membership-scoped portal, auth required (`role=customer`), no cross-store leak, ARD-035 API path contracts, wallet cache TTL 60–300s, `LoyaltyWalletViewed`/`ReceiptViewed` reserved, Persian copy + تومان + Jalali note, `assertUiuxGate` evidence
- Routes: `/s/{storeSlug}/dashboard` + `orders`/`wallet` Persian RTL stubs; storefront home nav link «پنل من»; `APP_ROUTER_FILESYSTEM` synced
- ARD-035 STATUS note updated (APIs / JWT middleware / live read models remain)
- Tests: 8 new; `npm run validate` green (514 tests)
- Iranian checklist: Persian copy; fa/rtl shell; auth/empty stubs; تومان; Jalali date note; mobile nav ≥44px; pickup-only history; live wallet APIs N/A
- STATUS → `completed`; Next: ADR-022 Merchant Staff PWA Architecture
