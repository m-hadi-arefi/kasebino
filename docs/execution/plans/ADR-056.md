# Plan — ADR-056 MongoDB Analytics and Telemetry Plane

## Summary

Encode MerchantOS MongoDB as the **analytics / audit / telemetry plane** (never OLTP SoT) in `src/mongodb-analytics/`: analytics-only role, locked collection naming (`mos_*`), document envelope shape, `MONGODB_URL` connection (compose + `.env.example`), tenant filter + admin-gate notes, checkout failure isolation pointer → ADR-065. Thin URL-resolving client stub under `src/infrastructure/mongodb/`. Compose `mongo` already from ADR-066 — verify only. Warehouse mirror / idempotent ingest / protocol driver → ADR-057+.

## Iranian First gate

| Check | Impact |
| --- | --- |
| Persian text? | No UX screens this cycle; dashboard labels remain Persian in ADR-014 UX contract (event codes English OK) |
| RTL? | N/A visual; chart RTL deferred to dashboard ADRs |
| Jalali? | Time buckets for merchant-facing analytics remain Jalali/`Asia/Tehran` (ADR-014 contract; ingest stores ISO UTC) |
| تومان? | Money truth stays PG; Mongo never money SoT |
| Iranian behavior? | Platform/management analytics admin-only; merchants not blocked by telemetry plane |
| Iranian mobile? | N/A screens; ingest stays off checkout critical path (POS on Iranian Android devices) |

Iranian checklist: UX N/A (infra/architecture plane). Preserve UTF-8 Persian in telemetry string payloads; human dashboards → later ADRs.

## Gates

- Telemetry: this ADR *is* the Mongo telemetry plane foundation; emit/ingest ports → ADR-057+
- Persistence: Mongo analytics-only; PG remains OLTP SoT; no Drizzle-on-Mongo
- uiuxpromax: N/A (no UI)
- Delivery: no courier/delivery
- Prerequisites: ADR-014, ADR-004 completed

## Task breakdown

1. STATUS `in_progress`; write this plan
2. `src/mongodb-analytics/` — engine/plane (analytics-only), never OLTP SoT / never money-stock SoT, collection map, envelope fields, `MONGODB_URL`, tenant+admin notes, off-critical-path + failure isolation defer ADR-065, align compose/env/analytics-boundaries, asserts
3. `src/infrastructure/mongodb/client.ts` — thin stub (URL resolve only; no protocol connect; no official driver dep yet)
4. Verify compose `mongo` + `MONGODB_URL` in `.env.example`
5. Flip ADR-014 `mongoPlaneDeferredTo` note / ARD-021 foundation checkbox notes
6. Tests + `npm run validate` → STATUS completed → progress-log Next: ADR-065

## Schema / API / UI / events / cache

- Schema: lock logical collection names (`mos_events`, `mos_audit`, `mos_product`, `mos_behavior`, `mos_security`, `mos_mgmt`); no live index bootstrap this cycle
- API: reserve health path note `GET /api/v1/analytics/health/mongo` (implement later with ingest)
- UI: none
- Events: warehouse via outbox `mongodb_warehouse` remains off critical path; mirror → ADR-057
- Cache: N/A

## Test matrix

- Plane = mongodb_analytics; never OLTP SoT; forbidden money/stock/membership SoT
- Collection naming map locked + asserts
- Envelope required fields present
- `MONGODB_URL` documented in compose + `.env.example`; stub validates scheme
- Compose ships `mongo` (port 27017, healthcheck, volume, analytics DB name)
- Align with `COMPOSE_DATA_PLANES.mongo` + `ANALYTICS_BOUNDARIES` mongo plane
- Failure isolation / POS unaffected → deferred ADR-065 (pointer + assert)
- Tenant filter + platform_admin gate notes
- Iranian: UTF-8 Persian payload safety; Jalali display deferred
- `npm run validate`

## Related ARD checkboxes

- ARD-021 — Mongo plane + Compose + `MONGODB_URL` + collection naming foundation; ingest ports / outbox bridge / indexes remain (ADR-057+)

## Iranian feature checklist (in scope)

- UX copy / RTL / mobile / تومان / Jalali display UI: N/A — plane contract only
- UTF-8 Persian in telemetry payloads: documented
- Merchant Jalali reports: remains ADR-014 / dashboard ADRs

## Risks

- Over-implementing warehouse ingest / official `mongodb` driver / index bootstrap → defer ADR-057+
- Using `MONGODB_URI` vs repo `MONGODB_URL` — contract must match compose/env-secrets
- Treating Mongo as SoT → explicit forbids + asserts

## Completion notes

- Implemented `src/mongodb-analytics` — analytics-only plane, never OLTP SoT, `mos_*` collection naming, canonical envelope, `MONGODB_URL`, tenant filters + platform_admin gates, failure isolation deferred to ADR-065, warehouse ingest deferred to ADR-057; thin `src/infrastructure/mongodb/client.ts` stub; compose mongo verified
- Validate green (573 tests); STATUS → completed; Next: ADR-065
- Iranian checklist: UX N/A (plane contract); UTF-8 Persian payload safety + Jalali presentation pointer (ADR-014)
- Flipped ADR-014 `mongoPlaneDeferredTo` → `mongoPlaneImplementedIn`; ARD-021 foundation notes + Compose/SoT acceptance checkboxes
