# Plan — ADR-002 Domain-Driven Design Strategy

## Summary

Encode strict DDD as a testable **layering + modeling contract**: module folder layout (`src/modules/<context>/{domain,application,infrastructure}`), inward dependencies, domain events (past tense), repository ports in domain / adapters in infrastructure, and shared domain primitives. Scaffold empty context module shells for later ADRs; no business aggregates yet (those land with domain ADRs).

## Iranian First impact

- Persian text: code ubiquitous language English; presentation/events resolve to Persian later (documented)
- RTL / Jalali: N/A UI
- تومان: shared `Money` VO stores IRR minor units; display unit defaults from ADR-001 (`toman`)
- Iranian phone: branded `PhoneNumber` VO placeholder (normalization in later auth ADRs)
- Checklist: UI N/A; money/phone VO conventions covered

## Database design

- N/A — repository **interfaces** only as typing conventions; no Drizzle yet (ADR-042)

## Domain changes

- `src/shared/ddd/` — layers, dependency rules, `DomainEvent`, aggregate event collection helpers, `EntityId` branding helper
- `src/shared/domain/` — Iranian retail VOs: `Money`, `PhoneNumber` (minimal validation stubs)
- `src/modules/<context>/{domain,application,infrastructure}/` shells for contexts from system overview
- Export `DDD_STRATEGY` snapshot for tests

## API / UI / events / cache

- N/A runtime; domain event **shape** only

## Gates

- Telemetry: N/A (events feed warehouse later)
- Persistence: ports only
- uiuxpromax: N/A
- No delivery: obey ADR-015 (no delivery aggregates)

## Test matrix

- Unit: module path pattern + required layers
- Unit: domain must-not-import list includes drizzle/next/react/mongo
- Unit: event names must be past-tense pattern (examples + helper)
- Unit: Money uses IRR minor units + toman display default from product architecture
- Unit: PhoneNumber rejects empty
- `npm run validate`

## Related ARD checkboxes

- ARD-001 foundation partial; remain `todo` until Compose/Next/etc.

## Risks

- Over-modeling aggregates early → only shells + shared primitives
- ADR-003 owns context map details → list folders aligned with system overview without locking map semantics beyond folder names

## Completion notes (2026-08-03)

- Implemented as planned; `npm run validate` green (20 tests)
- Iranian feature checklist: UI N/A; Money/PhoneNumber Iranian conventions in shared domain
- STATUS → `completed`
