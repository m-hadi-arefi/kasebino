# Plan — ADR-092 Generate and Apply Drizzle Kit OLTP Migrations

## Summary

Add identity OLTP tables (merchant auth users, customer identity, OTP challenges), generate a drizzle-kit baseline migration covering all existing schema modules + identity, upgrade CI to fail on schema drift without migration, document expand/contract for future breaking changes, and prove Persian UTF-8 round-trip on Compose Postgres.

## Iranian First impact

- Persian text: UTF-8 `text` columns preserved; no ASCII-only collations in migration SQL
- RTL: N/A (migration layer)
- Jalali: timestamps UTC `timestamptz`; display later
- تومان: money remains bigint IRR minor units
- Iranian phones: identity/membership `phone_national` / `phone_e164` columns + indexes for lookup
- Mobile: N/A at DDL layer
- Checklist: UX N/A (infra); Persian data safety + phone column shapes in scope

## Schema changes

New `src/infrastructure/database/schema/identity.ts`:

- `auth_users` — merchant staff AuthUser (phone national/e164 unique, token_version)
- `merchant_otp_challenges` — hashed OTP only; phone indexes; expires_at
- `customer_identities` — customer audience identity (phone unique)
- `customer_otp_challenges` — customer OTP hashed; isolated from merchant

Export from `schema/index.ts`. Baseline SQL via `npm run db:generate` only (no hand-authored baseline).

## API / UI / events / cache

- None (uiuxpromax N/A)
- Telemetry: N/A

## Task breakdown

1. Identity schema + indexes (phone lookup)
2. `npm run db:generate` → commit SQL under migrations/
3. CI: schema-drift gate fails if generate would add/change SQL
4. Expand/contract notes in migrations README + migration-strategy assert
5. Integration test: migrate + Persian trade_name round-trip (gated or Compose)
6. Document apply via `npm run db:migrate` for local Compose

## Test matrix

- Unit: identity tables exported; migration SQL committed (not only .gitkeep)
- Unit: CI workflow includes schema-drift check
- Unit/integration: no Prisma/TypeORM; drizzle-kit only
- Integration (Compose when available): migrate + insert/select Persian text
- `npm run validate`

## Related ARD checkboxes

- ARD-001 / ARD-002 / ARD-003 / ARD-030: migrations generated note

## Risks

- Large baseline lock on fresh DBs only (acceptable for greenfield)
- Compose not running in CI → gate SQL presence + optional local integration

## Post-complete

- Move `adrs/tasks/ADR-092-*` → `adrs/done/`
- Next: ADR-093 Drizzle repositories
