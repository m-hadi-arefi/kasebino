# Plan — ADR-003 Bounded Context Design

## Summary

Encode the MVP **bounded context map** as an importable contract: context ids, module folders, integration styles (OHS/ACL/Partner/Conformist), storefront ACL (no POS domain imports), merchant vs customer identity split, Ordering=pickup-only, Analytics OLTP vs Mongo platform planes. Align module shells with ADR-003 (add `customer-identity`).

## Iranian First impact

- Persian/RTL/Jalali UI: N/A
- تومان / phone: contexts that own money/membership (POS, CRM, Loyalty, Ordering) noted
- Iranian workflows: Ordering context locked to pickup fulfillment; no delivery context
- Checklist: UI N/A; workflow encapsulation in context map tests

## Database design

- N/A (context ownership of future tables only; no schemas)

## Domain changes

- `src/bounded-contexts/` — `BOUNDED_CONTEXTS`, `CONTEXT_INTEGRATIONS`, ACL helpers
- Scaffold `src/modules/customer-identity/{domain,application,infrastructure}`
- Update ADR-002 `BOUNDED_CONTEXT_MODULES` to include `customer-identity` (map alignment)
- Explicitly forbid a `delivery` context

## API / UI / events / cache

- N/A runtime; published language = events (documented in contract)

## Gates

- Telemetry: Analytics(Platform/Mongo) + Audit remain non-OLTP SoT
- Persistence: N/A
- uiuxpromax: N/A
- No delivery: Ordering pickup-only; no delivery context

## Test matrix

- Unit: all ADR-003 decision contexts present
- Unit: customer identity ≠ merchant identity modules
- Unit: storefront ACL forbids POS domain import path
- Unit: ordering fulfillment = pickup
- Unit: analytics_oltp plane=postgresql; analytics_platform plane=mongodb
- Unit: module folders exist for mapped contexts
- `npm run validate`

## Related ARD checkboxes

- Note only in progress log

## Risks

- Over-mapping unused edges → keep textual map edges from 03-bounded-contexts.md only
- Dual analytics contexts sharing `analytics` module folder — document via `plane` field

## Completion notes (2026-08-03)

- Implemented as planned; `npm run validate` green (27 tests)
- Iranian feature checklist: UI N/A; pickup/membership/customer-identity modeled in context map
- STATUS → `completed`
