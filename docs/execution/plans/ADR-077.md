# Plan — ADR-077 API Protection and Data Protection

## Summary

Implement MerchantOS **API Protection and Data Protection** as an importable contract in `src/api-protection/`: Zod validation-at-boundary stance, AuthZ + rate-limit composition pointers, CORS locked to app origins, public storefront DTO ACL (output minimization — no cost/PII/internal fields), Iranian phone PII handling (mask/hash for logs/telemetry, never on public DTOs), soft-delete default-exclude on API reads, CSRF/Next.js server-actions + SameSite stance, encryption-at-rest notes deferred to infra, Idempotency-Key owned by ADR-030 (`src/api-standards`) — referenced only. No Next.js middleware/route wiring this cycle (consume package when handlers land).

## Iranian First impact

- Persian text: Yes — validation / forbidden-field / phone invalid messages (`API_PROTECTION_MESSAGES_FA` + ADR-030 codes)
- RTL: UI N/A (no screens); Unicode-safe Persian strings in envelopes
- Jalali: N/A (no date display)
- تومان: N/A
- Iranian workflows: phone PII treat-as-sensitive (`09…` / `+98`); public storefront never leaks customer phones
- Mobile: payload minimization + no cost/PII keep storefront responses lean for Iranian mobile
- Checklist: error messages Persian in scope; UI/RTL/shell N/A

## Schema / API / UI / events / cache

- Persistence: soft-delete API defaults compose ADR-047; encryption-at-rest deferred (volume/managed PG)
- API: validation boundary helpers, public DTO ACL asserts, CORS allowlist, Persian protection messages
- UI: none — uiuxpromax N/A
- Events: N/A (abuse signals already RateLimitTriggered / AuthZ deny → ADR-055 / ADR-058)
- Cache: no PII in keys beyond hashed phone (align ADR-052 / rate-limiting)

## Domain / application changes

- `src/api-protection/` — decision snapshot, input validation stance, output minimization / storefront ACL, Iranian phone PII, CORS, CSRF/server actions, soft-delete defaults, encryption-at-rest deferral, compose AuthZ/rate-limit/idempotency, asserts + Persian messages
- Sync: `security-architecture` VALIDATION_AND_DATA → ADR-077 realized; `api-standards` SECURITY pointer; `06-security-architecture.md` / `15-api-architecture.md` / `api-rules.md` / `security-rules.md`; ARD-010 / ARD-020 notes

## Gates

- Telemetry: PII scrub/mask for phones; emit abuse remains ADR-055 / ADR-058
- Persistence: soft-delete defaults (ADR-047); encryption-at-rest infra deferred
- uiuxpromax: N/A
- No delivery: N/A
- Prerequisites ADR-076 + ADR-030 + ADR-055 completed

## Test matrix

- Unit: decision locks Zod + AuthZ + rate limits + CORS + no sensitive public DTO fields + soft-delete defaults
- Unit: public DTO ACL rejects costPrice / phone / deletedAt / OTP / tokens
- Unit: Iranian phone mask + hash (09… forms); public DTO never includes national/e164
- Unit: CORS allowlist denies unknown origins; allows configured app origins
- Unit: CSRF stance = Next.js server actions + SameSite (secure cookies)
- Unit: encryption-at-rest marked deferred (infra); app still hashes OTP (ADR-076)
- Unit: Idempotency-Key owned by api-standards (sale_complete / order_create)
- Unit: Persian protection messages contain Persian script
- Unit: soft-deleted rows not listed by default on API reads
- `npm run validate`

## Related ARD checkboxes

- ARD-010 Storefront — public DTO ACL / no cost+PII leakage contract ready; route handlers remain
- ARD-020 Production Hardening — API protection contract landed; pen smoke / middleware remain M6
- ARD-008 CRM — phone PII scrub helpers ready for logs when CRM surfaces land

## Iranian feature checklist (in scope)

- Error messages Persian (validation / protection)
- Iranian mobile PII rules (mask/hash; public DTOs exclude)
- Primary UI / RTL / mobile shell / تومان / Jalali: N/A — no UX screens

## Risks

- Live CORS / Zod middleware not attached yet — must not claim route matrix wired
- Storefront field ACL may grow when catalog DTOs land (ADR-008+) — extend allowlist carefully

## Completion notes

- Implemented as planned; `npm run validate` green (254 tests)
- Iranian feature checklist: Persian validation/CORS/protection messages; Iranian phone mask/hash + never on public DTOs; UI/RTL/تومان/Jalali N/A (uiuxpromax N/A)
- `src/api-protection` — boundary Zod stance, public storefront DTO ACL, Iranian phone PII, CORS lock, CSRF/Server Actions, soft-delete defaults, encryption-at-rest infra deferral; Idempotency-Key referenced from ADR-030
- Next.js CORS/Zod middleware / route matrix attachment remains for handlers / ARD-020
- STATUS → `completed`
- Next: ADR-008 Catalog and Inventory Domain (Phase C start)
