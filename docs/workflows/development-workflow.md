# Development Workflow

1. Identify work via ARD status (not ad-hoc).
2. Run ard-to-code (or manually obey the same steps).
3. Keep changes scoped to the ARD.
4. Validate fully.
5. Update STATUS + progress-log.
6. Self-improve docs per `docs/execution/self-improvement.md`.
